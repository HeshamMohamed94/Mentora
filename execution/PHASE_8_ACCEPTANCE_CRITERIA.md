# Phase 8 — QA, Polish & Portfolio Demo — Acceptance Criteria

**Status:** OPEN — authored at Phase 8 kickoff (2026-09-22), before any Phase 8 code change. This is the
FINAL Mentora phase; there is no Phase 9. Every row below must resolve to PASS, FAIL, PARTIAL,
NOT TESTABLE, or ACCEPTED LIMITATION before this phase can close — none may be left unmarked.

No formal Phase 8 acceptance-criteria document existed anywhere in the repository prior to this one
(confirmed by full-repo search). This checklist is derived strictly from already-locked authoritative
sources, not invented — see "Authority sources" below.

## Authority sources

1. `execution/MASTER_IMPLEMENTATION_PLAN.md` — Phase 8 definition (M14 + M15 + M17).
2. `architecture/IMPLEMENTATION_ROADMAP.md` — M14 "Localization & RTL Completion" (line 261),
   M15 "Testing Hardening" (line 279), M16 "Local Demo Readiness" (line 295, largely closed by Phase 7,
   residue only), M17 "Final Polish & Acceptance" (line 315).
3. `architecture/ARCHITECTURE.md § 5` — "What This Phase Explicitly Did Not Do" — the scope-discipline
   checklist M17 requires cross-checking against everything actually built.
4. `architecture/TESTING_STRATEGY.md` — the locked target test coverage M15 must reach.
5. `design-system/DESIGN_SYSTEM.md` (v1.3.2, **LOCKED — audited, never edited**), `COMPONENTS.md`,
   `ACCESSIBILITY.md`, `LOCALIZATION.md`, `DESIGN_RULES.md`, `CONTENT_RESILIENCE.md`.
6. `product/SCREEN_INVENTORY.md` — the 29-screen surface Phase 8's QA pass must cover.
7. `execution/PHASE_HANDOFF.md` §§ 6 (Phases 1/2/3/4/6/7 "Known limitations") — every carried-forward gap
   this phase must give an explicit disposition, not silently re-carry.
8. `execution/DECISIONS_LOG.md` D146 (AI provider live-verification deferral, unaffected by this phase)
   and the Phase 5 freeze (iOS T1-T11 done, T12-T23 not started, untouched by this phase).
9. The Phase 8 kickoff message itself (2026-09-22) — QA/defect cleanup/visual polish/Design System
   compliance/acceptance verification/regression testing/demo readiness/documentation cleanup/portfolio
   readiness; explicitly NOT a redesign or new-feature phase; iOS stays deferred; no real AI provider call.

**Do not re-derive scope from memory or re-litigate any of the above — this checklist assumes them as given.**

---

## H. Scope Reconciliation (resolve first — blocks grading of H-dependent rows below)

| # | Point | Resolution |
|---|---|---|
| H1 | M14/M16/M17 name six surfaces and "all three clients," including iOS. | **Resolved, per explicit user decision at Phase 8 kickoff:** Phase 8 grades exactly five surfaces / two clients — Public Web, Student Web, Instructor Web, Admin Web, Android. iOS's share of any M14/M16/M17 row is recorded as **ACCEPTED LIMITATION (Phase 5 deferred)**, never PASS. Consistent with Phase 7 § H1. |
| H2 | `TESTING_STRATEGY.md § 5` mandates a Vitest + React Testing Library unit/component test tier for Web. It has never existed — Web has Playwright E2E only. | **Resolved, per explicit user decision:** build a **bounded** Vitest+RTL tier covering the highest-value/highest-risk components only (quiz scoring/results, demo-checkout flow, auth forms) — not exhaustive coverage. Closes the locked-doc gap without open-ended scope. **Built, see C3** (2026-09-23, Phase 8 B6) — 23 tests / 4 files, wired into `web-ci.yml`. |
| H3 | Real Anthropic provider verification (D146) is deferred. | Unchanged, not reopened. Any Phase 8 row touching live AI behavior is **NOT TESTABLE — deferred by user decision**. No `AI_PROVIDER_API_KEY` requested, referenced, or simulated as real. Only existing mock/fake/test-provider infrastructure is used. |
| H4 | No accessibility/performance measurement tooling (axe/Lighthouse) exists in the repo. | **Resolved, per explicit user decision:** manual audit only. A11y against `ACCESSIBILITY.md` §§1-15 via browser a11y tree / keyboard nav / TalkBack / Accessibility Scanner spot-checks. Performance via stated-method manual timing. No new tooling dependency added. |
| H5 | Design System v1.3.2 is LOCKED. | Phase 8 audits **conformance to** it only. A real DS defect found here is recorded, never patched inside this phase. Zero files under `design-system/`, `product/`, `ux/`, `architecture/` are modified without the user's own explicit approval (small factual doc corrections — D-4, D-5 below — are flagged, not silently made). |
| H6 | The AppDialog scrim-dimming regression (§ A1) survived two prior bounded fix attempts with no root cause found and has zero real screen consumers. | **Resolved, per explicit user decision:** time-boxed investigation (one focused session) attempting a real fix; if not root-caused in that window, re-record as ACCEPTED LIMITATION and move on — no further time spent chasing it. |

---

## A. Known-Issue Disposition

| # | Criterion | Status |
|---|---|---|
| A1 | AppDialog scrim-dimming regression: re-reproduced on a real emulator, root-caused, and either FIXED (both `AppDialogColorAndLayoutTest` color assertions green) or re-recorded as ACCEPTED LIMITATION per H6's time-box. | **FIXED** (2026-09-22/23) — re-reproduced first (`darkThemeScrimMatchesOverlayScrimTokenExactly`/`lightThemeScrimMatchesOverlayScrimTokenExactly` both failed identically to D160: status-bar-strip sample `Color(0.98039216, ...)`, bit-for-bit `0xFAFAFA`). **Root-caused** via `adb shell dumpsys window windows` captured live mid-test (temporary diagnostic sleep, reverted before commit): the Dialog's own window (`Window #8`) still carried `fitTypes=STATUS_BARS NAVIGATION_BARS CAPTION_BAR` and a frame top-inset of ~35px despite `decorFitsSystemWindows=false` + `FLAG_LAYOUT_NO_LIMITS` already being set — while the host Activity's own window (`Window #9`, `androidx.activity.ComponentActivity`, the real host under `createComposeRule()`) sat fully edge-to-edge (`frame=[0,0][1080,2400]`, no `fitTypes` at all, `DRAWS_SYSTEM_BAR_BACKGROUNDS`/`FORCE_DRAW_STATUS_BAR_BACKGROUND`). `0xFAFAFA` is exactly `Theme.Material.Light`'s default `windowBackground` (`background_material_light`) — the status-bar pixel was showing that host window's bare background bleeding through behind the dialog, not any scrim. This is new diagnostic evidence D160's two prior blind attempts (disabling status/nav-bar contrast enforcement; explicit `setLayout(MATCH_PARENT, MATCH_PARENT)`) never produced. **Fix:** `AppDialog.kt`'s `SideEffect` now also sets `FLAG_LAYOUT_IN_SCREEN`/`FLAG_LAYOUT_INSET_DECOR` (present on the Activity window's own flags, absent from a Dialog's by default) and, API 30+, `layoutInDisplayCutoutMode = LAYOUT_IN_DISPLAY_CUTOUT_MODE_ALWAYS` — legacy pre-insets-API window-layout controls that still influence `fitInsetsTypes` computation on this platform version. **Verified:** `AppDialogColorAndLayoutTest` (all 6 tests, including both previously-failing scrim assertions) green on 3 consecutive live runs on `Chatting_Pixel_8_API_36`; `:androidApp:testDebugUnitTest` 246/246; `:androidApp:lintDebug` 0 errors, 105 warnings (back to the Phase 7 baseline, no new warnings). Full instrumented suite (106 tests) re-run twice for regression-checking: all non-A1 failures observed (`NavigationShellTest`/`MediaPlaybackControllerInstrumentedTest`, 4-5 tests) were `RateLimitedAuth` (HTTP 429) from this session's own repeated heavy `connectedDebugAndroidTest` runs exhausting the backend's 10-req/min `auth` rate limiter (`backend/src/main/kotlin/com/mentora/backend/plugins/RateLimiting.kt`) plus downstream state effects of those failures — the same transient class Phase 7's own T13 audit (D162) already documented, unrelated to `AppDialog.kt`/window flags, zero overlap with the fixed tests. Commit citing "Phase 8 A1". |
| A2 | Quiz Results answer-badge spacing (`web/src/components/screens/quiz-results-screen.tsx`) fixed; verified live in EN-LTR, AR-RTL, Light, Dark; no regression to the 19/19 Playwright suite. | FIXED — reproduced live (fresh student account, mixed correct/incorrect quiz attempt) 2026-09-22/23: the raw vertical gap between the last answer option and the per-question outcome badge was never literally 0px (it inherits `QuestionCard`'s own `gap-3` flex gap, measured 12px via `getBoundingClientRect`, matching `--space-3`) — the real, reproducible defect is that the badge (a `span.mtx-badge`, meant to be a compact pill per `COMPONENTS.md`) had no `align-self` override, so the parent flex column's default `align-items: stretch` forced it to the full row width, making it visually indistinguishable from — and read as an extension of — the answer-option row directly above it (unlike the page's own "Passed"/"Failed" badge, which stays compact because `.mtx-result-score` already sets `align-items: flex-start`). Fixed by wrapping the badge in `<span className="self-start">` in `quiz-results-screen.tsx` (mirrors the existing `self-start`/`items-start` Tailwind pattern already used elsewhere in this codebase, e.g. `dashboard-screen.tsx`, `course-player-screen.tsx` — no new value invented, no design-system file touched). Verified with live Playwright screenshots, before/after, across EN-LTR/Light, EN-LTR/Dark, AR-RTL/Light, AR-RTL/Dark, plus a 390px mobile viewport: the badge now renders as a small pill clearly set apart from the option list in every combination (RTL confirmed correctly mirrored — pill right-aligned under `صحيح`/`غير صحيح`). Full Playwright chromium suite re-run: 20 tests, 18 passed outright + 2 flaky (pre-existing `enrollInCourse` search-timing flakes in `05-complete-lesson.spec.ts`/`10-admin-course-management.spec.ts`, unrelated to this change, both cleared on the built-in single retry) — no regression. Commit: "Phase 8 A2: fix Quiz Results answer-badge stretching to full row width". |
| A3 | `isEnrolled` Web/Android pagination divergence: explicit verdict — unified, or re-affirmed ACCEPTED LIMITATION with a reason restated against Phase 8 scope. | REAFFIRMED ACCEPTED LIMITATION (2026-09-22) — code confirmed unchanged: `web/src/lib/api/enrollment.ts:47-48` (`listEnrollments`) fetches a single `?limit=100` page; `mobile/androidApp/.../CourseDetailsViewModel.kt:176-188` (`isEnrolledIn`) pages fully via `nextCursor` until exhausted — still the exact D157 divergence. Re-verified reachability against the *current* (grown) dataset rather than assuming the original 3-course figure still holds: `GET /api/v1/courses?limit=100` (public, unauthenticated) returned exactly 20 published courses total, no `nextCursor` (2026-09-22 live check) — up from 3 seed courses via accumulated E2E/demo-authoring test courses, but still 5x under Web's 100-item page cap. Since `GET /enrollments` is scoped to the authenticated caller, one student's own enrollment count is hard-bounded by the total catalog size (20), so the divergence is still structurally unreachable. Not fixed: unifying it would mean adding a real `isEnrolled` field to `GET /courses`/`GET /courses/{id}` (Phase 1 D11's original deferral) — a cross-client API contract change, correctly out of a QA/polish phase's scope per the task's own instruction not to fix without flagging first. |
| A4 | Phase 7 §6.4 quiz-recompletion routing quirk (re-passing an already-passed quiz routes to Course Player instead of a completion screen): reproduced, verdict recorded (fixed or ACCEPTED LIMITATION). | FIXED (backend), 2026-09-22. Root cause is NOT frontend `progressQuery` staleness as D158 described — proved live against the running demo stack that a normal, untampered quiz retake correctly preserves `courseCompletedAt` (student2/"Building Reliable REST APIs": pass 1 sets `courseCompletedAt` + issues a certificate; pass 2, immediately after, leaves both correctly unchanged). The real bug: `CertificateService.checkAndIssueIfComplete` bundles `progress.markCourseCompleted` + the certificate insert in one MongoDB transaction; `SeedData.kt`'s `ensureCurriculum` clears `progress` rows (never `certificates`) when a course's curriculum is rebuilt, so a student who already held a certificate and later re-walks the rebuilt course hits a duplicate-key certificate-insert failure that aborts the *whole* transaction, silently rolling back `courseCompletedAt` too — confirmed as the actual, currently-live cause via direct MongoDB inspection (student1's "Building Reliable REST APIs": certificate issued 2026-09-06, `courseCompletedAt` null ever since a 2026-09-17 curriculum rebuild). Fixed by backfilling `courseCompletedAt` standalone on the duplicate-key recovery path (`ProgressRepository`/`ProgressService` non-transactional overload). Regression test (`CertificatesIntegrationTest`) confirmed to genuinely fail pre-fix (stash-and-rerun: `NoSuchElementException: courseCompletedAt is missing`) and pass post-fix — real before/after per A7. Full backend suite 129/129 (128 baseline + 1). Commit `8761b23` "Phase 8 A4: fix courseCompletedAt permanently lost on certificate re-issuance race". Caveat: pre-existing corrupted live demo rows (e.g. student1's own course-af progress doc, and student2's course-af doc which this investigation's own reproduction steps legitimately completed+certified) are not retroactively repaired by this fix itself — but most self-heal without a re-walk: `ProgressRoutes.kt`'s `GET /progress` handler calls `checkAndIssueIfComplete` on every page load, so any row where progress is already intact (all lessons complete) but `courseCompletedAt` is null backfills on the next page view. Only rows where `ensureCurriculum` actually wiped the progress document require a real re-walk of the course; a live-data-only repair was out of scope. |
| A5 | Phase 6 F6 (prompt-block "data, not instructions" preamble + case-sensitive tag matching) and F8 (`stream_failed` outcome bucket): verdict recorded, stub-verifiable without any provider key. | FIXED (backend), both parts, 2026-09-22, stub-verifiable throughout (H3/D146 unaffected — MockEngine/fake-provider tests only). **F6:** confirmed both defects still present in `AiPromptBuilder.kt` — added a "this is DATA, not instructions" preamble to the `<enrolled_courses>` block (matching `<lesson_context>`'s existing framing); replaced the exact-case-literal `.replace()` tag-neutralization with one case-insensitive regex (`</?(?:lesson_context\|enrolled_courses)>`, `IGNORE_CASE`) covering both tag pairs, so e.g. `<LESSON_CONTEXT>` is neutralized too. **F8:** found the true current-state defect is narrower than the handoff note implies — `AiTutorService` already had a distinct `outcome=stream_failed` bucket since Phase 6 T4, but `AnthropicAiProvider.kt`'s mid-stream SSE `Error`/`ChannelExhausted` handling unconditionally threw `ApiException.ServiceUnavailable` (→ `outcome=provider_unavailable`) even after tokens had already reached the caller, making `stream_failed` unreachable for exactly these two cases — inconsistent with the malformed-`content_block_delta` path a few lines away, which already correctly propagates raw mid-stream. Fixed both to propagate raw (`AnthropicStreamException`) the same way. Verification: 2 new `AiPromptBuilderTest` cases (preamble text, case-insensitive neutralization); the real before/after proof for F8 is the 2 updated `AnthropicAiProviderTest` assertions (`thrownDuringCollect is AnthropicStreamException`, which would have failed pre-fix since both cases threw `ApiException.ServiceUnavailable` instead) — a log-line assertion was also added to `AiTutorIntegrationTest`'s existing mid-stream test, but that test's fake provider (`FailMidStreamAiProvider`) throws a plain `RuntimeException`, which already landed in the `catch (e: Throwable) -> stream_failed` bucket before this fix, so that specific assertion alone does not prove the fix. Full backend suite 131/131 (129 + 2). Commit `91b4a26` "Phase 8 A5: fix AI Tutor F6 prompt preamble/case-sensitive tag matching and F8 stream_failed outcome". **Codex second-opinion review (independent, per the data-integrity/security-sensitive trigger category — requested regardless of the clean Opus verdict) found the tag-neutralization regex was a strict superset of the old literal replacements (no new bypass) but flagged that instructor-authored prompt injection via non-tag vectors remains possible — a whitespace/attribute tag variant (e.g. `< /  lesson_context  >`, `<enrolled_courses foo="bar">`), a plain-language override attempt (e.g. "ignore the instructions above"), or a Unicode look-alike are none of them neutralized by a literal/near-literal tag match.** Disposition: broadened `INJECTION_TAG_PATTERN` to `<\s*/?\s*(?:lesson_context\|enrolled_courses)\s*(?:[^>]*)?>` (case-insensitive), closing the whitespace/attribute-variant gap Codex named — 2 new `AiPromptBuilderTest` cases added, full backend suite still 131/131. The plain-language-override and Unicode-look-alike classes are **explicitly NOT fixed further** — no string-sanitization pass can fully close them (an LLM does not enforce a hard data/instruction boundary the way a parser does), and a real structural fix (isolating untrusted instructor content onto a separate, non-privileged channel) is an architecture change, out of a QA/polish phase's scope. **Recorded as an ACCEPTED, DISCLOSED residual risk** (documented in `AiPromptBuilder.kt`'s `sanitize()` kdoc), under the explicit threat-model assumption that instructors are trusted, vetted platform accounts — the same trust tier already granted course-authoring/publishing privileges elsewhere in this app — not anonymous public actors; this mitigation is a courtesy safeguard against accidental tag-like text, not a defense against a deliberately adversarial instructor account, which this app's broader trust model does not otherwise defend against either. |
| A6 | Every open item in `PHASE_HANDOFF.md` §§6 (Phases 1/2/3/4/6/7) re-read and assigned: STILL OPEN-ACCEPTED / FIXED IN PHASE 8 / NO LONGER APPLICABLE. | DONE (2026-09-22) — all ~38 items across Phases 1/2/3/4/6/7 §6 classified; full item-by-item list recorded in this session's report (not reproduced here for readability). Summary: **FIXED IN PHASE 8 (3):** Phase 6 §6.2 (F6) and §6.3 (F8) — see A5; Phase 7 §6.4 (T10 quiz-recompletion observation) — see A4. **NO LONGER APPLICABLE (3):** Phase 1 §6.7 (synthetic seed video) — already superseded by Phase 2's own struck-through §6.2 resolution (PRE-PHASE-3 real-video seed expansion); Phase 3 §6.2 (androidMain Keystore path untested against a real provider) — explicitly closed by Phase 4 T4 Part D's `AndroidTokenStorageInstrumentedTest.kt` (confirmed present, real `connectedDebugAndroidTest` against the real `AndroidKeyStore`); Phase 4 §6.1 (Quiz/Quiz Results not re-verified live in that session) — superseded by Phase 7 T8's cross-client harness + T10/T11's live Website/Android walks. **STILL OPEN-ACCEPTED (~32):** every remaining item — each is a deliberate, already-disclosed, low-risk simplification; a real environment constraint (no Docker on this machine, no Mac for iOS); a locked-design conflict correctly left to its own D5/H5-governed row rather than silently patched here; or a new-feature-shaped gap (password-change endpoint, AI Tutor docked panel, offline cache, real certificate/icon/font assets) out of a QA/polish phase's fix-bugs-not-features scope. One item (Phase 6 §6.5, Android's "Login to Enroll" CTA not immediately refreshing post-login) is flagged as a plausible cheap-fix *candidate* for a future targeted task but was not fixed here — it falls outside this session's specific A3-A6 mandate and wasn't independently investigated. Items duplicating A1 (Phase 7 §6.3, AppDialog) and A2 (Phase 7 §6.2, Quiz Results spacing) were left as-is here since both are owned by parallel in-flight Phase 8 tasks this session (`AppDialog*`/`quiz-results-screen.tsx` were off-limits) — not independently reclassified to avoid conflicting with that work. |
| A7 | No known issue closed as "fixed" without a real, reproducible before/after verification on the affected client. | NOT STARTED |

## B. Localization & RTL Completion (M14)

| # | Criterion | Status |
|---|---|---|
| B1 | All 29 `SCREEN_INVENTORY.md` screens audited for EN/AR string coverage on each in-scope surface; no hardcoded English string reachable in `ar` locale. | PASS (B3) — grep-based sweep 2026-09-22/23 of every portfolio-priority + Admin/Instructor screen component under `web/src/components/screens/` (all 24 files), the public pre-auth pages (`app/[locale]/(public)/**`), `components/navigation/**`, and `components/ui/**`: zero plain-text JSX children (`>[A-Z]...<`) and zero hardcoded `placeholder=`/`aria-label=`/`alt=`/`title=` string literals outside `t()`/`useTranslations`. Consistent with B2's independent finding of 0 missing keys (491/491 en/ar). Necessarily a sampling exercise per the task's own framing (grep-based, not a full manual click-through of all 29 screens in both locales) but covers every screen component file in the repo, not merely the portfolio flow — no fix needed, nothing found to fix. |
| B2 | Mechanical translation-key parity check exists and passes for Web (`messages/en.json` ↔ `ar.json`) and Android (`values/strings.xml` ↔ `values-ar/strings.xml`) — closes D-7 (locked M14 deliverable). | PASS (B3) — built `web/scripts/check-translation-parity.js` (flattens both nested message trees to dotted key paths, diffs the sets, exits non-zero listing every missing/extra key); run 2026-09-23: "Translation-key parity OK: 491 keys match between en.json and ar.json." Android: `mobile/androidApp/src/test/kotlin/.../StringsParityTest.kt` (pre-existing, T18) confirmed already sufficient — parses the real `values`/`values-ar` XML directly (not a generator self-report), compares the FULL key sets both directions (`everyEnglishKey_hasAnArabicTranslation` / `arabicHasNoOrphanKeys_beyondWhatEnglishDefines`, `app_name` the one sanctioned OS-locale exception), plus blank-value and format-specifier-parity guards beyond mere key parity. No Android tooling changes needed. |
| B3 | Parity check wired into CI (extends `web-ci.yml`/`android-ci.yml`, not a parallel workflow), proven non-vacuous by a deliberate temporary drift. | PASS — added a `check:translation-parity` npm script and a new step in `web-ci.yml`'s existing `web-static` job (alongside lint/logical-properties/design-to-code/typecheck), not a new workflow. Android's `StringsParityTest` already runs via `android-ci.yml`'s existing `:androidApp:testDebugUnitTest` step in the unit-test job — no new step needed. **Proven non-vacuous both ways, then reverted exactly:** (1) Web — deleted `common.appName` from `ar.json`, ran the script: failed, exit 1, "Missing in ar.json (1): - common.appName"; `git status`/`git diff` on the restored file empty. (2) Android — deleted the `nav_home` `<string>` entry from `values-ar/strings.xml`, ran `./gradlew :androidApp:testDebugUnitTest --tests StringsParityTest`: `everyEnglishKey_hasAnArabicTranslation` FAILED with an `AssertionError` listing the missing key; restored, re-ran: BUILD SUCCESSFUL, `git diff` on the restored file empty. |
| B4 | RTL QA pass: every in-scope screen inspected under `ar`/RTL for mirroring correctness per `LOCALIZATION.md`/`DESIGN_RULES.md`. | PASS, broad live coverage (2026-09-23) — full Playwright-driven walk (screenshots + DOM inspection, both `ar`/RTL Dark as the primary combination and an `ar`/RTL Light spot-check) against the running local stack, covering: Landing/Home, Login, Register, Dashboard, Explore, Course Details, Demo Checkout, Purchase Success, Course Player, Quiz, Quiz Results, My Learning, Certificates (list + detail), Learning Paths, AI Tutor, Profile, Settings, Instructor Dashboard, Instructor Course Editor (Overview), Admin Dashboard, Admin Manage Courses, Admin Manage Users — 22 of the 29 `SCREEN_INVENTORY.md` screens walked live under `ar`/RTL this batch (the previous B3 spot-check's 4 screens are a subset, re-confirmed). Every screen showed correct logical-property mirroring: sidebar docked to layout-start/visual-right, nav/breadcrumb/back-icon mirroring, `DataTable` column order reversed to reading order with row-actions (`⋮`) at inline-end, dialog/form label+input alignment, Select chevron non-mirrored, price/date/count formatting locale-correct (see B6), video scrubber and wordmark correctly un-mirrored (re-confirming B5), checkout order-summary rows and quiz answer/badge rows mirrored correctly (re-confirming A2 holds under RTL). **Not walked live under `ar` this batch** (capped Instructor/Admin fidelity is an already-disclosed D5 drift item, not this row's target): Learning Path Details, Certificate Detail's sibling Course Editor — Curriculum/Lesson Editor/Quiz Editor, Admin Manage Instructors, Admin Manage Categories — left as a residual gap, low-risk given every *other* screen sharing the same `DataTable`/form/tab primitives already checked clean. One real, low-severity defect found and **documented, not fixed** (see D3): embedded English sentences ending in trailing neutral punctuation (`?`, `.`) rendered without `dir="auto"` inside the RTL document have that trailing character visually displaced to the paragraph-start side by the Unicode Bidi Algorithm (e.g. quiz question prompts, AI Tutor's stub response) — the English words themselves still render in correct left-to-right order (B7's core requirement holds), only the boundary punctuation placement is affected; full details under the D3 row and in this session's report. **Android side (B5, 2026-09-23):** live walk on a real debug APK / `Chatting_Pixel_8_API_36` emulator, `ar` locale, both Dark (primary) and Light, across Explore, Course Details, Demo Checkout, Purchase Success, Course Player (video + lesson-content bottom sheet), Quiz, Quiz Results, Certificates (list + detail), My Learning, AI Tutor, Profile, Settings, Learning Paths, Login, Register. All mirrored correctly: back chevron and "Previous lesson" icon both correctly point right (RTL "backward" convention, matches Course Player's own top-bar back button), bottom nav tab order reads Home→Explore→My Learning→AI Tutor→Profile right-to-left, quiz/lesson progress bars fill from the right, AI Tutor chat bubbles mirror via Compose's automatic `Alignment.Start`/`End` (assistant at layout-start/right, user at layout-end/left), certificate label:value rows (e.g. `:المدرّب`) correctly reverse colon-after-label order, bottom-sheet close (`×`) sits at layout-end/left while the sheet title stays at layout-start/right, video scrubber stays LTR-unmirrored (re-confirming B5's own scrubber row on Android). Same embedded-English-punctuation Bidi artifact the Web audit found also reproduces identically on Android (e.g. Quiz question 3, "Which status code means Not Found?", rendered with the `؟`-position trailing punctuation visually displaced to the run's start) — confirms this is a genuine Unicode Bidi Algorithm behavior common to both clients' text-rendering stacks, not a client-specific defect; same disposition as Web's D3 finding (documented, not fixed). No other Android-side RTL defects found. |
| B5 | Video scrubber and Mentora wordmark confirmed still LTR/unmirrored under `ar` (named explicitly in M14). | PASS (B3) — **Scrubber:** confirmed statically in `web/src/components/ui/video-player.tsx` — the timeline (`.mtx-video-timeline`) and volume slider both carry an explicit `dir="ltr"`, unconditional on locale, matching `LOCALIZATION.md § 2/§ 3`'s named exception. **Wordmark:** confirmed live — `/ar/explore` and `/ar/login` (both AR/RTL) screenshotted 2026-09-23, "Mentora" renders as a single unmirrored LTR run at the navbar/auth-header layout-start (visual right in RTL), exactly per `LOCALIZATION.md § 2/§ 8`'s locked decision; source (`auth-header.tsx`/`public-navbar.tsx`) renders it as plain text via `tCommon("appName")` with no manual `dir`/mirroring logic — correct by relying on the Unicode Bidi Algorithm's automatic isolation of an embedded Latin run, not by fighting it. Neither needed a code change — both already correct. |
| B6 | Locale-aware formatting (dates, prices, durations, numerals) verified on real screens in both locales. | PASS (2026-09-23) — confirmed the actual formatting mechanism is `web/src/lib/i18n/format.ts`'s three helpers (`formatPrice`/`formatCount`/`formatDate`), all built on real `Intl.NumberFormat`/`Intl.DateTimeFormat` with the locked `-u-nu-latn` Unicode extension forcing Western-Arabic digit glyphs under `ar` per `LOCALIZATION.md §§ 7-8` — not hand-built strings. Verified live on real screens, both locales: **Price** — Course Details/Demo Checkout/Course card show `EGP 850` (en) vs `850 ج.م.` (ar), Western digits both times, currency label/position following locale convention. **Date** — Certificate Detail shows `Sep 23, 2026` (en) vs `2026/09/23` (ar), Western digits both times (`Intl` `dateStyle:"medium"` per-locale pattern, not a hardcoded format string). **Count/numerals** — quiz "Question 1 of 3" → "السؤال 1 من 3", dashboard stat cards ("3 Courses completed" → "3 دورات مكتملة"), avg-progress percent ("25% complete" → "اكتمل 25%") — Western digits in every instance, confirming § 8's locked numeral decision holds everywhere exercised. **Duration** — no total-course-duration field exists anywhere in the product (already-disclosed adaptation, `course-card.tsx`'s own code comment: no duration/student-count fields on the backend), so there is nothing duration-shaped to format beyond the video player's per-second elapsed/remaining time, which is correctly exempted from locale formatting entirely (LTR-always scrubber, B5). One incidental, non-defect observation: `percentComplete`'s Arabic string (`messages/ar.json`) uses the Arabic percent glyph `٪` (e.g. "اكتمل 25٪") rather than `%` — a deliberate translator/content choice inside the message string itself, not a formatting-API artifact, and outside the locked numeral decision's scope (that decision governs digit glyphs only) — common convention in Arabic UI copy, not a defect. |
| B7 | Embedded English content inside an Arabic page still renders LTR (re-confirmed across the wider screen set). | PASS, with one disclosed nuance found and documented (not fixed) — 2026-09-23. Re-confirmed live across the wider screen set this batch: English-authored course titles/descriptions/curriculum-lesson-titles (`Building Reliable REST APIs` and its full 12-lesson curriculum), instructor names, quiz question/option text, AI Tutor's stub response text, and certificate course-name snapshots all render as correctly left-to-right, unreversed English runs inside `ar`/RTL pages, anchored at the container's logical-start (visual right) per `LOCALIZATION.md § 2` — the core requirement holds on every screen walked. **One related, lower-severity rendering nuance found and documented, not fixed** (small but not obviously well-contained — see reasoning below): an English sentence that (a) is rendered as plain text with no `dir="auto"`/`dir="ltr"` override and (b) ends in a trailing neutral punctuation mark (`?`, `.`) gets that trailing character visually displaced by the Unicode Bidi Algorithm to the paragraph-*start* side (visual right in RTL) instead of staying attached to the end of the English words — e.g. quiz question `"Which HTTP method is conventionally idempotent?"` renders as `"؟Which HTTP method is conventionally idempotent"` (repro: `web/src/components/ui/question-card.tsx:53`, `<h2>{prompt}</h2>` with no `dir`), reproduced identically on all 3 quiz questions and on the AI Tutor's own stub-response sentence (`ai-tutor-screen.tsx` message bubble). Root cause: with the ancestor's `dir` forced to `rtl` (from `<html lang="ar" dir="rtl">`, not auto-detected), UAX#9's neutral-character resolution rule places a trailing neutral adjacent to the paragraph's own (RTL) edge rather than the embedded LTR run's actual end, once nothing following it establishes the character's context. The English word order itself is unaffected — this is a punctuation-boundary artifact, not a mirroring failure, so it does not block this row's PASS — but it is real and reproducible. **Why not fixed here:** the correct remediation (`dir="auto"` on genuinely foreign/unknown-language content nodes) needs a deliberate inventory across every component that renders instructor/user-authored or system text whose language may not match the active locale (course/lesson titles and descriptions, quiz prompts/options, AI Tutor message bubbles, at minimum) — touching several files under time pressure risked an incomplete, inconsistent sweep; recorded here for a dedicated follow-up task rather than a piecemeal fix per this project's own "document, don't half-fix an ambiguous-scope defect" discipline. |
| B8 | `08-language-switch` Playwright flow extended to assert a sampling of screens beyond Settings. | PASS (B3) — added a new test to `web/e2e/08-language-switch.spec.ts` ("the persisted Arabic locale also renders Explore and Course Details correctly") asserting `explore.title`/`courseDetails.curriculum` AR strings and `dir="rtl"` on both screens after a locale switch originating in Settings, using the one seeded Arabic-authored course (`أساسيات تصميم تجربة المستخدم`) — Explore's `language` content filter correctly excludes English-only-authored courses from the `ar` listing, which is real product behavior (not a defect), so the fixture had to be a course with real Arabic content, not the suite's usual English-authored `QUIZ_COURSE_TITLE`. Full suite re-run `CI=true npx playwright test --project=chromium`: 19/19 on a clean run (one isolated re-run of `10-admin-course-management.spec.ts` confirmed pass); two immediately-prior back-to-back full-suite runs showed transient 429-rate-limit-cascade flakes/failures across unrelated specs (`01`, `03`, `06`, `07`, `10`) from hammering the real 10-req/min auth bucket (`e2e/helpers.ts`'s own documented rate-limit note) by re-running the whole suite repeatedly within its cooldown window — not a regression from this batch's changes; the new/modified spec itself passed in every run once past its own initial fixture-selection bug (fixed before commit). |
| B9 | Arabic typography placeholder on Android (`FontFamily.SansSerif`): verdict recorded — real asset added or ACCEPTED LIMITATION. | **ACCEPTED LIMITATION, reaffirmed with a real Android toolchain (B5, 2026-09-23).** Re-confirmed on the actual emulator (not deferred this time): `mobile/androidApp/.../theme/MentoraTheme.kt:236` (`MentoraArabicFontFamily = FontFamily.SansSerif`) unchanged; repo-wide search confirms zero `.ttf`/`.otf` font assets anywhere (`find … -iname "*.ttf" -o -iname "*.otf"` — only hit is a Playwright trace-viewer icon font under `web/playwright-report/`, unrelated) and no `res/font` directory under `androidApp`. Per this task's own instruction ("re-confirm the current state, don't add a font asset unless it's trivially available already in the repo"), no asset is trivially available, so none was added. Live-verified the placeholder is not visually broken: every Arabic screen walked this batch (Explore, Course Details, Checkout, Course Player, Quiz, Quiz Results, Certificates, My Learning, AI Tutor, Profile, Settings, Login, Register) rendered fully legible, correctly-shaped Arabic glyphs via the system `sans-serif` fallback — no missing-glyph tofu boxes, no obviously wrong letterforms. The structural, code-only adjustments `LOCALIZATION.md § 4` requires alongside a real font (zeroed `letterSpacing`, ×1.1 body line-height for Arabic runs) remain correctly implemented unconditionally in `mentoraTypography()`, independent of which font backs it. Verdict: **ACCEPTED LIMITATION** — sourcing/licensing/wiring a real Noto Sans Arabic asset is a new-asset-acquisition task, correctly out of a QA/polish batch's scope; not fixed here. |

## C. Testing Hardening (M15)

| # | Criterion | Status |
|---|---|---|
| C1 | Backend/`:shared`/`:androidApp` JVM/lint/Playwright suites pass at or above the Phase 7 T13 baseline (127/249/246/0 lint errors/19 chromium) — zero regressions. | **PASS (2026-09-23, updated through the C4/QuizService concurrency fix).** Backend 136/136 (127 baseline + 9 new regression tests added across D-10/A4/A5/A5-followup/D-11/C4/QuizService-fix — exact per-commit breakdown not separately tracked here, since it churned across several follow-up fixes; the confirmed running total at each checkpoint is recorded in that fix's own row), `:shared:testDebugUnitTest` 249/249, `:androidApp:testDebugUnitTest` 246/246, `:androidApp:lintDebug` 0 errors (105 warnings, baseline-matching), Playwright chromium 19 passed + 1 flaky-self-healed-on-retry = 20/20 functional (baseline 19 + 1 new B8 test). Zero regressions anywhere; several real pre-existing defects found and fixed along the way (D-10, D-11, A1, A2, A4, A5, C4) — see their own rows. |
| C2 | Android instrumented suite (106 tests, local emulator) run and accounted for test-by-test; the 2 AppDialog failures resolved per A1; any transient failure characterized, not hand-waved. | **PASS (B5, 2026-09-23).** Fresh debug APK built and installed (`./gradlew.bat :androidApp:installDebug`, `BUILD SUCCESSFUL`) on `Chatting_Pixel_8_API_36`, then `./gradlew.bat :androidApp:connectedDebugAndroidTest` run 3 times in immediate succession against the live local backend: **run 1** 104/106 passed, 2 failed; **run 2** 105/106 passed, 1 failed; **run 3** 106/106 passed, `BUILD SUCCESSFUL`. All failures across all 3 runs were the exact same 2 tests — `NavigationShellTest.home_certificatesEntryPoint_navigatesToCertificates_afterARealPurchase` and `MediaPlaybackControllerInstrumentedTest.realExoPlayer_reachesPlayingState_onARealSeededLessonVideo` — both failing identically with `IllegalStateException: real register failed in test setup: Failure(code=RateLimitedAuth, message=Too many requests. Please slow down., fields=null, httpStatus=429)`, i.e. the exact same transient class already documented at A1/D162/Phase 7 T13 (backend's 10-req/min `auth` rate limiter exhausted by this session's own heavy manual QA walk — multiple real logins/registrations plus repeated `connectedDebugAndroidTest` runs in a short window). The strictly monotonic 2→1→0 failure count across the 3 runs (same 2 tests each time, nothing new) is itself direct evidence of transience, not a real regression — confirmed by the clean 106/106 third run. AppDialog's own 2 tests (`AppDialogColorAndLayoutTest`) were green in all 3 runs, confirming A1's fix holds. No test failed that wasn't already in this known class; no genuinely new/different failure observed. |
| C3 | Bounded Web Vitest+RTL tier (H2) exists and runs in CI. | **PASS (2026-09-23, Phase 8 B6).** Built a bounded (not exhaustive) Vitest + React Testing Library tier: `web/vitest.config.ts` (jsdom, `@vitejs/plugin-react`, `@` alias matching `tsconfig.json`), `web/vitest-setup.ts` (`@testing-library/jest-dom`), new `test:unit` npm script (`vitest run`). Added devDependencies compatible with React 19/Next 15 (`vitest@5`, `@testing-library/react@16`, `@testing-library/jest-dom@7`, `@testing-library/user-event@14`, `jsdom@30`, `@vitejs/plugin-react@6`). 23 tests across 4 files, all passing (`npm run test:unit`): `src/lib/i18n/format.test.ts` (5 — price/count/date formatting incl. the locked Western-numeral-under-`ar` requirement, consumed by both Quiz Results' score display and Checkout's price display), `src/lib/auth/schemas.test.ts` (8 — `loginSchema`/`registerSchema`, the actual zod validation logic `LoginForm`/`RegisterForm` wire straight into `react-hook-form` with no logic of their own), `src/lib/api/quiz.test.tsx` (4 — `useQuiz`/`useLatestAttempt`'s not-found-vs-real-error mapping, the real frontend "quiz data" logic; actual scoring/isCorrect computation is server-side per `TESTING_STRATEGY.md § 1`, so there is no separate client scoring logic to unit-test), `src/lib/api/enrollment.test.tsx` (6 — `useEnrollments` role-gating and `useIsEnrolled` derivation, the demo-checkout/enrollment flow logic `CheckoutScreen` and Course Details consume). Deliberately did not touch `quiz-results-screen.tsx` (owned by another in-flight Phase 8 task) or any other excluded file — tested the underlying data/logic layers those screens consume instead, per this batch's own instruction. Wired into CI: added a "Unit/component tests" step to `.github/workflows/web-ci.yml`'s existing `web-static` job (not a parallel workflow), between design-to-code validation and type-check. `npm run typecheck` and `npm run lint` both re-confirmed clean after adding the tier (1 pre-existing unrelated lint warning, `course-thumbnail.tsx`'s `<img>` advisory). |
| C4 | High-risk-area coverage (demo-checkout idempotency, completion→certificate transaction, quiz `isCorrect` stripping) re-confirmed genuinely covered. | **PASS, with 2 real gaps found and fixed (2026-09-23, Phase 8 B6).** All three named areas re-read against their actual backend tests, confirming assertions are real (not just status-code checks): quiz `isCorrect` stripping (`QuizIntegrationTest.kt` asserts the raw response body text never contains `"isCorrect"` for the student fetch while the instructor/editor fetch does — a genuine content check, adequate, no gap). Demo-checkout idempotency and the completion→certificate transaction (`EnrollmentIntegrationTest.kt`/`CertificatesIntegrationTest.kt`, including A4's own regression test) were adequately covered for the *sequential* repeat-request case, but neither had ever exercised a *genuinely concurrent* duplicate request — both only reach `EnrollmentService.complete`/`CertificateService.checkAndIssueIfComplete`'s `MongoWriteException`+`DUPLICATE_KEY` recovery branch when one request's transaction has already committed before the next one's `repository.find` read. Added one true-concurrency test to each (10 parallel coroutines hitting `checkout/complete` / lesson-complete on the same student+course) to close this — and both **immediately, reproducibly failed** on first run: 8/10 concurrent checkout requests returned `500` (`com.mongodb.MongoCommandException`, error 112 `WriteConflict`, `TransientTransactionError` label) instead of the expected idempotent `200`/`201`, confirmed via the captured backend stack traces. Root cause: MongoDB's transaction concurrency control can abort a losing transaction with a `WriteConflict`-labelled `MongoCommandException` (not a `MongoWriteException`), which neither service's `catch` block ever handled, so a real concurrent double-submit (e.g. a double-tap on "Complete Demo Purchase") could 500 even though the unique index correctly prevented any actual duplicate document. **Fixed** both `EnrollmentService.complete` and `CertificateService.checkAndIssueIfComplete` to retry the whole transaction attempt (up to 10 times) on a `MongoException` carrying the `TransientTransactionError` label — the pattern MongoDB's own docs prescribe for this label — before re-checking the existing `DUPLICATE_KEY` recovery path, which then resolves cleanly once the winning request has committed. Both new tests pass reliably (3 consecutive reruns each, no flakes) after the fix; full backend suite 135/135 (133 baseline + 2). This was found *by* writing the coverage this row asked to verify, not a pre-existing known issue — a real, disclosed, small-scoped fix, not scope creep. **Independent review follow-up (2026-09-23):** an Opus review of this fix found it correct and safe (bounded, no double-write, composes cleanly with A4, verified via a real revert-and-rerun non-vacuity check) but surfaced the identical defect class still live and reproducible in a THIRD service the original fix missed — `QuizService.submit` (a double-tap on "Submit Quiz" hit the same unhandled `WriteConflict` on the shared `progress` document, 9/10 failures at 10-way concurrency) — plus a quantified capacity ceiling in the original fix (no backoff/jitter meant losers retried in lockstep; certificate-path success dropped to 31/100 at 100-way concurrency, though data integrity held throughout, verified: exactly 1 certificate, correct `courseCompletedAt`, at every concurrency level tested). **Both addressed:** extracted the retry logic into one shared `withRetryableTransaction` helper (`backend/common/TransactionRetry.kt`) — implementing MongoDB's full prescribed pattern (retry the transaction on `TransientTransactionError`, retry just the commit on `UnknownTransactionCommitResult`, exponential backoff with full jitter so concurrent losers spread out instead of re-forming the same contention) — and applied it to all three services (`CertificateService`, `EnrollmentService`, `QuizService`), removing three near-duplicate hand-rolled `inTransaction` helpers. Added a regression test for the `QuizService` gap (confirmed non-vacuous via revert-and-rerun); re-verified the 100-way ceiling improved substantially (31%→73% success) while the realistic double-tap scale (10-way) is now 100% reliable on all three services, with zero data corruption at any concurrency level tested. Full backend suite 136/136 (135 + 1). |
| C5 | CI green on a clean run, twice in a row, across all in-scope workflows — run IDs recorded. | NOT STARTED |
| C6 | Deliberate-regression sanity check: introduced on a scratch branch, confirmed caught by the suite, reverted. | NOT STARTED |
| C7 | Every flaky test observed is stabilized or documented with root cause and reason for acceptance. | NOT STARTED |
| C8 | D-5 (dead Testcontainers dependency vs. locked `TESTING_STRATEGY.md` claim) reconciled. | **PASS (2026-09-23, Phase 8 B6) — reconfirmed, not re-done.** D-5 already fixed in an earlier batch (dead `org.testcontainers:*` deps removed from `backend/build.gradle.kts`, `TESTING_STRATEGY.md § 1` corrected). Reconfirmed as part of this batch: zero `testcontainers` references anywhere in `backend/build.gradle.kts` or `backend/src` (repo-wide case-insensitive search), and `./gradlew.bat test` passes cleanly (135/135, see C4/C1) — no lingering reference, no regression. |
| C9 | WebKit E2E remains ACCEPTED LIMITATION (D64) — not reopened, not silently dropped. | NOT STARTED |

## D. Design System v1.3.2 Conformance Audit (LOCKED — audit only)

| # | Criterion | Status |
|---|---|---|
| D1 | `tokens-ci.yml` green: zero drift between `design-tokens.json` and generated outputs. | PROVISIONAL PASS (B2) — local equivalent `npm run validate:design-to-code` (`web/`) run 2026-09-22: "Design-to-Code validation: 37 screens, 6 patterns, 11 shared files checked. Validation PASSED." Actual `tokens-ci.yml` workflow not re-run in this static-only batch; full CI confirmation deferred to C5/B11. |
| D2 | Android `MentoraTokens` drift test green. | PROVISIONAL PASS (B2) — `./gradlew :androidApp:testDebugUnitTest --tests "com.mentora.android.theme.MentoraTokensDriftTest"` run 2026-09-22: BUILD SUCCESSFUL, all drift assertions green (colors, typography, shape, elevation, spacing, icon size, touch target). CI confirmation deferred to C5/B11. |
| D3 | Spot-audit of portfolio-flow screens for ad-hoc/hardcoded styling bypassing a token or specified component. | PASS, audit complete — 2 real, low-severity findings documented (not fixed; both ambiguous/wider-blast-radius than a safe one-line change). 2026-09-23: grepped every `style={{...}}` inline style and every hardcoded hex color across `web/src/components/screens`, `web/src/components/ui`, and `web/src/app` (37 matches total). The overwhelming majority already resolve through `var(--color-*)`/`var(--radius-*)` design tokens (text-secondary colors, skeleton radii, error colors) — conformant. Two real, disclosed exceptions: **(1)** `web/src/components/ui/instructor-card.tsx:15` hand-rolls its own avatar `<div>` with hardcoded inline pixel styles (`width: 64, height: 64, fontSize: 24`) and a locally-duplicated `initials()` helper, instead of using the existing, `COMPONENTS.md`-compliant `Avatar` component (`web/src/components/ui/avatar.tsx`) that `COMPONENTS.md § InstructorCard`'s own locked spec names explicitly ("`Avatar` (size `large`, 64)"). Not a simple swap, though: `Avatar`'s own `.mtx-avatar-large` CSS class (`components.css:328`) sets the fallback-initials font-size to the raw `--typography-label-large-font-size` token (14px) for every avatar size uniformly, never actually scaling it up for larger avatars the way `COMPONENTS.md § Avatar`'s own text says it should ("`typography.label.large` (scaled to avatar size)") — so swapping `InstructorCard` to use `Avatar` as-is would make its 64px-circle initials visibly smaller/less legible than today's (arguably-nicer-looking, if non-token) 24px, and truly fixing it means also correcting the shared `Avatar` CSS's font-scaling, which affects every `Avatar` consumer app-wide, not just this one card. Separately found while investigating: `components.css` defines **two different, non-identical `.mtx-avatar` base rules** (one at line ~320 for the real `Avatar` component + its nested `.mtx-avatar-fallback`; a second, differently-scoped one at line ~1046 with its own background/color/centering) — the second is what's accidentally making `InstructorCard`'s hand-rolled div look visually correct today despite not using the sanctioned component. All three (the InstructorCard bypass, the unscaled fallback font-size, the duplicate `.mtx-avatar` selector) are interconnected and a safe fix requires touching shared, widely-used CSS — correctly deferred rather than patched piecemeal under this session's time-box. **(2)** Admin "Manage Courses" search field (`admin-courses-screen.tsx`, `SearchField`/`.mtx-search-field`) hard-clips its placeholder text with no ellipsis at typical widths in both light and dark themes — "Search by title or descript" instead of the full "Search by title or description…" — reproduced live in both EN-Light and EN-Dark screenshots. Cause: `SearchField` renders a native `<input placeholder="...">` with no floating-label wrapper (unlike `TextField`/`PasswordField`, whose `.mtx-field-label` correctly has `text-overflow: ellipsis` per `components.css:138`), and native placeholder-text overflow has no reliable cross-browser ellipsis behavior. Low materiality (only visible on an empty, unfocused admin-only field) — documented rather than fixed given the two candidate fixes (widen the field token, or shorten the `admin.courses.searchPlaceholder` copy) both needed a considered call outside this audit's time-box, not because either is inherently risky. Neither finding blocks this row's PASS (the row's own scope is "audited," not "zero found") — both are cited as D5 disposition candidates. **Android side (B5, 2026-09-23):** grepped `mobile/androidApp/src/main/kotlin/com/mentora/android` for hardcoded `Color(0x...)`/`Color(0...)` literals outside the theme layer. Only 2 files match: `theme/MentoraTokens.kt` (expected — the token definitions themselves) and `ui/components/CourseArtwork.kt` (the decorative gradient generator behind every course-thumbnail placeholder, e.g. the purple gradients seen throughout Explore/My Learning/Course Details). The latter is a deliberate generative-placeholder-art system standing in for real per-course thumbnail images (an already-disclosed limitation, not a UI-chrome component `COMPONENTS.md` specifies token usage for) — not a defect, same category as Web's own generated course-card gradient placeholders. No hardcoded colors, spacing, or typography found bypassing `MentoraTokens` in any interactive/chrome component across the screens walked (buttons, fields, cards, chips, nav, dialogs, bottom sheets) — consistent with `D2`'s own passing `MentoraTokensDriftTest`. No new Android-side D3 defects found. **Both findings above FIXED in this batch (B9/D5 disposition pass, 2026-09-23) — audit-complete framing unchanged, the two candidates are now closed rather than deferred:** **(1) Avatar/InstructorCard:** on direct re-inspection, the original characterization ("flat 14px for every avatar size uniformly") was imprecise — `.mtx-avatar-small/medium/large/xlarge` each already used a distinct typography token (12/12/14/20px) — but the real defect stands: `large`'s jump from `medium` (12px→14px for a 60%-bigger 40px→64px box) was too weak, making 64px-circle initials look disproportionately small next to `xlarge`'s properly-scaled 20px. Fixed `.mtx-avatar-large` (`components.css:328`) to use `--typography-body-large-font-size` (18px, a real existing token, not invented) — restores monotonic scaling (12/12/18/20) close to `medium`'s own established ~0.30 box:font ratio, without colliding with `xlarge`. Removed the dead, duplicate second `.mtx-avatar` rule (`components.css:1046-1057`) — confirmed via specificity/cascade analysis and live screenshots that its properties (background/color/font/display) were fully shadowed by `.mtx-avatar-fallback`'s own explicit styling for the real `Avatar` component, and it existed only to accidentally make `InstructorCard`'s non-conformant hand-rolled div look correct. Swapped `InstructorCard` (`instructor-card.tsx`) to render `<Avatar name={name} size="large" />` instead of its hand-rolled div + local `initials()` helper. **Visually verified live** (Playwright, real browser, real seeded data) before/after across every existing `Avatar` consumer, confirming zero regression: Profile `xlarge` (96px, "YA" initials, unchanged — untouched size class), Admin Manage Instructors/Users `small` (24px, "OK"/"MA"/etc., unchanged — untouched size class), and the new Course Details `large` (64px, "OK"/"MA" initials, now legible and proportionate) — all re-checked in EN-Light, AR-RTL, and a dark-preference pass; RTL correctly mirrors the avatar to the row's visual end exactly like the rest of the app's flex rows, no new RTL branching needed. **(2) Admin search placeholder:** root cause was actually the field's own intrinsic flex sizing (an unconstrained flex item defaults to browser-default `<input>` width, never approaching its own `max-width` cap) rather than the cap being too small — raising the multiplier alone would have been a no-op, confirmed by measuring `clientWidth` before and after in a live page. Fixed by giving `.mtx-admin-filter-bar .mtx-search-field` (page-scoped selector, not the shared `SearchField`/`.mtx-search-field` component rule) `flex: 1 1 auto; min-width: 0;` alongside a modest `max-width` bump (`space-16 * 5.5` → `* 6`, 352px → 384px) so it actually grows to fill the available row instead of sitting at native input width. Verified live via canvas text-measurement (`ctx.measureText` against the input's real computed font) confirming the full placeholder ("Search by title or description…", 214px) now fits with ~30% headroom inside the 302px available text area (previously only 181px was available, clipping around 214px of needed width) — and via direct screenshots, EN and AR/RTL, at both 1280px and a narrower 900px desktop width, confirming no overlap with the adjacent Status `Select` and no regression to Manage Users'/Manage Instructors' shorter placeholders (same shared filter-bar rule, now just has a bit more headroom). No copy change was needed. |
| D4 | Web ↔ Android visual-parity audit on the portfolio-priority flow; divergence corrected or disclosed. | PARTIAL, now with both sides' own-client walk complete (Android added B5, 2026-09-23). **Web side** (from an earlier batch): fully walked and screenshotted for the full nine-step portfolio-priority flow, EN-Light and AR-Dark, confirming internal Web-side DS conformance throughout — see B4/B6/B7. **Android side (this batch):** walked the same flow live on a real debug APK/emulator (Explore→Course Details→Demo Checkout→Purchase Success→Course Player→Quiz→Quiz Results→Certificate→My Learning), AR-Dark primary + EN-Dark/AR-Light spot-checks. Visual language matches Web closely at the component-intent level: same purple/indigo accent family, same card/pill/button shapes, same sticky-bottom-CTA checkout pattern, same "Demo Payment/Purchase" disclosure chip copy pattern, same score/badge/checkmark treatment on Quiz Results, same certificate field-label layout. **One notable, disclosed divergence found:** Course Details' "already enrolled → Continue Learning" CTA renders as a light-lavender **tonal** button (secondary-looking, matching the visual weight of Login's *disabled* state elsewhere in the app) rather than a solid primary-purple button — needs a side-by-side design-spec diff against Web's equivalent state to confirm whether this is an intentional secondary/tertiary treatment (plausible: distinguishing "you already own this" from "buy now") or an unintended token-swap; not fixed here since it reads as plausibly intentional and a wrong guess would be worse than leaving it disclosed. No thumbnail/image parity is meaningful to compare — both clients use generated placeholder art, not real course images (already-disclosed, shared limitation). Full pixel-level side-by-side diffing was not performed (no shared screenshot-diff tooling exists per H4); this remains a qualitative, screenshot-based comparison — recorded PARTIAL rather than PASS for that reason, not because a blocking divergence was found. |
| D5 | Each documented DS-drift item (icon placeholders, `DataTable` breakpoint conflict D62, no real certificate asset, Arabic font placeholder, capped Instructor/Admin fidelity) given explicit verdict. | **DONE (2026-09-23).** Every documented drift item given an explicit, individually-reasoned verdict — none re-litigated from scratch, each traced to its originating decision: <br>• **Web icon placeholder set** (hand-drawn inline-SVG `Icon` component, `components/ui/icon.tsx`, D40) instead of the real self-hosted Material Symbols Rounded variable font `DESIGN_SYSTEM.md` specifies — **ACCEPTED LIMITATION**: sourcing/subsetting/self-hosting a real icon font is a new-asset-acquisition task, not a bug; D40 already discloses this as a stable-API placeholder ("drop-in replacement behind `Icon`'s existing API — no call sites change"), correctly out of a QA/polish phase's scope. <br>• **Android icon placeholder set** (hand-drawn `ImageVector` 1:1 port of the same Web placeholder set, D40/G8) — **ACCEPTED LIMITATION**, same reasoning: no Material Symbols asset exists in the repo, porting was a deliberate stable-API placeholder decision (G8), not newly discoverable scope for this phase. <br>• **`DataTable` breakpoint spec conflict** (`COMPONENTS.md`: collapses below `desktop` vs. `design-tokens.json`: collapses below `tablet`, D62) — **ACCEPTED LIMITATION, already resolved, not reopened**: D62 already adjudicated this in favor of the more detailed component-specific prose (`COMPONENTS.md`) as the higher-fidelity source, disclosed non-blocking in every admin screen's `design-to-code/screens/admin-*.json#/knownGaps` — nothing further to fix; re-opening the token file's own conflicting breakpoint value would mean editing `design-tokens.json` itself, a locked design-system file this phase may not touch (H5). <br>• **No real certificate image asset** (`CertificateCard`/`CertificateDetail` render a token-driven placeholder panel instead of an actual generated/stored certificate image, D42) — **ACCEPTED LIMITATION**: the backend has no certificate-image generation or storage at all (`CertificateDetailResponse` is pure text data, no image URL field) — building a real rendering/asset pipeline is a new-feature-shaped gap, correctly out of a QA/polish phase's bug-fix scope, same posture D42 originally took. <br>• **Arabic typography placeholder on Android** (`FontFamily.SansSerif` instead of a real Noto Sans Arabic asset) — **ACCEPTED LIMITATION**, already re-confirmed this same phase at row B9 (2026-09-23) on a real emulator: zero font assets exist anywhere in the repo to wire in trivially; not re-litigated here, B9's verdict stands. <br>• **Capped Instructor/Admin visual fidelity** (~66-78%, D48/D49) — **ACCEPTED LIMITATION**: both caps are the direct, disclosed consequence of the locked `product/SCREEN_INVENTORY.md`/`ux/INSTRUCTOR_ADMIN_UX.md` specs conflicting with the separate locked showcase mockup (3-vs-4 stat cards, 2-tab-vs-3-tab/persistent-rail Course Editor, exact table columns) — D48/D49 deliberately did not chase the showcase past the locked functional spec's own authority, since doing so would mean silently overriding a locked Product/UX decision with a visual reference, which this phase's own H5 instruction (audit conformance, never patch a locked-doc conflict) forbids without the user's explicit re-ratification. <br>• **InstructorCard/Avatar bypass + unscaled fallback font-size + duplicate `.mtx-avatar` selector** (this phase's own D3 finding) — **FIXED**, see updated D3 row above. <br>• **Admin search placeholder clipping** (this phase's own D3 finding) — **FIXED**, see updated D3 row above. |
| D6 | Zero files under `design-system/`, `product/`, `ux/`, `architecture/` modified without explicit user approval. | **CONFIRMED (2026-09-23).** `git log 33c59c6..HEAD --stat -- design-system/ product/ ux/ architecture/` (33c59c6 = Phase 7 completion commit, the Phase 8 starting point) shows exactly 3 commits touching 6 files total, all pre-approved one-line factual corrections, zero design/content decisions changed: `6a1748c` (D-5, `architecture/TESTING_STRATEGY.md` — Testcontainers→real-local-MongoDB description correction), `a9c0a42` (D-5 follow-up, `architecture/DEPLOYMENT.md`/`TECH_STACK.md`/`IMPLEMENTATION_ROADMAP.md`/`TESTING_STRATEGY.md` — the same stale Testcontainers reference corrected in the 3 remaining files it was verbatim-duplicated into), `6a1748c4`/`ec8db01` (D-4, `product/SCREEN_INVENTORY.md` — stale "v1.2" COMPONENTS.md version-string corrected to "v1.3.2"). **Zero files under `design-system/` or `ux/` touched at all** in the entire Phase 8 window. Every one of these 6 touches is a verbatim factual correction (a version number, a dependency/mechanism description) explicitly named and pre-approved in this same document's own "Known discrepancies" table (D-4, D-5) before being applied — no design, content, or product/UX decision was changed in any of them. Discipline held. |

## E. Accessibility Pass (M17 / manual, per H4)

| # | Criterion | Status |
|---|---|---|
| E1 | Contrast spot-checked against `ACCESSIBILITY.md §1`/§13, Light and Dark. | NOT STARTED |
| E2 | Focus states visible/correct on Web; full keyboard navigation of portfolio flow, no trap, no unreachable control. | NOT STARTED |
| E3 | Touch targets meet §4 minimum on Android and Web mobile viewport. | NOT STARTED |
| E4 | Screen-reader labels present/meaningful on portfolio flow (browser a11y tree; TalkBack/Accessibility Scanner). | NOT STARTED |
| E5 | Color never the only signal — re-verified on Quiz Results correct/incorrect breakdown, tied to A2. | NOT STARTED |
| E6 | Disabled-state contrast and error accessibility verified on both clients. | NOT STARTED |
| E7 | Text scaling (browser zoom; Android font scaling) — no clipping/unreachable content on portfolio flow. | NOT STARTED |
| E8 | Reduced-motion behavior honored where motion is used. | NOT STARTED |
| E9 | Long-translated-content resilience spot-checked in Arabic on text-dense screens. | NOT STARTED |

## F. Performance Sanity (M17, per H4)

| # | Criterion | Status |
|---|---|---|
| F1 | Page-load sanity measured for portfolio-flow Web screens, stated method, recorded numbers. | NOT STARTED |
| F2 | Video start time measured on Web and Android against local media storage. | NOT STARTED |
| F3 | AI Tutor response latency measured in stub mode only (H3/D146 stands). | NOT STARTED |
| F4 | Any unacceptable result fixed or recorded with a reason; no number reported without its method. | NOT STARTED |

## G. Cross-Client Parity & Regression

| # | Criterion | Status |
|---|---|---|
| G1 | `tools/cross-client-check/check.js` re-run against the live local stack; all 8 assertion groups PASS after Phase 8 changes. | **PASS (2026-09-23, Phase 8 B6).** Re-run twice against the live local stack (backend already up on `:8080`, seeded, left untouched per this batch's own instruction not to restart it) after all Phase 8 backend fixes landed so far (D-10, D-11, A4, A5). Both runs: all 9 assertion groups (the script's own README/table enumerates 9 — C1, D1, A1, A2/E1, E2, A3, A4, A5, E3 — the summary line's "8" in the tool's own header comment is a stale off-by-one in the tool itself, not touched here since it's cosmetic and out of this row's scope) report `PASS` (none `NOT-EXERCISED` this run — A4 certificate parity actually exercised both times). Confirms A4's certificate-issuance fix and A5's AI Tutor fixes hold identically across both the cookie/CSRF (Website-shaped) and Bearer-token (Android/KMP-shaped) transports. Not re-run again after this same batch's own C4 concurrency fixes (`EnrollmentService`/`CertificateService`) since those only change behavior under genuine concurrent duplicate requests, which this sequential-flow script doesn't drive — covered instead by the new backend integration tests in C4. |
| G2 | Full nine-step portfolio-priority flow re-walked end-to-end on Website and Android after all fixes land — EN/AR, Light/Dark. | NOT STARTED |
| G3 | No Phase 8 fix reintroduces a Phase 7-fixed defect (C4 auth staleness, cross-account tab-state leak, shadowJar packaging, `keepalive` progress save). | **PASS, Android-relevant items re-checked (B5, 2026-09-23).** The Android-relevant item — cross-account tab-state leak (Phase 7 T11) — was live-regression-tested this batch: logged in as `student1@mentora.dev`, purchased/completed a course, viewed My Learning/Certificates/AI Tutor content, then logged out and logged in as `student2@mentora.dev` on the same running app instance (no process kill in between). Every tab (Home, Explore, My Learning, AI Tutor, Profile) showed exclusively `student2`'s own data (name "Salma Mostafa", own distinct enrolled-course set, own distinct AI Tutor chat history, own distinct certificate) — zero visible carryover from `student1`'s session. `MentoraNavHost.kt` source re-inspected: both `clearAllTabBackStacks(navController)` call sites (login-success and logout, see G4) are unchanged from the Phase 7 fix. The other three named Phase 7 defects (C4 auth staleness, shadowJar packaging, `keepalive` progress save) are backend/Web-shaped concerns outside this Android batch's direct verification surface — not independently re-tested here; left to the backend/Web batches that own them. |
| G4 | `clearAllTabBackStacks()` remains correctly invoked at every account-switch site. | **PASS (B5, 2026-09-23).** Source-verified `mobile/androidApp/.../navigation/MentoraNavHost.kt`: exactly 2 call sites, unchanged from the Phase 7 T11 fix — line 142, inside the login-success branch (`previous !is AuthState.Authenticated && isAuthenticatedNow`), called before consuming any pending nav intent; line 165, inside the logout branch (`previous is AuthState.Authenticated && !isAuthenticatedNow`), called after resetting `pendingNavIntent`/navigating to `ExploreGraph`. No other account-transition branch exists in this `when` block, so these 2 are exhaustive for the current auth-state-transition surface. Live-verified effective (not just present in source) via the same account-switch walk described under G3: no stale per-tab state resurfaced for `student2` after `student1`'s session. |

## I. Portfolio Demo Readiness (M16 residue + M17)

| # | Criterion | Status |
|---|---|---|
| I1 | Clean-checkout dry run of `start-mentora.ps1`/`stop-mentora.ps1` + READMEs brings up the full local stack with no undocumented step. | NOT STARTED |
| I2 | `seedDemoData` produces demo-quality data: no empty state, no broken thumbnail, no unplayable video, no lorem-ipsum on a reviewer-visible screen. | NOT STARTED |
| I3 | Android demo path verified against local backend via `10.0.2.2`; hardcoded-base-URL limitation re-stated or resolved. | **PASS (B5, 2026-09-23).** Verified live end-to-end on a fresh debug APK: every screen walked this batch (login, registration, browse, checkout, course player, quiz, certificates, AI Tutor, account switching, relaunch) made real network calls to the local backend via `10.0.2.2:8080` and worked correctly — confirms the demo path is functional, not just configured. **Re-stated, not a bug:** `MentoraApplication.kt:88-98` resolves `ApiEnvironment` via `if (BuildConfig.DEBUG) ApiEnvironment.androidEmulator() else ApiEnvironment.androidEmulator()` — both branches currently resolve to the same emulator URL, with the `else` (release) branch's own code comment explicitly disclosing why: no production backend has ever existed anywhere in this project (Phases 1-3 never deployed one), so a release build correctly has no real endpoint to point at yet; using the emulator URL as a release placeholder is a deliberate choice so a stray release build fails cleanly (blocked cleartext / no such host) rather than silently misbehaving, not an oversight. Since the portfolio demo path only ever ships/runs a **debug** build (`installDebug`, exactly as used throughout this batch), the `BuildConfig.DEBUG` branch — the one actually exercised — is unconditionally correct for local demo purposes. No fix needed; this is a disclosed, intentional build-config gate, not a hardcoded-URL defect. |
| I4 | Nine-step portfolio-priority flow demo-ready end-to-end, both languages, every in-scope client (H1). | NOT STARTED |
| I5 | iOS simulator run-verification (M16) recorded as ACCEPTED LIMITATION — Phase 5 deferred, never PASS. | NOT STARTED |
| I6 | Repository presents well to a portfolio reviewer: no stray artifacts, no dead files, READMEs accurate, `git status` clean. | NOT STARTED |

## J. Documentation Accuracy & Scope Discipline (M17 cross-check)

| # | Criterion | Status |
|---|---|---|
| J1 | No real payment code path exists anywhere — word-boundary scan re-run across backend/web/mobile source. | PASS (B2) — word-boundary scan (stripe/paypal/creditcard/credit_card/payment_intent/braintree/square.js/adyen, case-insensitive) across `backend/src`, `web/src`, `mobile/shared/src`, `mobile/androidApp/src` re-run 2026-09-22: zero real matches. Only false-positive substring hits: "stripe" inside a code comment about a visual "vertical stripe texture" (`mobile/androidApp/.../CourseArtwork.kt`), and "adyen" as a case-insensitive substring of "alreadyEnrolled" in enrollment DTOs/services (backend, web, mobile shared). Corroborated by the existing `mobile/shared/src/commonTest/.../NoPaymentVocabularyTest.kt`. Mirrors Phase 7 D2. |
| J2 | Every bullet of `ARCHITECTURE.md §5` re-verified against what was actually built. | PASS (B2) — re-verified 2026-09-22: (1) `design-system/`, `product/`, `ux/` confirmed untouched since the initial commit (`git log -- design-system/ product/ ux/` shows only `c3a2ed5 Initial Mentora project setup`) aside from this batch's own two pre-approved D-4/D-5 one-line corrections; (2)/(3) "did not write production code" / AI-provider-resolved-not-implemented bullets describe the scope of the architecture-authoring phase itself at time of writing (historical), not an ongoing claim about the repo — not a discrepancy; (4) `product/SCREEN_INVENTORY.md` still lists exactly 29 `### N.` screen headings; (5) no cloud-infra config found repo-wide (`*.tf`, `terraform*`, `*cloudformation*`, `k8s*`, `kubernetes*` — zero matches). All bullets still hold. |
| J3 | Stale docs corrected in `execution/`: D-1 (sequencing line), D-2 (Android test count), D-3 (decision count off-by-one). | FIXED (B2) — `execution/MASTER_IMPLEMENTATION_PLAN.md:79` corrected to "Phases 1-7 are COMPLETE; Phase 8 is IN_PROGRESS"; `mobile/androidApp/README.md:70` corrected to 246/246 (confirmed live via `./gradlew :androidApp:testDebugUnitTest`, 0 failures/errors); `execution/PHASE_HANDOFF.md:419` corrected "D146-D161"/"Sixteen" to "D146-D162"/"Seventeen" and D162 added to the enumeration (confirmed last decision in `DECISIONS_LOG.md` is D162). Commit: "Phase 8 B2: fix stale doc facts (J3)". |
| J4 | D-4 (`SCREEN_INVENTORY.md` stale "v1.2" reference, a locked doc) — flagged to user, one-line factual correction applied since it is non-design-affecting. | NOT STARTED |
| J5 | `CURRENT_STATUS.md`, `DECISIONS_LOG.md`, `PHASE_HANDOFF.md` updated with a complete Phase 8 record in the established 10-subsection format. | NOT STARTED |
| J6 | Clean git state at every checkpoint, one commit per task, pushed after each. | NOT STARTED |
| J7 | Every Phase 8 change traces to a row in this matrix — no new features, no redesign, no invented scope. | NOT STARTED |
| J8 | Closing statement of what the project deliberately did not do (final phase — no gap left looking like an oversight). | NOT STARTED |

---

## Known discrepancies carried into Phase 8 (from repository recovery, 2026-09-22)

| # | Discrepancy | Disposition |
|---|---|---|
| D-1 | `MASTER_IMPLEMENTATION_PLAN.md:79` stale sequencing line ("Phase 1 IN_PROGRESS; Phases 2-8 NOT_STARTED"). | Fix under J3 (writable `execution/` doc). |
| D-2 | `mobile/androidApp/README.md:70` documents 241/241 unit tests; actual is 246/246. | Fix under J3. |
| D-3 | `PHASE_HANDOFF.md:419` says "D146-D161"; D162 exists (the entry itself). | Fix under J3. |
| D-4 | `product/SCREEN_INVENTORY.md:3` says COMPONENTS.md "is now v1.2"; actual is v1.3.2. Locked doc. | RESOLVED (B2) — flagged under J4, one-line factual correction applied 2026-09-22 (confirmed `design-system/DESIGN_SYSTEM.md:1` = "v1.3.2"). Per-screen "(added DS v1.2)" historical markers left untouched — they correctly record when those 7 components were added, not the current doc version. Commit: "Phase 8 B2: fix stale COMPONENTS.md version reference (D-4)". |
| D-5 | `backend/build.gradle.kts` declares unused Testcontainers deps; `TESTING_STRATEGY.md §1` describes Testcontainers-based integration tests that don't exist (real tests hardcode local Mongo). | RESOLVED (B2) — confirmed zero `testcontainers` imports/usage anywhere in `backend/src`; removed the 3 dead `org.testcontainers:*` test dependency declarations from `backend/build.gradle.kts`; verified `./gradlew compileTestKotlin` still succeeds. Corrected `TESTING_STRATEGY.md §1`'s Backend Integration row (flagged, non-design-affecting factual correction) to describe the actual mechanism: a real local MongoDB instance at `mongodb://localhost:27017/?replicaSet=rs0` (confirmed in `HealthCheckIntegrationTest.kt` and siblings). Contributes to C8 (full C8 grading, including a fresh backend test run, deferred to B6). Commit: "Phase 8 B2: remove dead Testcontainers dep + correct locked-doc claim (D-5)". |
| D-6 | `TESTING_STRATEGY.md §5` mandates Web Vitest+RTL tier — never built. | Resolved under H2/C3 (bounded tier). |
| D-7 | M14's "lint/CI check for missing translation keys per platform" never built for Web/Android. | Resolved under B2/B3. |
| D-8 | M14/M16/M17 name iOS as in-scope. | Resolved under H1. |
| D-9 | No a11y/performance tooling exists. | Resolved under H4. |
| D-10 | **New defect found during B0 baseline (not previously documented):** `AdminRepository.listCourses` used MongoDB `$text` search combined with `_id`-ascending pagination and no relevance sort. `$text` OR-matches any query word across the corpus; once the catalog exceeds the 20-item default page limit, a genuinely-matching course whose `_id` sorts past page 1 was silently dropped from admin search results — reproduced live via Playwright (`10-admin-course-management.spec.ts`, both attempts failed identically), root-caused via direct API probing. **FIXED:** replaced `$text` with the same case-insensitive substring-regex pattern already used correctly by `userSearch` in the same file (deterministic match, compatible with `_id` pagination). Added a regression test (`AdminIntegrationTest`: "course search finds a title beyond the default page limit even when a common word is shared") reproducing the exact defect class. Backend suite 128/128 (127+1), E2E spec re-verified passing live. The public Explore `CourseRepository.listPublished` search has the identical `$text`+`_id`-sort pattern but was not reproduced as broken and intentionally also searches localized (EN/AR) metadata via the text index (`CoursesIndexes.kt`) — changing its ranking behavior risks that locked design intent, so it is **not touched** in this phase; recorded here as a disclosed, related, latent risk for any future dedicated work, not silently carried as if unknown. **Trade-off disclosed:** the regex substring match is literal and contiguous, unlike `$text`'s word-based, stemmed, order-independent OR-matching — a multi-word out-of-order query (e.g. searching "Zephyr Course" against a course titled "Course Zephyr") that previously matched under `$text` now returns zero results; single-word searches are unaffected, and this mirrors `userSearch`'s existing, already-accepted behavior in this same file. |
| D-11 | **The public Explore `CourseRepository.listPublished` search — flagged as a disclosed latent risk in D-10 — was subsequently reproduced as ACTIVELY BROKEN during B4 Website QA**, once the E2E-test-generated catalog grew past the 20-item default page limit over the course of this session (two independent Playwright specs, `09-instructor-course-authoring.spec.ts` and `10-admin-course-management.spec.ts`, both failed identically: a just-published course's own exact title search on `/explore` found nothing). Same root cause as D-10: `$text` OR-matching + `_id`-ascending pagination with no relevance sort. **FIXED:** replaced `text(it)` with a 6-field case-insensitive substring-regex OR (`title`, `description`, `translations.en.{title,description}`, `translations.ar.{title,description}`) — preserving the full multi-locale search coverage `CoursesIndexes.kt`'s text index was built for (D57), unlike a naive title-only regex would have. Added a regression test (`CoursesCategoriesIntegrationTest`: "public course search finds a title beyond the default page limit even when a common word is shared") reproducing the exact defect class; the existing multi-locale search test ("stays searchable in both languages") re-verified passing, confirming no loss of the EN/AR search-coverage feature. Full backend suite green at the time of this fix (confirmed 133/133 by direct count). Same word-order/contiguous-substring trade-off as D-10 applies here too. |

---

**Do not silently skip any row.** Every row above must be revisited before Phase 8 closes.
