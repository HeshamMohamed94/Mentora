# Phase 8 — QA, Polish & Portfolio Demo — Acceptance Criteria

**Status:** OPEN — authored at Phase 8 kickoff (2026-09-22), before any Phase 8 code change. This is the
FINAL Mentora phase; there is no Phase 9. Every row below must resolve to PASS, FAIL, PARTIAL,
NOT TESTABLE, or ACCEPTED LIMITATION before this phase can close — none may be left unmarked.

No formal Phase 8 acceptance-criteria document existed anywhere in the repository prior to this one
(confirmed by full-repo search). This checklist is derived strictly from already-locked authoritative
sources, not invented — see "Authority sources" below.

## Authority sources

1. `execution/MASTER_IMPLEMENTATION_PLAN.md` — Phase 8 definition (M14 + M15 + M17).
2. `architecture/IMPLEMENTATION_ROADMAP.md` — M14 "Localization & RTL Completion" (line 261),
   M15 "Testing Hardening" (line 279), M16 "Local Demo Readiness" (line 295, largely closed by Phase 7,
   residue only), M17 "Final Polish & Acceptance" (line 315).
3. `architecture/ARCHITECTURE.md § 5` — "What This Phase Explicitly Did Not Do" — the scope-discipline
   checklist M17 requires cross-checking against everything actually built.
4. `architecture/TESTING_STRATEGY.md` — the locked target test coverage M15 must reach.
5. `design-system/DESIGN_SYSTEM.md` (v1.3.2, **LOCKED — audited, never edited**), `COMPONENTS.md`,
   `ACCESSIBILITY.md`, `LOCALIZATION.md`, `DESIGN_RULES.md`, `CONTENT_RESILIENCE.md`.
6. `product/SCREEN_INVENTORY.md` — the 29-screen surface Phase 8's QA pass must cover.
7. `execution/PHASE_HANDOFF.md` §§ 6 (Phases 1/2/3/4/6/7 "Known limitations") — every carried-forward gap
   this phase must give an explicit disposition, not silently re-carry.
8. `execution/DECISIONS_LOG.md` D146 (AI provider live-verification deferral, unaffected by this phase)
   and the Phase 5 freeze (iOS T1-T11 done, T12-T23 not started, untouched by this phase).
9. The Phase 8 kickoff message itself (2026-09-22) — QA/defect cleanup/visual polish/Design System
   compliance/acceptance verification/regression testing/demo readiness/documentation cleanup/portfolio
   readiness; explicitly NOT a redesign or new-feature phase; iOS stays deferred; no real AI provider call.

**Do not re-derive scope from memory or re-litigate any of the above — this checklist assumes them as given.**

---

## H. Scope Reconciliation (resolve first — blocks grading of H-dependent rows below)

| # | Point | Resolution |
|---|---|---|
| H1 | M14/M16/M17 name six surfaces and "all three clients," including iOS. | **Resolved, per explicit user decision at Phase 8 kickoff:** Phase 8 grades exactly five surfaces / two clients — Public Web, Student Web, Instructor Web, Admin Web, Android. iOS's share of any M14/M16/M17 row is recorded as **ACCEPTED LIMITATION (Phase 5 deferred)**, never PASS. Consistent with Phase 7 § H1. |
| H2 | `TESTING_STRATEGY.md § 5` mandates a Vitest + React Testing Library unit/component test tier for Web. It has never existed — Web has Playwright E2E only. | **Resolved, per explicit user decision:** build a **bounded** Vitest+RTL tier covering the highest-value/highest-risk components only (quiz scoring/results, demo-checkout flow, auth forms) — not exhaustive coverage. Closes the locked-doc gap without open-ended scope. |
| H3 | Real Anthropic provider verification (D146) is deferred. | Unchanged, not reopened. Any Phase 8 row touching live AI behavior is **NOT TESTABLE — deferred by user decision**. No `AI_PROVIDER_API_KEY` requested, referenced, or simulated as real. Only existing mock/fake/test-provider infrastructure is used. |
| H4 | No accessibility/performance measurement tooling (axe/Lighthouse) exists in the repo. | **Resolved, per explicit user decision:** manual audit only. A11y against `ACCESSIBILITY.md` §§1-15 via browser a11y tree / keyboard nav / TalkBack / Accessibility Scanner spot-checks. Performance via stated-method manual timing. No new tooling dependency added. |
| H5 | Design System v1.3.2 is LOCKED. | Phase 8 audits **conformance to** it only. A real DS defect found here is recorded, never patched inside this phase. Zero files under `design-system/`, `product/`, `ux/`, `architecture/` are modified without the user's own explicit approval (small factual doc corrections — D-4, D-5 below — are flagged, not silently made). |
| H6 | The AppDialog scrim-dimming regression (§ A1) survived two prior bounded fix attempts with no root cause found and has zero real screen consumers. | **Resolved, per explicit user decision:** time-boxed investigation (one focused session) attempting a real fix; if not root-caused in that window, re-record as ACCEPTED LIMITATION and move on — no further time spent chasing it. |

---

## A. Known-Issue Disposition

| # | Criterion | Status |
|---|---|---|
| A1 | AppDialog scrim-dimming regression: re-reproduced on a real emulator, root-caused, and either FIXED (both `AppDialogColorAndLayoutTest` color assertions green) or re-recorded as ACCEPTED LIMITATION per H6's time-box. | **FIXED** (2026-09-22/23) — re-reproduced first (`darkThemeScrimMatchesOverlayScrimTokenExactly`/`lightThemeScrimMatchesOverlayScrimTokenExactly` both failed identically to D160: status-bar-strip sample `Color(0.98039216, ...)`, bit-for-bit `0xFAFAFA`). **Root-caused** via `adb shell dumpsys window windows` captured live mid-test (temporary diagnostic sleep, reverted before commit): the Dialog's own window (`Window #8`) still carried `fitTypes=STATUS_BARS NAVIGATION_BARS CAPTION_BAR` and a frame top-inset of ~35px despite `decorFitsSystemWindows=false` + `FLAG_LAYOUT_NO_LIMITS` already being set — while the host Activity's own window (`Window #9`, `androidx.activity.ComponentActivity`, the real host under `createComposeRule()`) sat fully edge-to-edge (`frame=[0,0][1080,2400]`, no `fitTypes` at all, `DRAWS_SYSTEM_BAR_BACKGROUNDS`/`FORCE_DRAW_STATUS_BAR_BACKGROUND`). `0xFAFAFA` is exactly `Theme.Material.Light`'s default `windowBackground` (`background_material_light`) — the status-bar pixel was showing that host window's bare background bleeding through behind the dialog, not any scrim. This is new diagnostic evidence D160's two prior blind attempts (disabling status/nav-bar contrast enforcement; explicit `setLayout(MATCH_PARENT, MATCH_PARENT)`) never produced. **Fix:** `AppDialog.kt`'s `SideEffect` now also sets `FLAG_LAYOUT_IN_SCREEN`/`FLAG_LAYOUT_INSET_DECOR` (present on the Activity window's own flags, absent from a Dialog's by default) and, API 30+, `layoutInDisplayCutoutMode = LAYOUT_IN_DISPLAY_CUTOUT_MODE_ALWAYS` — legacy pre-insets-API window-layout controls that still influence `fitInsetsTypes` computation on this platform version. **Verified:** `AppDialogColorAndLayoutTest` (all 6 tests, including both previously-failing scrim assertions) green on 3 consecutive live runs on `Chatting_Pixel_8_API_36`; `:androidApp:testDebugUnitTest` 246/246; `:androidApp:lintDebug` 0 errors, 105 warnings (back to the Phase 7 baseline, no new warnings). Full instrumented suite (106 tests) re-run twice for regression-checking: all non-A1 failures observed (`NavigationShellTest`/`MediaPlaybackControllerInstrumentedTest`, 4-5 tests) were `RateLimitedAuth` (HTTP 429) from this session's own repeated heavy `connectedDebugAndroidTest` runs exhausting the backend's 10-req/min `auth` rate limiter (`backend/src/main/kotlin/com/mentora/backend/plugins/RateLimiting.kt`) plus downstream state effects of those failures — the same transient class Phase 7's own T13 audit (D162) already documented, unrelated to `AppDialog.kt`/window flags, zero overlap with the fixed tests. Commit citing "Phase 8 A1". |
| A2 | Quiz Results answer-badge spacing (`web/src/components/screens/quiz-results-screen.tsx`) fixed; verified live in EN-LTR, AR-RTL, Light, Dark; no regression to the 19/19 Playwright suite. | FIXED — reproduced live (fresh student account, mixed correct/incorrect quiz attempt) 2026-09-22/23: the raw vertical gap between the last answer option and the per-question outcome badge was never literally 0px (it inherits `QuestionCard`'s own `gap-3` flex gap, measured 12px via `getBoundingClientRect`, matching `--space-3`) — the real, reproducible defect is that the badge (a `span.mtx-badge`, meant to be a compact pill per `COMPONENTS.md`) had no `align-self` override, so the parent flex column's default `align-items: stretch` forced it to the full row width, making it visually indistinguishable from — and read as an extension of — the answer-option row directly above it (unlike the page's own "Passed"/"Failed" badge, which stays compact because `.mtx-result-score` already sets `align-items: flex-start`). Fixed by wrapping the badge in `<span className="self-start">` in `quiz-results-screen.tsx` (mirrors the existing `self-start`/`items-start` Tailwind pattern already used elsewhere in this codebase, e.g. `dashboard-screen.tsx`, `course-player-screen.tsx` — no new value invented, no design-system file touched). Verified with live Playwright screenshots, before/after, across EN-LTR/Light, EN-LTR/Dark, AR-RTL/Light, AR-RTL/Dark, plus a 390px mobile viewport: the badge now renders as a small pill clearly set apart from the option list in every combination (RTL confirmed correctly mirrored — pill right-aligned under `صحيح`/`غير صحيح`). Full Playwright chromium suite re-run: 20 tests, 18 passed outright + 2 flaky (pre-existing `enrollInCourse` search-timing flakes in `05-complete-lesson.spec.ts`/`10-admin-course-management.spec.ts`, unrelated to this change, both cleared on the built-in single retry) — no regression. Commit: "Phase 8 A2: fix Quiz Results answer-badge stretching to full row width". |
| A3 | `isEnrolled` Web/Android pagination divergence: explicit verdict — unified, or re-affirmed ACCEPTED LIMITATION with a reason restated against Phase 8 scope. | REAFFIRMED ACCEPTED LIMITATION (2026-09-22) — code confirmed unchanged: `web/src/lib/api/enrollment.ts:47-48` (`listEnrollments`) fetches a single `?limit=100` page; `mobile/androidApp/.../CourseDetailsViewModel.kt:176-188` (`isEnrolledIn`) pages fully via `nextCursor` until exhausted — still the exact D157 divergence. Re-verified reachability against the *current* (grown) dataset rather than assuming the original 3-course figure still holds: `GET /api/v1/courses?limit=100` (public, unauthenticated) returned exactly 20 published courses total, no `nextCursor` (2026-09-22 live check) — up from 3 seed courses via accumulated E2E/demo-authoring test courses, but still 5x under Web's 100-item page cap. Since `GET /enrollments` is scoped to the authenticated caller, one student's own enrollment count is hard-bounded by the total catalog size (20), so the divergence is still structurally unreachable. Not fixed: unifying it would mean adding a real `isEnrolled` field to `GET /courses`/`GET /courses/{id}` (Phase 1 D11's original deferral) — a cross-client API contract change, correctly out of a QA/polish phase's scope per the task's own instruction not to fix without flagging first. |
| A4 | Phase 7 §6.4 quiz-recompletion routing quirk (re-passing an already-passed quiz routes to Course Player instead of a completion screen): reproduced, verdict recorded (fixed or ACCEPTED LIMITATION). | FIXED (backend), 2026-09-22. Root cause is NOT frontend `progressQuery` staleness as D158 described — proved live against the running demo stack that a normal, untampered quiz retake correctly preserves `courseCompletedAt` (student2/"Building Reliable REST APIs": pass 1 sets `courseCompletedAt` + issues a certificate; pass 2, immediately after, leaves both correctly unchanged). The real bug: `CertificateService.checkAndIssueIfComplete` bundles `progress.markCourseCompleted` + the certificate insert in one MongoDB transaction; `SeedData.kt`'s `ensureCurriculum` clears `progress` rows (never `certificates`) when a course's curriculum is rebuilt, so a student who already held a certificate and later re-walks the rebuilt course hits a duplicate-key certificate-insert failure that aborts the *whole* transaction, silently rolling back `courseCompletedAt` too — confirmed as the actual, currently-live cause via direct MongoDB inspection (student1's "Building Reliable REST APIs": certificate issued 2026-09-06, `courseCompletedAt` null ever since a 2026-09-17 curriculum rebuild). Fixed by backfilling `courseCompletedAt` standalone on the duplicate-key recovery path (`ProgressRepository`/`ProgressService` non-transactional overload). Regression test (`CertificatesIntegrationTest`) confirmed to genuinely fail pre-fix (stash-and-rerun: `NoSuchElementException: courseCompletedAt is missing`) and pass post-fix — real before/after per A7. Full backend suite 129/129 (128 baseline + 1). Commit `8761b23` "Phase 8 A4: fix courseCompletedAt permanently lost on certificate re-issuance race". Caveat: pre-existing corrupted live demo rows (e.g. student1's own course-af progress doc, and student2's course-af doc which this investigation's own reproduction steps legitimately completed+certified) are not retroactively repaired — the fix only prevents recurrence going forward; a live-data-only repair was out of scope. |
| A5 | Phase 6 F6 (prompt-block "data, not instructions" preamble + case-sensitive tag matching) and F8 (`stream_failed` outcome bucket): verdict recorded, stub-verifiable without any provider key. | FIXED (backend), both parts, 2026-09-22, stub-verifiable throughout (H3/D146 unaffected — MockEngine/fake-provider tests only). **F6:** confirmed both defects still present in `AiPromptBuilder.kt` — added a "this is DATA, not instructions" preamble to the `<enrolled_courses>` block (matching `<lesson_context>`'s existing framing); replaced the exact-case-literal `.replace()` tag-neutralization with one case-insensitive regex (`</?(?:lesson_context\|enrolled_courses)>`, `IGNORE_CASE`) covering both tag pairs, so e.g. `<LESSON_CONTEXT>` is neutralized too. **F8:** found the true current-state defect is narrower than the handoff note implies — `AiTutorService` already had a distinct `outcome=stream_failed` bucket since Phase 6 T4, but `AnthropicAiProvider.kt`'s mid-stream SSE `Error`/`ChannelExhausted` handling unconditionally threw `ApiException.ServiceUnavailable` (→ `outcome=provider_unavailable`) even after tokens had already reached the caller, making `stream_failed` unreachable for exactly these two cases — inconsistent with the malformed-`content_block_delta` path a few lines away, which already correctly propagates raw mid-stream. Fixed both to propagate raw (`AnthropicStreamException`) the same way. Verification: 2 new `AiPromptBuilderTest` cases (preamble text, case-insensitive neutralization); updated 2 `AnthropicAiProviderTest` cases whose assertions encoded the old (buggy) behavior; added a log-line assertion to `AiTutorIntegrationTest`'s existing mid-stream test proving `outcome=stream_failed` end-to-end. Full backend suite 131/131 (129 + 2). Commit `91b4a26` "Phase 8 A5: fix AI Tutor F6 prompt preamble/case-sensitive tag matching and F8 stream_failed outcome". |
| A6 | Every open item in `PHASE_HANDOFF.md` §§6 (Phases 1/2/3/4/6/7) re-read and assigned: STILL OPEN-ACCEPTED / FIXED IN PHASE 8 / NO LONGER APPLICABLE. | DONE (2026-09-22) — all ~38 items across Phases 1/2/3/4/6/7 §6 classified; full item-by-item list recorded in this session's report (not reproduced here for readability). Summary: **FIXED IN PHASE 8 (3):** Phase 6 §6.2 (F6) and §6.3 (F8) — see A5; Phase 7 §6.4 (T10 quiz-recompletion observation) — see A4. **NO LONGER APPLICABLE (3):** Phase 1 §6.7 (synthetic seed video) — already superseded by Phase 2's own struck-through §6.2 resolution (PRE-PHASE-3 real-video seed expansion); Phase 3 §6.2 (androidMain Keystore path untested against a real provider) — explicitly closed by Phase 4 T4 Part D's `AndroidTokenStorageInstrumentedTest.kt` (confirmed present, real `connectedDebugAndroidTest` against the real `AndroidKeyStore`); Phase 4 §6.1 (Quiz/Quiz Results not re-verified live in that session) — superseded by Phase 7 T8's cross-client harness + T10/T11's live Website/Android walks. **STILL OPEN-ACCEPTED (~32):** every remaining item — each is a deliberate, already-disclosed, low-risk simplification; a real environment constraint (no Docker on this machine, no Mac for iOS); a locked-design conflict correctly left to its own D5/H5-governed row rather than silently patched here; or a new-feature-shaped gap (password-change endpoint, AI Tutor docked panel, offline cache, real certificate/icon/font assets) out of a QA/polish phase's fix-bugs-not-features scope. One item (Phase 6 §6.5, Android's "Login to Enroll" CTA not immediately refreshing post-login) is flagged as a plausible cheap-fix *candidate* for a future targeted task but was not fixed here — it falls outside this session's specific A3-A6 mandate and wasn't independently investigated. Items duplicating A1 (Phase 7 §6.3, AppDialog) and A2 (Phase 7 §6.2, Quiz Results spacing) were left as-is here since both are owned by parallel in-flight Phase 8 tasks this session (`AppDialog*`/`quiz-results-screen.tsx` were off-limits) — not independently reclassified to avoid conflicting with that work. |
| A7 | No known issue closed as "fixed" without a real, reproducible before/after verification on the affected client. | NOT STARTED |

## B. Localization & RTL Completion (M14)

| # | Criterion | Status |
|---|---|---|
| B1 | All 29 `SCREEN_INVENTORY.md` screens audited for EN/AR string coverage on each in-scope surface; no hardcoded English string reachable in `ar` locale. | PASS (B3) — grep-based sweep 2026-09-22/23 of every portfolio-priority + Admin/Instructor screen component under `web/src/components/screens/` (all 24 files), the public pre-auth pages (`app/[locale]/(public)/**`), `components/navigation/**`, and `components/ui/**`: zero plain-text JSX children (`>[A-Z]...<`) and zero hardcoded `placeholder=`/`aria-label=`/`alt=`/`title=` string literals outside `t()`/`useTranslations`. Consistent with B2's independent finding of 0 missing keys (491/491 en/ar). Necessarily a sampling exercise per the task's own framing (grep-based, not a full manual click-through of all 29 screens in both locales) but covers every screen component file in the repo, not merely the portfolio flow — no fix needed, nothing found to fix. |
| B2 | Mechanical translation-key parity check exists and passes for Web (`messages/en.json` ↔ `ar.json`) and Android (`values/strings.xml` ↔ `values-ar/strings.xml`) — closes D-7 (locked M14 deliverable). | PASS (B3) — built `web/scripts/check-translation-parity.js` (flattens both nested message trees to dotted key paths, diffs the sets, exits non-zero listing every missing/extra key); run 2026-09-23: "Translation-key parity OK: 491 keys match between en.json and ar.json." Android: `mobile/androidApp/src/test/kotlin/.../StringsParityTest.kt` (pre-existing, T18) confirmed already sufficient — parses the real `values`/`values-ar` XML directly (not a generator self-report), compares the FULL key sets both directions (`everyEnglishKey_hasAnArabicTranslation` / `arabicHasNoOrphanKeys_beyondWhatEnglishDefines`, `app_name` the one sanctioned OS-locale exception), plus blank-value and format-specifier-parity guards beyond mere key parity. No Android tooling changes needed. |
| B3 | Parity check wired into CI (extends `web-ci.yml`/`android-ci.yml`, not a parallel workflow), proven non-vacuous by a deliberate temporary drift. | PASS — added a `check:translation-parity` npm script and a new step in `web-ci.yml`'s existing `web-static` job (alongside lint/logical-properties/design-to-code/typecheck), not a new workflow. Android's `StringsParityTest` already runs via `android-ci.yml`'s existing `:androidApp:testDebugUnitTest` step in the unit-test job — no new step needed. **Proven non-vacuous both ways, then reverted exactly:** (1) Web — deleted `common.appName` from `ar.json`, ran the script: failed, exit 1, "Missing in ar.json (1): - common.appName"; `git status`/`git diff` on the restored file empty. (2) Android — deleted the `nav_home` `<string>` entry from `values-ar/strings.xml`, ran `./gradlew :androidApp:testDebugUnitTest --tests StringsParityTest`: `everyEnglishKey_hasAnArabicTranslation` FAILED with an `AssertionError` listing the missing key; restored, re-ran: BUILD SUCCESSFUL, `git diff` on the restored file empty. |
| B4 | RTL QA pass: every in-scope screen inspected under `ar`/RTL for mirroring correctness per `LOCALIZATION.md`/`DESIGN_RULES.md`. | NOT STARTED — full walk deferred to its own dedicated batch (B4 in `PHASE_8_IMPLEMENTATION_PLAN.md`) per this batch's scope (B3: tooling + B1 audit + a bounded RTL spot-check for B5). A non-exhaustive spot-check of 4 screens was performed live in this batch as supporting evidence for B5 (Explore, Login, Instructor Dashboard, Student Dashboard/Home, all under `/ar/`) — all four showed correct logical-property mirroring (sidebar docked to the layout-start/visual-right, `DataTable`-style list column order reversed to reading order, `StatCard`/search-field/nav-item order correct) with nothing to fix. Not sufficient coverage to close this row; left for B4's own batch. |
| B5 | Video scrubber and Mentora wordmark confirmed still LTR/unmirrored under `ar` (named explicitly in M14). | PASS (B3) — **Scrubber:** confirmed statically in `web/src/components/ui/video-player.tsx` — the timeline (`.mtx-video-timeline`) and volume slider both carry an explicit `dir="ltr"`, unconditional on locale, matching `LOCALIZATION.md § 2/§ 3`'s named exception. **Wordmark:** confirmed live — `/ar/explore` and `/ar/login` (both AR/RTL) screenshotted 2026-09-23, "Mentora" renders as a single unmirrored LTR run at the navbar/auth-header layout-start (visual right in RTL), exactly per `LOCALIZATION.md § 2/§ 8`'s locked decision; source (`auth-header.tsx`/`public-navbar.tsx`) renders it as plain text via `tCommon("appName")` with no manual `dir`/mirroring logic — correct by relying on the Unicode Bidi Algorithm's automatic isolation of an embedded Latin run, not by fighting it. Neither needed a code change — both already correct. |
| B6 | Locale-aware formatting (dates, prices, durations, numerals) verified on real screens in both locales. | NOT STARTED |
| B7 | Embedded English content inside an Arabic page still renders LTR (re-confirmed across the wider screen set). | NOT STARTED |
| B8 | `08-language-switch` Playwright flow extended to assert a sampling of screens beyond Settings. | PASS (B3) — added a new test to `web/e2e/08-language-switch.spec.ts` ("the persisted Arabic locale also renders Explore and Course Details correctly") asserting `explore.title`/`courseDetails.curriculum` AR strings and `dir="rtl"` on both screens after a locale switch originating in Settings, using the one seeded Arabic-authored course (`أساسيات تصميم تجربة المستخدم`) — Explore's `language` content filter correctly excludes English-only-authored courses from the `ar` listing, which is real product behavior (not a defect), so the fixture had to be a course with real Arabic content, not the suite's usual English-authored `QUIZ_COURSE_TITLE`. Full suite re-run `CI=true npx playwright test --project=chromium`: 19/19 on a clean run (one isolated re-run of `10-admin-course-management.spec.ts` confirmed pass); two immediately-prior back-to-back full-suite runs showed transient 429-rate-limit-cascade flakes/failures across unrelated specs (`01`, `03`, `06`, `07`, `10`) from hammering the real 10-req/min auth bucket (`e2e/helpers.ts`'s own documented rate-limit note) by re-running the whole suite repeatedly within its cooldown window — not a regression from this batch's changes; the new/modified spec itself passed in every run once past its own initial fixture-selection bug (fixed before commit). |
| B9 | Arabic typography placeholder on Android (`FontFamily.SansSerif`): verdict recorded — real asset added or ACCEPTED LIMITATION. | NOT STARTED |

## C. Testing Hardening (M15)

| # | Criterion | Status |
|---|---|---|
| C1 | Backend/`:shared`/`:androidApp` JVM/lint/Playwright suites pass at or above the Phase 7 T13 baseline (127/249/246/0 lint errors/19 chromium) — zero regressions. | NOT STARTED |
| C2 | Android instrumented suite (106 tests, local emulator) run and accounted for test-by-test; the 2 AppDialog failures resolved per A1; any transient failure characterized, not hand-waved. | NOT STARTED |
| C3 | Bounded Web Vitest+RTL tier (H2) exists and runs in CI. | NOT STARTED |
| C4 | High-risk-area coverage (demo-checkout idempotency, completion→certificate transaction, quiz `isCorrect` stripping) re-confirmed genuinely covered. | NOT STARTED |
| C5 | CI green on a clean run, twice in a row, across all in-scope workflows — run IDs recorded. | NOT STARTED |
| C6 | Deliberate-regression sanity check: introduced on a scratch branch, confirmed caught by the suite, reverted. | NOT STARTED |
| C7 | Every flaky test observed is stabilized or documented with root cause and reason for acceptance. | NOT STARTED |
| C8 | D-5 (dead Testcontainers dependency vs. locked `TESTING_STRATEGY.md` claim) reconciled. | NOT STARTED |
| C9 | WebKit E2E remains ACCEPTED LIMITATION (D64) — not reopened, not silently dropped. | NOT STARTED |

## D. Design System v1.3.2 Conformance Audit (LOCKED — audit only)

| # | Criterion | Status |
|---|---|---|
| D1 | `tokens-ci.yml` green: zero drift between `design-tokens.json` and generated outputs. | PROVISIONAL PASS (B2) — local equivalent `npm run validate:design-to-code` (`web/`) run 2026-09-22: "Design-to-Code validation: 37 screens, 6 patterns, 11 shared files checked. Validation PASSED." Actual `tokens-ci.yml` workflow not re-run in this static-only batch; full CI confirmation deferred to C5/B11. |
| D2 | Android `MentoraTokens` drift test green. | PROVISIONAL PASS (B2) — `./gradlew :androidApp:testDebugUnitTest --tests "com.mentora.android.theme.MentoraTokensDriftTest"` run 2026-09-22: BUILD SUCCESSFUL, all drift assertions green (colors, typography, shape, elevation, spacing, icon size, touch target). CI confirmation deferred to C5/B11. |
| D3 | Spot-audit of portfolio-flow screens for ad-hoc/hardcoded styling bypassing a token or specified component. | NOT STARTED |
| D4 | Web ↔ Android visual-parity audit on the portfolio-priority flow; divergence corrected or disclosed. | NOT STARTED |
| D5 | Each documented DS-drift item (icon placeholders, `DataTable` breakpoint conflict D62, no real certificate asset, Arabic font placeholder, capped Instructor/Admin fidelity) given explicit verdict. | NOT STARTED |
| D6 | Zero files under `design-system/`, `product/`, `ux/`, `architecture/` modified without explicit user approval. | NOT STARTED |

## E. Accessibility Pass (M17 / manual, per H4)

| # | Criterion | Status |
|---|---|---|
| E1 | Contrast spot-checked against `ACCESSIBILITY.md §1`/§13, Light and Dark. | NOT STARTED |
| E2 | Focus states visible/correct on Web; full keyboard navigation of portfolio flow, no trap, no unreachable control. | NOT STARTED |
| E3 | Touch targets meet §4 minimum on Android and Web mobile viewport. | NOT STARTED |
| E4 | Screen-reader labels present/meaningful on portfolio flow (browser a11y tree; TalkBack/Accessibility Scanner). | NOT STARTED |
| E5 | Color never the only signal — re-verified on Quiz Results correct/incorrect breakdown, tied to A2. | NOT STARTED |
| E6 | Disabled-state contrast and error accessibility verified on both clients. | NOT STARTED |
| E7 | Text scaling (browser zoom; Android font scaling) — no clipping/unreachable content on portfolio flow. | NOT STARTED |
| E8 | Reduced-motion behavior honored where motion is used. | NOT STARTED |
| E9 | Long-translated-content resilience spot-checked in Arabic on text-dense screens. | NOT STARTED |

## F. Performance Sanity (M17, per H4)

| # | Criterion | Status |
|---|---|---|
| F1 | Page-load sanity measured for portfolio-flow Web screens, stated method, recorded numbers. | NOT STARTED |
| F2 | Video start time measured on Web and Android against local media storage. | NOT STARTED |
| F3 | AI Tutor response latency measured in stub mode only (H3/D146 stands). | NOT STARTED |
| F4 | Any unacceptable result fixed or recorded with a reason; no number reported without its method. | NOT STARTED |

## G. Cross-Client Parity & Regression

| # | Criterion | Status |
|---|---|---|
| G1 | `tools/cross-client-check/check.js` re-run against the live local stack; all 8 assertion groups PASS after Phase 8 changes. | NOT STARTED |
| G2 | Full nine-step portfolio-priority flow re-walked end-to-end on Website and Android after all fixes land — EN/AR, Light/Dark. | NOT STARTED |
| G3 | No Phase 8 fix reintroduces a Phase 7-fixed defect (C4 auth staleness, cross-account tab-state leak, shadowJar packaging, `keepalive` progress save). | NOT STARTED |
| G4 | `clearAllTabBackStacks()` remains correctly invoked at every account-switch site. | NOT STARTED |

## I. Portfolio Demo Readiness (M16 residue + M17)

| # | Criterion | Status |
|---|---|---|
| I1 | Clean-checkout dry run of `start-mentora.ps1`/`stop-mentora.ps1` + READMEs brings up the full local stack with no undocumented step. | NOT STARTED |
| I2 | `seedDemoData` produces demo-quality data: no empty state, no broken thumbnail, no unplayable video, no lorem-ipsum on a reviewer-visible screen. | NOT STARTED |
| I3 | Android demo path verified against local backend via `10.0.2.2`; hardcoded-base-URL limitation re-stated or resolved. | NOT STARTED |
| I4 | Nine-step portfolio-priority flow demo-ready end-to-end, both languages, every in-scope client (H1). | NOT STARTED |
| I5 | iOS simulator run-verification (M16) recorded as ACCEPTED LIMITATION — Phase 5 deferred, never PASS. | NOT STARTED |
| I6 | Repository presents well to a portfolio reviewer: no stray artifacts, no dead files, READMEs accurate, `git status` clean. | NOT STARTED |

## J. Documentation Accuracy & Scope Discipline (M17 cross-check)

| # | Criterion | Status |
|---|---|---|
| J1 | No real payment code path exists anywhere — word-boundary scan re-run across backend/web/mobile source. | PASS (B2) — word-boundary scan (stripe/paypal/creditcard/credit_card/payment_intent/braintree/square.js/adyen, case-insensitive) across `backend/src`, `web/src`, `mobile/shared/src`, `mobile/androidApp/src` re-run 2026-09-22: zero real matches. Only false-positive substring hits: "stripe" inside a code comment about a visual "vertical stripe texture" (`mobile/androidApp/.../CourseArtwork.kt`), and "adyen" as a case-insensitive substring of "alreadyEnrolled" in enrollment DTOs/services (backend, web, mobile shared). Corroborated by the existing `mobile/shared/src/commonTest/.../NoPaymentVocabularyTest.kt`. Mirrors Phase 7 D2. |
| J2 | Every bullet of `ARCHITECTURE.md §5` re-verified against what was actually built. | PASS (B2) — re-verified 2026-09-22: (1) `design-system/`, `product/`, `ux/` confirmed untouched since the initial commit (`git log -- design-system/ product/ ux/` shows only `c3a2ed5 Initial Mentora project setup`) aside from this batch's own two pre-approved D-4/D-5 one-line corrections; (2)/(3) "did not write production code" / AI-provider-resolved-not-implemented bullets describe the scope of the architecture-authoring phase itself at time of writing (historical), not an ongoing claim about the repo — not a discrepancy; (4) `product/SCREEN_INVENTORY.md` still lists exactly 29 `### N.` screen headings; (5) no cloud-infra config found repo-wide (`*.tf`, `terraform*`, `*cloudformation*`, `k8s*`, `kubernetes*` — zero matches). All bullets still hold. |
| J3 | Stale docs corrected in `execution/`: D-1 (sequencing line), D-2 (Android test count), D-3 (decision count off-by-one). | FIXED (B2) — `execution/MASTER_IMPLEMENTATION_PLAN.md:79` corrected to "Phases 1-7 are COMPLETE; Phase 8 is IN_PROGRESS"; `mobile/androidApp/README.md:70` corrected to 246/246 (confirmed live via `./gradlew :androidApp:testDebugUnitTest`, 0 failures/errors); `execution/PHASE_HANDOFF.md:419` corrected "D146-D161"/"Sixteen" to "D146-D162"/"Seventeen" and D162 added to the enumeration (confirmed last decision in `DECISIONS_LOG.md` is D162). Commit: "Phase 8 B2: fix stale doc facts (J3)". |
| J4 | D-4 (`SCREEN_INVENTORY.md` stale "v1.2" reference, a locked doc) — flagged to user, one-line factual correction applied since it is non-design-affecting. | NOT STARTED |
| J5 | `CURRENT_STATUS.md`, `DECISIONS_LOG.md`, `PHASE_HANDOFF.md` updated with a complete Phase 8 record in the established 10-subsection format. | NOT STARTED |
| J6 | Clean git state at every checkpoint, one commit per task, pushed after each. | NOT STARTED |
| J7 | Every Phase 8 change traces to a row in this matrix — no new features, no redesign, no invented scope. | NOT STARTED |
| J8 | Closing statement of what the project deliberately did not do (final phase — no gap left looking like an oversight). | NOT STARTED |

---

## Known discrepancies carried into Phase 8 (from repository recovery, 2026-09-22)

| # | Discrepancy | Disposition |
|---|---|---|
| D-1 | `MASTER_IMPLEMENTATION_PLAN.md:79` stale sequencing line ("Phase 1 IN_PROGRESS; Phases 2-8 NOT_STARTED"). | Fix under J3 (writable `execution/` doc). |
| D-2 | `mobile/androidApp/README.md:70` documents 241/241 unit tests; actual is 246/246. | Fix under J3. |
| D-3 | `PHASE_HANDOFF.md:419` says "D146-D161"; D162 exists (the entry itself). | Fix under J3. |
| D-4 | `product/SCREEN_INVENTORY.md:3` says COMPONENTS.md "is now v1.2"; actual is v1.3.2. Locked doc. | RESOLVED (B2) — flagged under J4, one-line factual correction applied 2026-09-22 (confirmed `design-system/DESIGN_SYSTEM.md:1` = "v1.3.2"). Per-screen "(added DS v1.2)" historical markers left untouched — they correctly record when those 7 components were added, not the current doc version. Commit: "Phase 8 B2: fix stale COMPONENTS.md version reference (D-4)". |
| D-5 | `backend/build.gradle.kts` declares unused Testcontainers deps; `TESTING_STRATEGY.md §1` describes Testcontainers-based integration tests that don't exist (real tests hardcode local Mongo). | RESOLVED (B2) — confirmed zero `testcontainers` imports/usage anywhere in `backend/src`; removed the 3 dead `org.testcontainers:*` test dependency declarations from `backend/build.gradle.kts`; verified `./gradlew compileTestKotlin` still succeeds. Corrected `TESTING_STRATEGY.md §1`'s Backend Integration row (flagged, non-design-affecting factual correction) to describe the actual mechanism: a real local MongoDB instance at `mongodb://localhost:27017/?replicaSet=rs0` (confirmed in `HealthCheckIntegrationTest.kt` and siblings). Contributes to C8 (full C8 grading, including a fresh backend test run, deferred to B6). Commit: "Phase 8 B2: remove dead Testcontainers dep + correct locked-doc claim (D-5)". |
| D-6 | `TESTING_STRATEGY.md §5` mandates Web Vitest+RTL tier — never built. | Resolved under H2/C3 (bounded tier). |
| D-7 | M14's "lint/CI check for missing translation keys per platform" never built for Web/Android. | Resolved under B2/B3. |
| D-8 | M14/M16/M17 name iOS as in-scope. | Resolved under H1. |
| D-9 | No a11y/performance tooling exists. | Resolved under H4. |
| D-10 | **New defect found during B0 baseline (not previously documented):** `AdminRepository.listCourses` used MongoDB `$text` search combined with `_id`-ascending pagination and no relevance sort. `$text` OR-matches any query word across the corpus; once the catalog exceeds the 20-item default page limit, a genuinely-matching course whose `_id` sorts past page 1 was silently dropped from admin search results — reproduced live via Playwright (`10-admin-course-management.spec.ts`, both attempts failed identically), root-caused via direct API probing. **FIXED:** replaced `$text` with the same case-insensitive substring-regex pattern already used correctly by `userSearch` in the same file (deterministic match, compatible with `_id` pagination). Added a regression test (`AdminIntegrationTest`: "course search finds a title beyond the default page limit even when a common word is shared") reproducing the exact defect class. Backend suite 128/128 (127+1), E2E spec re-verified passing live. The public Explore `CourseRepository.listPublished` search has the identical `$text`+`_id`-sort pattern but was not reproduced as broken and intentionally also searches localized (EN/AR) metadata via the text index (`CoursesIndexes.kt`) — changing its ranking behavior risks that locked design intent, so it is **not touched** in this phase; recorded here as a disclosed, related, latent risk for any future dedicated work, not silently carried as if unknown. |

---

**Do not silently skip any row.** Every row above must be revisited before Phase 8 closes.
