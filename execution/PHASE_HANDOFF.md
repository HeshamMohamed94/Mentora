# Mentora — Phase Handoff Log

Each completed phase gets one dated section below, appended (never overwritten), in the fixed structure required by the implementation supervision rules: Phase status, what was implemented, files/modules created, API/contracts produced, database changes, tests/verification performed, known limitations, decisions made, what the next phase depends on, what the next phase must NOT redo, and the git commit/state reference.

---

## PHASE 1 — Backend Foundation & API

**Status:** COMPLETE (started 2026-09-05, completed 2026-09-06) — pending the user's explicit approval of the Phase 1 report before Phase 2 begins.

### 1. What was implemented

The full backend for Milestones M0–M2, M5–M13 (backend slices), M15, and M16 (backend portion) of `architecture/IMPLEMENTATION_ROADMAP.md`, per `execution/MASTER_IMPLEMENTATION_PLAN.md`'s Phase 1 scope. A Ktor/Kotlin backend with 13 feature modules plus shared infrastructure, running against a local MongoDB replica set, with a real (not stubbed) implementation of every backend behavior except the one deliberately-scoped exception (the AI Tutor's LLM call, explicitly deferred to Phase 6 per the user's own instruction — see D4).

Tasks 1–23 all complete: continuity docs, toolchain audit, Gradle scaffold, Ktor foundation, Auth & RBAC, Users, Courses & Categories, Enrollment/Demo Checkout, Progress, Quiz, Certificates, Learning Paths, Media, Instructor aggregation, Admin aggregation, AI Tutor scaffold, backend test-suite completeness review, seed/demo data script, local run instructions, this quality-gate pass, and this write-up.

### 2. Files/modules created

13 feature modules under `backend/src/main/kotlin/com/mentora/backend/`: `auth`, `users`, `categories`, `courses`, `enrollment`, `progress`, `quiz`, `certificates`, `learningpaths`, `media`, `instructor`, `admin`, `aitutor` — each with its own `repository`/`service`/`routes` package (plus a module-level Koin file), consistent with `architecture/BACKEND_ARCHITECTURE.md`'s module-boundary design. Shared infrastructure: `common` (error taxonomy, pagination, CSRF, principal/role helpers, response envelope), `config` (typed, fail-fast, env-driven `AppConfig`), `plugins` (CORS, security/JWT auth, rate limiting, request validation, status pages, monitoring, database lifecycle, health check). One top-level `SeedData.kt` entry point. 15 test files under `backend/src/test/kotlin/com/mentora/backend/` (12 integration suites, 3 pure-unit-test suites). `backend/README.md` and `backend/.env.example` (new this phase — local run instructions did not exist before task 21).

**Git:** 19 commits on `main` since the initial project-setup commit (`c3a2ed5`), touching 115 files under `backend/` (8,155 insertions), plus this `execution/` continuity layer. Full list: `git log --oneline c3a2ed5..HEAD`. Working tree is clean.

### 3. API/contracts produced

The full `architecture/API_CONTRACT.md` surface for every Phase 1 module: auth (register/login/logout/refresh), users (profile read/update), categories (CRUD), courses (browse/CRUD/sections/lessons/publish/unpublish), enrollment (checkout preview/complete, list), progress (get/complete-lesson/position), quiz (student view/editor/submit/latest), certificates (list/get), learning paths (list/get/follow/unfollow), media (upload/file/playback-url/stream), instructor dashboard, admin (dashboard/courses/users/instructors), AI Tutor (conversation/messages). The as-built shape of every one of these — including every place it differs in a small, deliberate way from the original architecture docs — is recorded in `execution/INTEGRATION_CONTRACT.md`, which is the authoritative as-built reference for Phase 2+ client work. Every deviation there is cross-referenced to the `DECISIONS_LOG.md` entry that explains why.

### 4. Database changes

MongoDB collections now live and indexed: `users`, `refreshTokens`, `categories`, `courses`, `enrollments`, `demoPurchases`, `progress`, `quizzes`, `quizAttempts`, `certificates`, `learningPaths`, `learningPathFollows`, `media`, `aiConversations`, `aiMessages` — matching `architecture/DATABASE_MODEL.md` exactly, with the one intentional, tracked exception (`media.ownerRefId` typed `String` not `ObjectId`, D25). `instructor` and `admin` own no collections by design (read-aggregation modules, D-precedent in D16/D28). The local MongoDB instance was converted from standalone to a single-node replica set (`rs0`) to support the real multi-document transactions this design requires (D12) — this is a one-time environment setup step, now documented in `backend/README.md`, not a schema change.

### 5. Tests/verification performed

**Automated:** 65 tests across 15 suites (12 integration — Ktor `testApplication` + live local MongoDB; 3 pure unit — JUnit5+MockK, no live Mongo), 0 failures, 0 errors, verified with a from-scratch clean build (`gradlew.bat clean` then `gradlew.bat test build --rerun-tasks`) as the final gate for this report — not a cached/incremental result. Every module's suite was independently re-verified by Claude at commit time (never trusting Codex's self-report alone); this pass additionally found and fixed one pre-existing flaky test (D31) and one genuine security gap (D33, below) that had escaped every prior review.

**Manual/live:** started the actual backend process and exercised it over real HTTP — `/healthz` returns `{"status":"ok","mongo":"ok"}`; logged in as a seeded demo account and received a valid access token with the correct role.

**Security-specific:** demo-checkout idempotency (real MongoDB transaction + unique-index fallback, proven via duplicate requests), the lesson-completion→progress→certificate transaction (both orderings), the quiz `isCorrect`-stripping projection (raw-JSON substring check, not just a null-field check), Auth & RBAC (reuse-detection breach path, timing-safe login, CSRF enforcement on every mutating route — including the one gap found and closed this session), a full grep-based sweep confirming zero payment vocabulary anywhere in the backend, zero hardcoded secrets, zero stray debug logging, `.env` never committed.

### 6. Known limitations

None of these block Phase 1's quality gate — each is a deliberate, documented, non-blocking simplification appropriate to this phase's scope, not an undetected defect:

1. **Testcontainers not used** (D5) — this dev machine has no Docker; integration tests run against the local native MongoDB service instead, with a disposable per-test-class database. A deviation from `TESTING_STRATEGY.md`'s stated *tooling*, not from its *intent* (real Mongo behavior is still exercised, not mocked).
2. **Course media reference fields are format-only validated** (D10) — `publish()` checks `thumbnailMediaId`/lesson `videoMediaId` are present and well-formed, not that a real, `ready` media document exists for that id. Still open even after the `media` module shipped (task 15 never added this cross-check). Low risk at this scale; if ever wanted, it's an additive `media`-service read call from `courses`, not a redesign.
3. **`isEnrolled` on course responses was deferred and never implemented** (D11) — a client currently determines enrollment by cross-referencing `GET /enrollments` itself rather than reading a flag directly on `GET /courses`/`GET /courses/{id}`. Worth a decision at Phase 2 UI-integration time (add the field, or keep the client-side join).
4. **A known, minor, non-blocking demo-checkout concurrency edge case** (D14) — a true simultaneous duplicate checkout request can surface a transient MongoDB transaction error that isn't auto-retried. The idempotency guarantee itself (never two enrollments) holds either way; the additive fix (retry once on `TransientTransactionError`) is documented but not implemented.
5. **Rate-limit caller-identity granularity** — `AUTH_SECURITY.md § 7`'s illustrative table describes `/auth/login`/`/auth/register` as "per-IP and per-email"; the actual implementation uses Ktor's `RateLimit` plugin with no custom `requestKey`, i.e. its default keying, not an explicitly-verified per-IP-and-per-email scheme. Zero public exposure exists at this phase (local-only MVP, `ADR-012`), so this carries no current risk — flagged here so it gets an explicit look before any future phase changes that exposure.
6. **AI Tutor is a real, functional boundary bound to a stub provider** (D4/D30, by explicit user instruction, not a defect) — auth, rate-limiting, persistence, and the enrollment gate are all genuinely live; the response content itself is fixed placeholder text until Phase 6 implements the real Anthropic Claude API call. No route/schema/contract change is expected then, only the `AiProvider` Koin binding.
7. **Seed script media is synthetic byte content**, not real video files (D34) — correct and sufficient for a backend-only phase with no client yet to play one; Phase 2/7's "Local Demo" readiness (real-looking thumbnails and at least one real demo video, per `DEPLOYMENT.md § 2`) is explicitly a later concern, not part of this phase's M16 slice.

### 7. Decisions made

34 implementation-time decisions recorded in `execution/DECISIONS_LOG.md` (D1–D34), covering local-environment facts (native MongoDB, no Docker, the replica-set conversion), every implementation-time gap-fill between an architecture doc's prose and what the existing codebase could actually do (D9-D11, D21, D25, D28, D30, D34), every security-relevant finding and fix (D27's JWT-claim bug, D31's flaky test, D33's CSRF gap), and every Codex-dispatch process note (D3, D27, D29). None reopens or contradicts a locked architecture decision — every one is either a local-environment fact or an implementation-time detail the architecture correctly left unspecified at the ADR level.

### 8. What Phase 2 (and later phases) depend on

- `execution/INTEGRATION_CONTRACT.md` is the authoritative as-built API reference — read it before `architecture/API_CONTRACT.md` where the two differ (every difference is explained there and cross-referenced to a decision).
- The backend runs locally per `backend/README.md` — MongoDB replica-set conversion, `.env` setup, `seedDemoData` for realistic content, all four demo-account roles ready to authenticate against from Phase 2's Web client.
- `CourseService.requireOwnership`/`EnrollmentService.requireEnrollment` are the established cross-module read-reuse pattern (used by `media`, `quiz`, `aitutor`) — Phase 2 client work doesn't need this, but any *further backend* work should follow the same reuse pattern rather than a new direct collection query.
- The `AiProvider` interface (D4/ADR-009) is ready for Phase 6 to bind a real implementation with zero route/schema change.

### 9. What later phases must NOT redo

- Do not rebuild any of the 13 backend modules, their routes, or their persistence — they are complete, tested, and reviewed. Phase 2 consumes them as-is.
- Do not re-litigate the AI Tutor's Phase 1 stub — Phase 6 only swaps the Koin binding.
- Do not reintroduce a real-payment code path anywhere — explicitly, permanently out of scope (verified zero payment vocabulary this session, per `product/DEMO_PAYMENT_FLOW.md`).
- Do not build `infra/docker/mongo-init` reflexively because `REPOSITORY_STRUCTURE.md` mentions it — Phase 1's `seedDemoData` Gradle task is the working equivalent for as long as Docker isn't otherwise needed (D34); revisit only if/when Docker genuinely enters the stack (e.g. CI Testcontainers).

### 10. Git commit/state reference

`main` branch, working tree clean. Phase 1 spans commits `18040a0`..`bad0d06` (19 commits; `git log --oneline c3a2ed5..HEAD` for the full list with messages). The design-system/product/ux/architecture directories were never modified during Phase 1 (verified via `git log --name-only` over the full commit range) — every locked doc is exactly as it was at Phase 1's start, consulted and adapted-to, never edited. No `web/`/`mobile/`/`infra/` directory exists — Phase 2+ has not been started.

---

## INTERIM NOTE (2026-09-07) — Mentora Design-to-Code Source-of-Truth Pipeline

**This is NOT a Phase 2 completion entry and not a numbered Phase 2 product task** — Phase 2 (Website) remains `IN_PROGRESS` through Task 11 (Instructor Web), Task 12 (Admin Web) has explicitly **not** started, and this note does not supersede or complete Phase 2's own eventual handoff section (which will be written when Phase 2 itself is done). It is recorded here only because it is a durable, cross-cutting artifact (not scoped to one task) that future phases — especially any future Android/iOS client work — need to know exists.

Built between Task 11 and Task 12, per explicit user instruction: a structured, machine-readable `design-to-code/` directory normalizing the locked Design System v1.3.2, the locked Mentora Showcase, and the locked Product/UX specs into a platform-neutral source (tokens, component recipes, navigation/shell rules, the governed course-artwork system, 24 screen specs, 6 reusable layout patterns) — intended to reduce future reliance on developers visually approximating screenshots, and to make a future Android/iOS effort start from data rather than re-deriving everything from `design-system/*.md` prose a second time. No locked document (`design-system/`, `product/`, `ux/`, `architecture/`) was modified — every value in `design-to-code/` traces back to one of them via `design-to-code/SOURCE_MANIFEST.json`'s precedence rule. See `execution/DECISIONS_LOG.md` D50 for the full account, `design-to-code/README.md` for the directory map, and `design-to-code/validation/*.md` for extraction/coverage/mapping detail. Task 12 (Admin Web) still has not started as of this note.

---

## PHASE 2 — Website

**Status:** COMPLETE (started 2026-09-06, completed 2026-09-11) — pending the user's explicit approval of the Phase 2 report before Phase 3 begins.

### 1. What was implemented

The full Next.js 15 App Router website for Milestones M3–M4, M6–M16 (web slices) of `architecture/IMPLEMENTATION_ROADMAP.md`, consuming the Phase 1 backend over real HTTP with zero mocking anywhere in the app. Every screen in `product/SCREEN_INVENTORY.md`'s Phase 2 scope is implemented and live against real seeded data, in both `en` (LTR) and `ar` (RTL), Light and Dark mode: public discovery (Landing, Explore, Course Details, Learning Paths), auth (Login/Register), the full Student surface (Dashboard, My Learning, Course Player, Quiz, Quiz Results, Certificates List/Detail, AI Tutor chat, Profile, Settings), the full Instructor surface (Dashboard, Course Editor, Lesson Editor, Quiz Editor), and the full Admin surface (Dashboard, Manage Courses/Users/Instructors/Categories).

Tasks 1–17 all complete: foundation/tokens/i18n/auth-plumbing, auth screens, public discovery, demo checkout, Student Dashboard/My Learning, Course Player/Quiz/Quiz Results, Certificates, Learning Paths follow/unfollow (already complete at task 3), AI Tutor chat UI, Profile/Settings, Instructor Web, Admin Web, localization completion pass, Playwright E2E suite, `web/README.md`, this quality-gate pass, and this write-up. Plus four dedicated, explicitly user-requested visual-fidelity correction passes (D45/D46/D48/D49/D52/D53) and one cross-cutting source-of-truth pipeline (D50, `design-to-code/`) sitting between Task 11 and Task 12, all recorded above/in `DECISIONS_LOG.md`.

### 2. Files/modules created

`web/` — a full Next.js 15 App Router project: `src/app/[locale]/...` (thin route wrappers), `src/components/screens/` (one component per `SCREEN_INVENTORY.md` entry, shared across public/Student/Instructor/Admin route trees per the "same screens, not duplicate screens" IA rule), `src/components/ui/` (design-system component kit — Button, TextField, Select, Toggle, Tabs, FileUpload, DataTable, AppDialog, Avatar, and more), `src/components/navigation/` (AppShell/InstructorShell/AdminShell, parameterized nav), `src/lib/api/` (one typed module per backend resource, TanStack Query hooks), `src/lib/auth/`, `src/lib/design-to-code.generated.ts` (generated, D50), `messages/{en,ar}.json` (491/491 key parity), `e2e/` (10 Playwright spec files + `helpers.ts` + `playwright.config.ts`, D64), `web/README.md` + `web/.env`-equivalent config. `design-to-code/screens/admin-*.json` — 5 new screen specs added during Task 12 (screen count 24→29). Two repo-root scripts, `start-mentora.ps1`/`stop-mentora.ps1`, for one-command local dev-stack management.

**Git:** 33 commits on `main` from Phase 2 kickoff (`a35c80e`) through this write-up's parent commit, touching 220 files (23,770 insertions) under `web/`, `design-to-code/`, and `execution/`. Full list: `git log --oneline a35c80e^..HEAD`. Working tree is clean.

### 3. API/contracts produced

No new backend routes — Phase 2 is a pure consumer of the Phase 1 `architecture/API_CONTRACT.md`/`execution/INTEGRATION_CONTRACT.md` surface. Two small, approved backend changes were made *in service of* the web client, both additive/corrective, never a new contract: `instructorName` denormalized onto `CourseSummary`/`CourseResponse` (D37, avoids an N+1 client-side join) and a real backend defect fix in `CourseService.get()` — an already-enrolled student was losing Course Player access the moment their course was unpublished, contradicting `ux/INSTRUCTOR_ADMIN_UX.md`'s explicit "enrolled students keep access" rule; fixed by injecting `EnrollmentRepository` and allowing the draft-course fetch when the caller is enrolled (D64). Both are documented in `INTEGRATION_CONTRACT.md`/`DECISIONS_LOG.md` and covered by both integration and E2E tests.

### 4. Database changes

No schema changes. Seed data content was upgraded in place, still via the existing `SeedData.kt`/`seedDemoData` pipeline (no new collection, no new field beyond what D37/D64 already cover): every published seed course's first lesson now has a real, playable ffmpeg-encoded MP4 (D59/D60, one topic-relevant video per course, a genuinely Arabic video for the Arabic-content course) instead of synthetic placeholder bytes, and the 4 real published courses now have real topic-specific thumbnail images (D61) instead of the generic gradient motif colliding across categories. Per-locale course title/description fields were added and populated (D57) to close a language-leak found on the Dashboard (D56).

### 5. Tests/verification performed

**Automated, backend:** 77 tests / 17 suites / 0 failures / 0 errors, re-verified from a clean, non-cached state as this phase's final gate (`gradlew.bat clean test --rerun-tasks`), not a cached/incremental or self-reported result.

**Automated, frontend:** `typecheck` clean; `lint` clean (one pre-existing, expected `<img>` warning on `course-thumbnail.tsx`); `lint:logical-properties` clean (zero physical-direction CSS anywhere under `src/` — this app is RTL-first by construction); `validate:design-to-code` clean (29 screens/6 patterns/11 shared files); a stopped-dev-server production `build` clean (47 routes, both locales, 0 errors).

**Automated, end-to-end:** `web/e2e/`'s 10 Playwright specs (one per `architecture/TESTING_STRATEGY.md § 6` priority flow), run against the real local backend + MongoDB, never mocked. **Chromium: 19/19 passing. Firefox: 19/19 passing.** WebKit: 1/19 — a real, investigated, non-product `Secure`/`SameSite` cookie-policy incompatibility with local plain-HTTP `localhost` (see Known limitations). A genuine, locked-spec-violating backend defect was found and fixed while building this suite (§3 above), covered by both a new integration-test assertion and a dedicated E2E test that proves the fix end-to-end through a real browser.

**Manual/live, every task:** every one of Tasks 1–15 was independently verified live in a real browser against the real seeded backend, in both `en`/LTR and `ar`/RTL, before being marked DONE — not just gate-script output. Four dedicated visual-fidelity passes (D45/D46/D48/D49/D52/D53) additionally scored implementation against the locked `design-review-locked/Mentora Showcase.dc.html` screen-by-screen and corrected the highest-priority gaps, with every remaining gap explicitly disclosed rather than silently left (see Known limitations).

### 6. Known limitations

None of these block Phase 2's quality gate — each is a deliberate, disclosed, non-blocking gap or a real environment constraint investigated to its root cause, not an undetected defect (full detail at the cross-referenced decision):

1. **WebKit E2E: 1/19 passing** (D64) — a real `Secure`/`SameSite` cookie-policy difference between this app's correct production cookie config (`secure=true`/`SameSite=Lax`) and WebKit's stricter handling of that config over plain-HTTP `localhost`; Chromium/Firefox's well-known "localhost is trustworthy" relaxation doesn't apply the same way in WebKit. Not fixed — would require local HTTPS or a real CI TLS-terminating proxy, neither in scope; not a defect in the app's security-correct cookie config, which should not be weakened to accommodate this.
2. ~~Seed course lesson-2 videos are still fake placeholder bytes~~ — **RESOLVED** by the PRE-PHASE-3 seed data expansion (D67, 2026-09-11): every lesson in every published seed course now uploads a real, playable demo video; no seeded lesson opens into "This video can't be played" anymore. Kept here struck through rather than deleted so the original Phase 2 gap and its resolution are both traceable.
3. **Mobile drawer breakpoint behavior verified by code review only, not live** (D40) — the browser-automation environment's window resize doesn't change the rendered viewport, so the Sidebar's off-canvas behavior on narrow viewports is unverified live.
4. **`DataTable` breakpoint spec conflict** (D62) — `design-system/COMPONENTS.md` (collapses below `desktop`) vs. `design-tokens.json` (collapses below `tablet`) disagreed; resolved in favor of the more detailed component-specific prose, disclosed non-blocking.
5. **Course Editor IA conflict left unresolved** (D48) — showcase shows a persistent rail, locked spec implies two tabs; still built as tabs, deliberately not forced toward the showcase past the locked spec's own authority.
6. **Instructor Dashboard / Course Editor capped visual-fidelity scores** (Instructor Dashboard ≈78%, Course Editor ≈66–72%, D48/D49) — both capped by the same disclosed showcase-vs-locked-spec conflicts (4th stat card/table columns, Media tab/persistent rail) intentionally left unresolved rather than chased past the locked spec.
7. **Admin Course Management: two deliberate showcase deviations** (D62) — a 5-item nav (incl. Instructors) over the showcase's 4-item version; a read-only status Badge + one-way Unpublish over the showcase's bidirectional Toggle, since no admin-capable publish endpoint exists.
8. **No password-change endpoint; `avatarMediaId` is a dead field** (D44) — both real spec-vs-backend gaps in Profile/Settings, scoped out rather than built against a nonexistent contract.
9. **AI Tutor has no docked-panel variant** — only the full-screen `/app/ai-tutor` chat screen exists; carried forward unchanged since Task 9.
10. **Purchase Success renders with `shell:"none"`** (D52) — a deliberate, disclosed layout choice, not a bug.
11. **True-concurrency double-completion edge case in demo checkout** (Phase 1, D14) — a documented, non-blocking theoretical race under true concurrent duplicate requests; carried forward unchanged, out of Phase 2's scope to revisit.
12. **Landing page's `CourseCard`/`LearningPathCard` usage hardcodes the Guest `basePath`** (D39) — low priority since Landing is conceptually Guest-only; unchanged since Task 3/4.
13. **Icon set is a hand-drawn inline-SVG placeholder** (D40) — for the real self-hosted Material Symbols Rounded font; swap later behind the same `Icon` component API, no call-site changes needed.

### 7. Decisions made

32 implementation-time decisions recorded in `execution/DECISIONS_LOG.md` (D35–D66), spanning: the token/design-to-code pipelines (D35, D50); every UI-vs-backend gap-fill (D36, D38, D42, D44); four dedicated visual-fidelity correction passes against the locked showcase (D45/D46/D48/D49/D52/D53); the Student Dashboard acceptance-criteria and localized-metadata slice (D54–D61); Admin Web and its showcase-mockup discovery (D62); the localization/RTL completion audit (D63); the Playwright E2E suite, including a genuine backend defect found and fixed while building it (D64); `web/README.md` (D65); and this phase's own quality-gate verification (D66). None reopens or contradicts a locked architecture/product/UX/design-system decision — every one is either a real implementation-time gap the locked docs correctly left for implementation to resolve, or a disclosed, non-blocking limitation.

### 8. What Phase 3 (and later phases) depend on

- `design-to-code/` (D50, extended to 29 screens by D62) is the platform-neutral source Phase 3+ (KMP shared core, then Android/Phase 4 and iOS/Phase 5) should read from first, rather than re-deriving screen/component specs from `design-system/*.md` prose a second time.
- `execution/INTEGRATION_CONTRACT.md` remains the authoritative as-built API reference, now proven correct against a real, full client (this website) rather than only against Phase 1's own tests.
- The full local dev stack (`start-mentora.ps1`/`stop-mentora.ps1`, `web/README.md`, `backend/README.md`) is ready for any future phase's own local development needs.
- The `web/e2e/` Playwright suite and its `helpers.ts` (real registration/login/enrollment/course-authoring flows against a live backend) is a reusable reference for how to drive this backend for real from a test — useful pattern precedent for any future mobile-side integration testing, even though KMP/Android/iOS will need their own tooling.

### 9. What later phases must NOT redo

- Do not rebuild any Phase 2 web screen, component, or shell — the full Student/Instructor/Admin/public surface is complete, live-verified, and E2E-tested. Phase 3+ consumes the backend Phase 2 already proved out; it does not touch `web/`.
- Do not re-attempt to "fix" the WebKit cookie issue by weakening the backend's `Secure`/`SameSite` cookie configuration — that would be a real security regression for a real HTTPS deployment. If WebKit coverage is ever wanted, the fix is local HTTPS or a CI TLS-terminating proxy, not a cookie-policy change.
- Do not re-litigate the Course Editor IA conflict (persistent rail vs. tabs) or the Instructor Dashboard/Course Editor showcase-vs-locked-spec gaps — both were investigated twice (D48, D49) and deliberately left as-is in favor of the locked spec's own authority over the showcase.
- Do not treat the mobile-drawer live-verification gap as a new discovery — already disclosed, known, and low-priority; re-investigating it from scratch would be duplicate work. (The lesson-2 placeholder-video gap this bullet used to also name was resolved by the PRE-PHASE-3 seed data expansion, D67 — see `CURRENT_STATUS.md`.)

### 10. Git commit/state reference

`main` branch, working tree clean. Phase 2 spans commits `a35c80e`..`7748204` (33 commits; `git log --oneline a35c80e^..HEAD` for the full list with messages, includes the one pre-kickoff approved backend commit `4d33fcc`). The `design-system/`, `product/`, `ux/`, `architecture/`, and `design-review-locked/` directories were never modified during Phase 2 (verified via `git log --name-only a35c80e^..HEAD` over the full commit range) — every locked doc is exactly as it was at Phase 2's start, consulted and adapted-to, never edited. No `mobile/`/`infra/` directory exists — Phase 3+ has not been started.

---

## PHASE 3 — KMP Shared Mobile Core

**Status:** COMPLETE (started 2026-09-12, completed 2026-09-12) — pending the user's explicit approval of the Phase 3 report before Phase 4 begins.

### 1. What was implemented

The full `mobile/shared` Kotlin Multiplatform module per `execution/MASTER_IMPLEMENTATION_PLAN.md`'s M4 slice and `execution/PHASE_3_KMP_PLAN.md`'s 17-task acceptance criteria: domain models, use cases, Ktor Client networking, auth/session orchestration with single-flight token refresh, the secure-storage `TokenStorage`/non-secret `PreferenceStore` boundary, and a platform-agnostic lesson-playback interface — covering every one of the 10 student-facing domains (auth, user, catalog, enrollment, progress, quiz, certificates, learning paths, media, AI Tutor). No Android/iOS app shell and no UI of any kind — that is Phase 4/5's job entirely; `shared` has zero Compose/SwiftUI/`android.*` UI dependency anywhere, enforced by a real file-scan test, not just convention.

Tasks 1–17 all complete: Gradle/KMP scaffold, wire-contract primitives (envelope/error taxonomy/`ApiResult`/`CursorPage`), environment config + Ktor client, the `TokenStorage`/`PreferenceStore` boundary, `SessionManager`/auth interception/auth domain, user profile + locale sync, catalog domain, enrollment/demo checkout, progress, quiz, certificates, learning paths, media/playback contract, AI Tutor (paged conversation + streaming + quick actions), the public `MentoraSdk` façade + Koin wiring + host-guarded iOS/SKIE config, live integration verification against the real running backend, and this documentation/handoff pass.

### 2. Files/modules created

`mobile/` — a new, independent Gradle KMP root (own wrapper, `settings.gradle.kts` including only `:shared`), sibling to `backend/`/`web/`, never a composite build with either. `mobile/shared/src/commonMain/kotlin/com/mentora/shared/` — `config/` (`ApiEnvironment`), `auth/` (`SessionManager`, `AuthState`, `TokenStorage`, `AuthPlugin`), `settings/` (`PreferenceStore`, `AppLocale`, `ThemePreference`, `resolveInitialLocale`), `domain/model/` (~30 pure data types), `domain/validation/`, `domain/usecase/` (one package per domain, ~35 use cases total), `data/network/` (`ApiClient`, `HttpClientFactory`, envelope/error DTOs, `CursorPage`, `localeQueryParam`, per-feature `dto/`), `data/repository/` (interface + `internal` Impl per domain), `playback/` (`LessonPlaybackController` — interface only), `di/` (`initKoin`, one Koin module per domain), `facade/` (10 façade classes), `MentoraSdk.kt`. `mobile/shared/src/androidMain/` (`AndroidTokenStorage`, `AndroidPreferenceStore`, OkHttp engine `actual`, `platformModule(context)`), `mobile/shared/src/iosMain/` (`IosTokenStorage`, `IosPreferenceStore`, Darwin engine `actual`, `platformModule()` — written but not wired into the build on this Windows host, see § 6). `mobile/shared/src/commonTest/` (MockEngine + fakes) and `mobile/shared/src/androidUnitTest/` (plain-JVM tests, architecture-boundary reflection tests, `LiveBackendIntegrationTest`). This write-up also adds `mobile/README.md`, `mobile/shared/README.md`, and the Phase 3 additions to `execution/INTEGRATION_CONTRACT.md` § 12 and this file.

**Git:** 32 commits on `main` for Phase 3 Tasks 1–16 (task commits interleaved with per-task `CURRENT_STATUS.md` continuity commits), spanning `5c2e739`..`229f8ca` (`git log --oneline 5c2e739^..229f8ca` for the full list with messages) — this Task 17 documentation pass itself is not yet committed as of this write-up.

### 3. API/contracts produced

**No new backend routes and no backend changes of any kind** — Phase 3 is a pure client-side consumer of the Phase 1/2 backend, per the standing "Phase 3 makes zero backend changes" rule. `execution/INTEGRATION_CONTRACT.md` § 12 (new, additive-only) records every mobile-specific as-built wire-contract fact discovered while building `shared` against the real backend source: the literal CSRF header value (`X-Requested-With: mentora-web`) sent on every mutating request, the deliberate no-cookie-jar design decision, `429`'s missing JSON envelope (synthesized client-side by request path), the real `AUTH_TOKEN_INVALID`/`AUTH_TOKEN_EXPIRED` contract drift (D77 — both codes now trigger a client-side refresh attempt, a client adaptation, not a backend defect), `?language=`'s dual metadata-resolver/result-filter role and the exact 4 read endpoints that carry it, the unpaginated `categories`/`learning-paths` list endpoints, AI Tutor's non-standard `{conversationId, messages[], nextCursor?}` envelope plus its raw chunked-`text/plain` streaming send, the permanently-absent lesson `duration` field, certificate ids as fully opaque public strings, and the media playback token-in-URL pattern. Every fact there is cross-referenced to the `DECISIONS_LOG.md` entry (D69–D77) that found it.

### 4. Database changes

**None.** Phase 3 made zero backend or database changes of any kind — no new collection, no new field, no schema change, no seed-data change. `mobile/shared` reads and writes exclusively through the existing Phase 1/2 REST API; it has no database access, direct or indirect.

### 5. Tests/verification performed

**Automated:** `:shared:testDebugUnitTest` — 249/249 tests green, 100% offline/deterministic (`commonTest`'s Ktor `MockEngine` + fakes, plus the rest of `androidUnitTest`'s plain-JVM/reflection/architecture-boundary tests) — no backend, no MongoDB, no network access required. `:shared:assembleDebug` clean throughout every task, independently re-verified (not just trusted from a single implementer pass) at every checkpoint, including a from-scratch `clean` build at Task 15. `:shared:liveBackendIntegrationTest` (Task 16) — a dedicated, isolated Gradle `Test` task (excluded from the default suite, see § 6) drove the real `MentoraSdk` façade through the full student journey (register→login→categories→search→course detail→checkout preview→demo checkout, first + idempotent repeat→progress→all lessons complete→playback url→quiz→certificate (verified live that issuance requires BOTH lessons-complete AND quiz-passed)→learning-path follow→AI Tutor stream→`?language=ar` content-difference assertion→forced token refresh→logout) against the real local backend + seeded MongoDB — 4/4 clean isolated runs, all `@kmp.mentora.test` test data cleaned up afterward, every pre-existing account independently verified untouched.

**Manual/review:** every task was independently re-reviewed against the real backend source (not the plan's paraphrase) before being marked DONE — this project's standing discipline throughout every phase. This review process caught and fixed two real defects before they could reach Phase 4/5: a fabricated placeholder `SessionUser` on cold-start restore (D71, Task 5) and a locale-precedence mechanism that was built/tested in isolation but never actually wired into a real login/register call (D72, Task 6). It also surfaced the one genuine, non-blocking backend/shared contract drift recorded in § 3 above (D77, Task 16).

### 6. Known limitations

None of these block Phase 3's quality gate — each is a deliberate, disclosed, non-blocking gap or a real environment constraint, not an undetected defect:

1. **iOS targets configure but were never compiled, linked, or SKIE-verified on this machine** (D69, disclosed from Phase 3 kickoff, unchanged throughout) — this is a Windows development machine; Kotlin/Native `iosArm64`/`iosSimulatorArm64` and the SKIE Gradle plugin both require a macOS host (Xcode toolchain). `iosMain`'s `actual` implementations (`IosTokenStorage`/Keychain, `IosPreferenceStore`/`NSUserDefaults`, the Darwin Ktor engine) exist on disk, written to the same `expect`/`actual`-shaped boundary as their Android counterparts, but the `iosMain` source set itself is not wired into `shared/build.gradle.kts` here (Kotlin never creates that source-set object while the iOS targets are disabled via `kotlin.native.ignoreDisabledTargets=true`). SKIE is applied host-guarded (`if (isMacOs)`). **Genuinely untested until a macOS host is available — this is Phase 5's job**, not a Phase 3 defect.
2. **`androidMain` actuals are tested only as plain JVM `androidUnitTest` cases with fake test doubles substituted at the DI boundary** — the REAL AES-256-GCM/Android-Keystore code path (`AndroidTokenStorage`) has never run against a real Android Keystore provider; no Robolectric is configured in this project. This needs a real emulator/instrumented test — **Phase 4's job**, not done here. (`AndroidTokenStorage`'s crypto logic was reviewed line-by-line at Task 4/D70 — IV handling, GCM tag length, decrypt-failure behavior all confirmed correct by code review — but line-by-line review is not a substitute for a real Keystore-backed instrumented run.)
3. **No offline cache/local persistence of catalog/course data** — every read goes to the network; there is no SQLDelight or any other local relational cache anywhere in `shared` (confirmed out of scope from `product/MVP_SCOPE.md` at kickoff, D69). Phase 4/5 may choose to add their own cache layer on top of `shared`'s repositories; `shared` itself provides none.
4. **The `AUTH_TOKEN_INVALID`/`AUTH_TOKEN_EXPIRED` mobile-side workaround** (D77) — `mobile/shared`'s `AuthPlugin` treats both error codes as refresh triggers, since the real backend's JWT `challenge` handler only ever emits `AUTH_TOKEN_INVALID` for a bad access token in practice. See `execution/INTEGRATION_CONTRACT.md` § 12 item 4 for the full cross-reference — this is a documented client-side adaptation, not something Phase 4/5 needs to re-solve or re-investigate.
5. **`LiveBackendIntegrationTest` lives in its own isolated Gradle task** (`:shared:liveBackendIntegrationTest`), separate from the default `:shared:testDebugUnitTest`, and needs a real running local backend to do anything beyond skip cleanly — it is not part of the fast, CI-equivalent quality gate. Found necessary after a cross-suite flake (D77) that never reproduced in isolation; excluding it from the default suite is also the architecturally correct split per this project's own "prefer not requiring live backend for every unit test" standard.
6. **`GetMyLearningUseCase` fails the whole call on the first per-course composition error**, rather than silently dropping a row (D74, deliberate) — Phase 4/5's "My Learning" UI must handle this as a genuine, retriable error state, not assume a partial list is always safe to render.
7. **`ReportPlaybackPositionUseCase`'s throttle state is per-`MentoraSdk`-instance** (D76) — correct only as long as Phase 4/5 resolve it exactly once (through the façade) and reuse that same `MentoraSdk` instance for the app's lifetime; bypassing the façade to resolve it fresh from Koin per call would silently reset throttling every time.

### 7. Decisions made

9 implementation-time decisions recorded in `execution/DECISIONS_LOG.md` (D69–D77): the Phase 3 kickoff plan and every backend-source-derived conflict resolution (D69); the Android secure-storage mechanism choice and the plain-interface (not literal `expect`/`actual`) modeling for `TokenStorage`/`PreferenceStore` (D70); the highest-consequence task's single-flight refresh design and a fabricated-placeholder-profile fix caught during review (D71); the locale-precedence wiring gap found and fixed during review (D72); the `localeQueryParam()` placement and enum `@SerialName` requirement (D73); the fail-fast-not-silently-partial `GetMyLearningUseCase` composition choice (D74); the AI quick-action context-requirement split and the empirically-verified Ktor `ByteChannel` stream-failure-detection behavior (D75); the non-global Koin application, `internal` repository visibility, and per-instance-throttle caveat (D76); and the real `AUTH_TOKEN_INVALID`/`AUTH_TOKEN_EXPIRED` contract drift plus the isolated live-test Gradle task (D77). None reopens or contradicts a locked architecture/product/UX decision — every one is either a real implementation-time gap the locked docs correctly left for implementation to resolve, a from-real-backend-source correction of a plausible-but-wrong assumption, or a disclosed, non-blocking environmental limitation. `execution/DECISIONS_LOG.md` needed no new entry for this documentation task itself — D69–D77 already fully cover Phase 3.

### 8. What Phase 4 (and later phases) depend on

- `mobile/shared`'s public `MentoraSdk` façade (`MentoraSdk.create(environment, platformModule, enableNetworkLogging)`) is the **only** sanctioned way to consume this module — never a repository, `ApiClient`, or `HttpClient` directly (structurally enforced: every repository `Impl` is `internal`). See `mobile/shared/README.md` for the full consumption guide and the 10 façade domains.
- Phase 4 (Android) must supply its own `platformModule(context: Context)` (already defined in `androidMain`, ready to call as-is) and its own `LessonPlaybackController` implementation backed by ExoPlayer/Media3 — `shared` provides the interface only, no implementation.
- `execution/INTEGRATION_CONTRACT.md` § 12 is the authoritative as-built mobile wire-contract reference — read it before writing any Android networking code that might seem to duplicate what `shared` already handles.
- The full local dev stack (`backend/README.md`, `start-mentora.ps1`/`stop-mentora.ps1`) remains ready for Phase 4's own local development and live-integration-testing needs.
- `design-to-code/` (D50, extended to 29 screens by D62) remains the platform-neutral visual/structural source Phase 4 should read from for Android UI, rather than re-deriving screen/component specs from `design-system/*.md` prose a second time.

### 9. What later phases must NOT redo

- Do not rebuild any of the 10 domains' repositories, use cases, or DTOs in `mobile/shared` — they are complete, tested, and reviewed. Phase 4/5 consume them as-is through `MentoraSdk`.
- Do not re-derive the auth/session/token-refresh strategy, the CSRF header value, the no-cookie-jar rule, or the `?language=` threading mechanism — all already built into `shared` and documented in `execution/INTEGRATION_CONTRACT.md` § 12; call the existing `localeQueryParam()`/`AuthPlugin`/`HttpClientFactory` mechanisms, don't reinvent them at the Android/iOS app layer.
- Do not re-litigate the `AUTH_TOKEN_INVALID`/`AUTH_TOKEN_EXPIRED` drift as a bug to fix in the backend — it is a documented, correct client-side adaptation (D77).
- Do not attempt to wire the `iosMain` source set back into `shared/build.gradle.kts` on a non-macOS host — it cannot be made to work here; this is genuinely Phase 5's task, on a real Mac.
- Do not build a local SQLDelight/offline cache reflexively — `shared` deliberately ships none (§ 6 item 3); only add one if Phase 4/5 has a real, specific product need for it.

### 10. Git commit/state reference

`main` branch. Phase 3 Tasks 1–16 span commits `5c2e739`..`229f8ca` (32 commits; `git log --oneline 5c2e739^..229f8ca` for the full list with messages); this Task 17 documentation write-up (`mobile/README.md`, `mobile/shared/README.md`, `INTEGRATION_CONTRACT.md` § 12, this entry, `CURRENT_STATUS.md`'s final Phase 3 update) is staged on top, not yet committed as of this write-up — the user reviews and commits it. The `architecture/`, `product/`, `ux/`, `design-system/`, `design-review-locked/`, `backend/`, and `web/` directories were never modified by any Phase 3 Task 1–16 commit (verified via `git log --oneline --name-only 5c2e739^..229f8ca` over the full commit range — every touched path falls under `mobile/` or `execution/` only) — every locked doc, and both prior phases' shipped code, are exactly as they were at Phase 3's start. No `infra/` directory exists — Phase 4+ has not been started.

---

## PHASE 4 — Android

**Status:** COMPLETE (started 2026-09-13, completed 2026-09-15) — pending the user's explicit approval of this Phase 4 report before Phase 5 begins. One mid-phase interruption occurred and was fully recovered with zero work lost or redone — see § 5/§ 7 and `execution/DECISIONS_LOG.md` D94 for the complete account.

### 1. What was implemented

The full native Android app (`:androidApp`, Jetpack Compose + Material3), consuming the Phase 3 `mobile/shared` KMP module exclusively through its public `MentoraSdk` façade — never a repository, `ApiClient`, or `HttpClient` directly. All 18 screens in `product/SCREEN_INVENTORY.md`'s Android/mobile scope are implemented and live-verified against the real Phase 1 backend, in both `en` (LTR) and `ar` (RTL), Light and Dark theme, at both the default and a 130% system font scale: the 5-tab authenticated shell (Home, Explore, My Learning, AI Tutor, Profile) with a fully independent per-tab back-stack and a real guest/auth gate; Login/Register; Explore (+ Learning Paths segment); Course Details; Demo Checkout/Purchase Success; Course Player (a real `LessonPlaybackController` backed by ExoPlayer/Media3, this phase's own contribution — `shared` provides only the interface) + Curriculum Bottom Sheet; Quiz/Quiz Results; Certificates List/Detail; Learning Path Details (follow/unfollow); AI Tutor (real streaming chat against the still-genuinely-stubbed backend `StubAiProvider`); Profile/Settings (the first genuinely functional in-app language switch this phase shipped, Task 18/D93); and a full localization/RTL/theme/font-scale QA sweep + Compose UI test-suite completion pass (Task 19).

Tasks 1–20 all complete: module scaffold, token pipeline + `MentoraTheme`, locked-showcase visual-reference extraction, app bootstrap/session-restore/Keystore instrumented test, two component kits (atoms; cards/state-patterns/sheets), the 5-tab navigation shell + auth gate, Auth screens, Explore, Course Details, Demo Checkout/Purchase Success, Home/My Learning, `LessonPlaybackController`+Course Player+Curriculum Sheet, Quiz/Quiz Results, Certificates, Learning Path Details, AI Tutor, Profile/Settings, the localization/RTL/theme/font-scale QA sweep, and this live-verification/README/handoff pass.

### 2. Files/modules created

`mobile/androidApp/` — a new AGP `application` module (`com.mentora.android`, `compileSdk`/`targetSdk` 36, `minSdk` 26 matching `:shared`'s androidTarget), Compose wired via the K2 `org.jetbrains.kotlin.plugin.compose` plugin, Compose BOM 2024.10.01. `src/main/kotlin/com/mentora/android/`: `MainActivity.kt` (entry point, wraps content in `LocalizedContent`); `navigation/` (`MentoraNavHost`, `Destinations` — an 18-screen `@Serializable` sealed route model, `AuthGate`); `locale/` (`LocalizedContent`, `LocaleController`, `StringsParityTest` source, the Task 19 locale-reload sweep); `theme/` (`MentoraTheme`, generated `MentoraTokens.kt`, `MentoraDimens`); `ui/components/` (the two component kits — ~15 atoms + ~15 cards/state-patterns/sheets, ~42 hand-ported icon `ImageVector`s); one `ui/<screen>/` package per screen (Screen + ViewModel pair, 18 screens); `playback/` (`MediaPlaybackController`, ExoPlayer/Media3). `src/main/res/values{,-ar}/strings.xml` (kept in exact key + format-specifier parity throughout, `StringsParityTest.kt`). `src/test/kotlin/` (241 JVM unit tests) and `src/androidTest/kotlin/` (105 instrumented tests, Compose UI + real-device playback/session/Keystore tests). New `mobile/androidApp/README.md` (this task). Purely-additive extensions to two pre-existing, sanctioned-exception files per `PHASE_4_ANDROID_PLAN.md § 6` (G6): `tools/token-pipeline/generate.js` gained a 4th (Android) output target, and 8 new `design-to-code/screens/mobile-*.json` exact-showcase specs were extracted (screen count 29→37) — both guardrailed throughout to leave Web's own generated output byte-identical or additively changed only, re-verified every time either was touched.

**Git:** 24 commits on `main` from Phase 4 kickoff (`b4fd9da`) through Task 19's completion (`802bc75`), touching 164 files (29,080 insertions) — plus this Task 20 documentation pass on top. Full list: `git log --oneline b4fd9da..802bc75`. Working tree is clean; all commits pushed to `origin/main`.

### 3. API/contracts produced

**No new backend routes and no backend changes of any kind** — Phase 4 is a pure client of the Phase 1 backend via the Phase 3 `mobile/shared` module, per the standing "later phases don't touch `backend/`" rule (independently re-verified via `git log --name-only b4fd9da..802bc75` — zero commits touch `backend/`). No new `mobile/shared` contract either — Phase 4 consumes `MentoraSdk`'s existing 10 façade domains exactly as Phase 3 shipped them; the one Phase-4-owned interface implementation is `LessonPlaybackController` (ExoPlayer/Media3), fulfilling a contract `shared` already defined, not adding a new one.

### 4. Database changes

**None.** Phase 4 made zero backend or database changes — no new collection, no new field, no schema change, no seed-data change. The app reads/writes exclusively through `MentoraSdk`, which itself talks only to the existing Phase 1 REST API.

### 5. Tests/verification performed

**Automated:** `:shared:testDebugUnitTest` 249/249 throughout every single task of this phase, zero diff in `mobile/shared` (the standing Phase 3 regression guard) — independently re-confirmed at every checkpoint, not assumed. `:androidApp:testDebugUnitTest` 241/241 (grew from 0 at Task 1 to 241 by Task 19, one new JVM test file/class added almost every task). `:androidApp:connectedDebugAndroidTest` — **105/105**, the full instrumented suite, run against the real `Chatting_Pixel_8_API_36` emulator with the real local backend, confirmed green for the first time against the exact final commit during Task 19's recovery (see § 7). Every task's own per-task instrumented count is recorded in `CURRENT_STATUS.md`'s Phase 4 task table (3/3 at Task 4 growing to 105/105 by Task 19) — the suite was re-run, not merely trusted, at essentially every checkpoint throughout the phase.

**Manual/live, every task:** every one of the 18 screens was independently verified live on the real emulator against the real seeded backend before being marked DONE — this project's standing discipline, unchanged from Phases 1–3. Task 19 + Task 20 together add a dedicated **cross-cutting** live pass, beyond each screen's own original task-time verification: 16 of 18 screens re-checked specifically in Arabic+Dark (the combined state most likely to expose either an RTL bug or a color-token bug hiding behind the other), plus a font-scale (130%) no-clipping spot-check. Quiz/Quiz Results were not part of this cross-cutting re-check (see Known limitations) — both were verified at their own Task 14 review time and are covered by the current 105/105 instrumented run.

**Review discipline:** nearly every substantial task (5 of the 2 component kits, the navigation shell, Auth, Home/My Learning, Course Player, Quiz, Certificates, Learning Path Details, AI Tutor, Profile/Settings) went through at least one dedicated Opus review round per the standing routing policy, several through two, plus two independent Codex second opinions on security-sensitive code (App Bootstrap's missing `INTERNET` permission, D4; Demo Checkout/Purchase Success's cross-session token-race root cause, D83) — full per-task account in `CURRENT_STATUS.md`'s Phase 4 task table, cross-referenced to `DECISIONS_LOG.md` D78–D95.

### 6. Known limitations

None of these block Phase 4's quality gate — each is a deliberate, disclosed, non-blocking gap or a real environment constraint, not an undetected defect (full detail at the cross-referenced decision):

1. **Quiz/Quiz Results not re-verified live this session** (D95, Task 20) — both already passed their own Task 14 review round (D89) and are covered by the current 105/105 instrumented run; reaching them live requires completing all 12 lessons of a seeded course, judged not worth the time cost given no code in either screen changed since Task 14.
2. **The RTL/theme/font-scale sweep is a representative spot-check, not an exhaustive walk of every screen × every dimension** (D94) — 16 of 18 screens were live-checked in Arabic+Dark; font-scale verification is a no-clipping-observed check, not a rigorous scaled-text measurement.
3. **`AnswerOption`/`QuestionCard`'s new `retryLabel`-class string-resource defaults were verified by compilation only, not live** (D94) — every current real call site overrides them explicitly, so the defaults themselves are unexercised on-device; reaching them needs an in-progress quiz attempt, not set up this session.
4. **No password-change endpoint; `avatarMediaId` is a dead field** — the same real spec-vs-backend gap Web's Phase 2 disclosed (D44), inherited unchanged; Profile/Settings omit both.
5. **No real certificate image asset** — `CertificateCard`/`CertificateDetailScreen` render a token-driven placeholder "document" presentation, same disclosed precedent as Web's D42.
6. **Icon set is a hand-drawn `ImageVector` port of Web's own 42-icon placeholder set** (D80), not the real Material Symbols Rounded font this project's design system ultimately specifies.
7. **Arabic typography uses `FontFamily.SansSerif` as a placeholder** — no Noto Sans Arabic (or equivalent) font asset exists in the repo yet.
8. **The backend base URL is hardcoded to the Android emulator's `10.0.2.2` alias** — a physical device needs a manual source change; no build-variant/config mechanism exists yet for this (D53 precedent — no production backend exists anywhere in this project).
9. **AI Tutor has no docked-panel variant for Course Player** — only the full-screen tab-root chat screen exists; the same disclosed gap Web's Phase 2 Task 9 already carried, inherited here unchanged.
10. **`GetMyLearningWithProgressUseCase`/the followed-Learning-Paths join are layered in `androidApp`, not `shared`** (G3/G5, `PHASE_4_ANDROID_PLAN.md § 6`) — an accepted N+1-at-demo-scale tradeoff mirroring Web's own D40 precedent; Phase 5 (iOS) will have to repeat this join independently unless a future decision promotes it into `shared`.
11. **A narrow, self-correcting "wrong tab momentarily associated" edge case** when a guest triggers the Continue-Learning auth gate specifically (D81/Task 6) — back-press and nav-hiding both still behave correctly regardless; disclosed, not fixed, low-impact.
12. **A possible brief over-dimmed flash on `AppDialog` open specifically** (Task 8) — the steady-state scrim color (what a user sees for the duration the dialog stays open) is measured-correct in both themes; only the transient first-frame-or-two during the open animation is unverified.

### 7. Decisions made

18 implementation-time decisions recorded in `execution/DECISIONS_LOG.md` (D78–D95), spanning: the architect-derived 20-task plan and every backend/design-source-derived gap-fill (D78); the Android secure-storage/Keystore verification and a real missing-`INTERNET`-permission catch by an independent Codex review (D4-equivalent, recorded under D-numbering at Task 4); the component-kit fidelity passes, including a 3-attempt scrim-color chase independently re-verified by Codex (Task 8); the navigation shell's structural-anchor HIGH bug (Task 6); a real cross-session token-identity race chased through 3 attempts to its true root cause by an independent Codex second opinion after two Opus-review attempts fell short (D83, Demo Checkout/Purchase Success); the `LessonPlaybackController`/Course Player concurrency-critical state machine's 5-review-round account (D85/D87/D88); Quiz's locked-spec conflict resolution and answer-persistence-across-back-navigation fix (D89); the first genuinely functional in-app language switch and its `ContextWrapper`-vs-raw-`createConfigurationContext` correctness distinction (D93); and the Task 19 session-interruption recovery + final locale-reload/hardcoded-default sweep (D94) and this Task 20 write-up (D95). None reopens or contradicts a locked architecture/product/UX/design-system decision — every one is either a real implementation-time gap the locked docs correctly left for implementation to resolve, or a disclosed, non-blocking limitation.

### 8. What Phase 5 (and later phases) depend on

- `mobile/shared`'s public `MentoraSdk` façade remains the only sanctioned consumption path — Phase 5 (iOS) must supply its own `platformModule()` (already defined in `iosMain`, unwired on this Windows host per Phase 3's own disclosed limitation) and its own `LessonPlaybackController` implementation (AVPlayer or equivalent) — `shared` provides the interface only, exactly as it did for Android.
- `design-to-code/` (now 37 screens, 8 of them mobile-specific exact-showcase specs added this phase) is the platform-neutral source Phase 5 should read from for iOS UI — the 8 mobile screen specs extracted this phase are platform-neutral in the same way the original 29 were, not Android-specific, and should save Phase 5 the same re-extraction work Phase 4 itself avoided.
- `execution/INTEGRATION_CONTRACT.md` § 12 (Phase 3's mobile wire-contract reference) remains authoritative and unchanged — Phase 4 added no new mobile-specific wire-contract facts beyond what Phase 3 already documented.
- The full local dev stack (`backend/README.md`, `mobile/shared/README.md`, `mobile/androidApp/README.md`, `start-mentora.ps1`/`stop-mentora.ps1`) is ready for Phase 5's own local development and live-integration-testing needs — `mobile/androidApp/README.md`'s Known Limitations section in particular consolidates every Android-specific gap Phase 5 should check whether it inherits or must solve independently for iOS (e.g. the font-placeholder, icon-placeholder, and hardcoded-base-URL gaps are very likely to recur identically on iOS unless deliberately addressed there).
- The Task 19 locale-reload sweep pattern (`reloadOnLocaleChange`, D93/D94 — reload only endpoints that are actually locale-sensitive, never blanket-reload everything on a language switch) is a reusable design precedent Phase 5 should follow for its own ViewModel/state-holder equivalent, not reinvent.

### 9. What later phases must NOT redo

- Do not rebuild any of the 18 Android screens, either component kit, the navigation shell, or `MediaPlaybackController` — they are complete, tested (241 JVM + 105 instrumented), and reviewed. Phase 5 builds its own iOS equivalents; it does not touch `mobile/androidApp/`.
- Do not re-litigate the Course-Player-docked AI-Tutor-panel gap, the certificate-placeholder-image decision, or the icon-set/Arabic-font placeholders as new discoveries for iOS to "fix on Android's behalf" — all are already disclosed, cross-phase-consistent (Web carries the same gaps), and out of scope for a phase that is not Android's own.
- Do not re-derive the `LessonPlaybackController` interface contract, the `MentoraSdk` façade consumption pattern, or the locale-reload-only-what's-sensitive design precedent — all already established and documented; Phase 5 implements against the same interfaces/patterns for iOS, it does not redesign them.
- Do not assume the Task 19/20 RTL/theme/font-scale spot-check was exhaustive across all 18 screens — it explicitly was not (§ 6 items 1–3); if Phase 5 or a future Android follow-up needs full-coverage confidence, that remains real, not-yet-done work, not a re-litigation of a completed task.

### 10. Git commit/state reference

`main` branch, working tree clean, all commits pushed to `origin/main`. Phase 4 spans commits `b4fd9da`..`802bc75` (24 commits; `git log --oneline b4fd9da..802bc75` for the full list with messages) plus this Task 20 documentation/handoff commit on top. The `architecture/`, `product/`, `ux/`, `design-system/`, `design-review-locked/`, and `backend/` directories were never modified by any Phase 4 commit (verified via `git log --name-only b4fd9da..802bc75`); the only touches outside `mobile/`/`execution/` are the two pre-sanctioned, guardrailed exceptions from § 2 (`tools/token-pipeline/generate.js`, `design-to-code/screens/mobile-*.json` + the byte-verified regenerated `web/src/lib/design-to-code.generated.ts`) — every locked doc and Phase 1–3's shipped code are exactly as they were at Phase 4's start. No `infra/` directory exists — Phase 5+ has not been started.

**Phase 5 (iOS) note:** Phase 5 was started after Phase 4 (T1-T11 done, CI-confirmed) and then explicitly **frozen/deferred by user project decision** on 2026-09-20 (no Mac/iPhone on this machine for meaningful interactive validation; iOS work should not gate the rest of the project) — see the "PHASE 5 FREEZE" section in `execution/CURRENT_STATUS.md` for the full account. It has no dedicated numbered entry in this log because it never reached a completion checkpoint; Phase 6 proceeded without it, as explicitly authorized.

---

## PHASE 6 — AI Tutor Integration

**Status:** T1-T7 and T9 COMPLETE; **T8 (live runtime verification against the real Anthropic API) hard-BLOCKED** on a missing external credential (`AI_PROVIDER_API_KEY`), per this project's explicit standing instruction never to fabricate, simulate, or assume a successful real provider call. **Phase 7 must NOT begin without the user's separate, explicit approval** — this is a standing constraint restated here, not a new one.

### 1. What was implemented

The real `AnthropicAiProvider` (Ktor CIO client, manual Server-Sent-Events parsing of Anthropic's Messages API streaming format) now stands behind the AI Tutor endpoint (`/api/v1/ai-tutor/*`) whenever `AI_PROVIDER_API_KEY` is configured, selected automatically at startup via `AppConfig.aiProviderMode()` with `StubAiProvider` remaining the safe, zero-cost, always-available default when it isn't. This was **not a blank-slate build**: the AI Tutor architecture, provider abstraction, and Anthropic-as-provider decision (ADR-009) were already locked, and the backend `aitutor` module plus the KMP/Android/Website clients were already fully built and live-verified against `StubAiProvider` in Phases 1-4 (D139's recovery finding). The real remaining scope, delivered this phase: (a) implement `AnthropicAiProvider` for real; (b) reshape the `AiProvider` interface from a lazily-evaluated `Flow`-returning shape to `suspend fun complete(request, onStream): AiUsage` — an eager-connect-then-stream design specifically so a real provider failure always surfaces as an ordinary pre-stream `ApiException`, never a broken mid-stream response, with zero client/wire-format change; (c) close two previously-disclosed product gaps — no enrolled-course context in the AI Tutor's system prompt (the "What should I learn next?" quick action was answering from a fixed persona string with no live enrollment data), and no history normalization/turn-merging before sending conversation history to a real provider (a gap that would otherwise let a retry-after-failure send two adjacent same-role turns and permanently corrupt a conversation, per Anthropic's strict alternating-turn requirement — found and fixed as F1 below); (d) add basic prompt-injection mitigation (tag-neutralization) for course/lesson titles injected into the system prompt; (e) add observability logging; (f) verify Android and Website genuinely, live, against the new code (not just re-trust the stub-era pass).

Tasks T1-T9 (per `execution/PHASE_6_IMPLEMENTATION_PLAN.md`): T1 provider-boundary foundation, T2 real `AnthropicAiProvider`, T3 `AiPromptBuilder` (prompt text + enrolled-course context + history normalization), T4 observability logging + failure-path tests, T5 mandatory two-round review (Opus + independent Codex second opinion), T6 documentation + config surface, T7 cross-platform verification without a key, T8 (BLOCKED, see below), T9 this final acceptance audit.

### 2. Files/modules created

Backend only — no `web/`, `mobile/`, `architecture/`, `product/`, `ux/`, or `design-system/` file touched anywhere in Phase 6 (verified via `git diff --stat` at every task's completion gate, and again at this T9 audit). New: `backend/src/main/kotlin/com/mentora/backend/aitutor/provider/AnthropicAiProvider.kt`, `AnthropicSse.kt`, `AnthropicWire.kt`; `backend/src/main/kotlin/com/mentora/backend/aitutor/service/AiPromptBuilder.kt`. Modified: `AiProvider.kt`/`StubAiProvider.kt` (interface reshape), `AiTutorService.kt` (prompt building + observability logging), `AiTutorModule.kt` (provider selection + HTTP client lifecycle), `AiTutorRoutes.kt` (streaming call site moved inside the provider callback), `CourseRepository.kt`/`CourseService.kt` (`findByIds`/`enrolledCourseBriefs`), `ApiException.kt` (new `ServiceUnavailable`), `AppConfig.kt` (new AI-provider config fields), `Application.kt` (startup log line), `build.gradle.kts` (Ktor client dependencies). New test files: `AnthropicAiProviderTest.kt` (22 cases), `AiPromptBuilderTest.kt` (18 cases), `AiTutorServiceTest.kt` (4 cases); `AiTutorIntegrationTest.kt` grown from 6 to 10 cases, the original 6 kept byte-identical.

**Git:** 10 commits on `main` from Phase 6 kickoff (`a607da6`) through this T9 audit, spanning `execution/PHASE_6_ACCEPTANCE_CRITERIA.md`/`PHASE_6_SYSTEM_DESIGN.md`/`PHASE_6_IMPLEMENTATION_PLAN.md` (kickoff), T1-T4 implementation, T5's two review rounds (`c11080e`, `0196bde`), T6 documentation (`82e2bb4`), T7 verification (`f08cfee`), and this T9 handoff/audit commit on top. Full list: `git log --oneline a607da6..HEAD`. Working tree clean, all commits pushed to `origin/main`.

### 3. API/contracts produced

**No route, request, or response shape change** — `GET /api/v1/ai-tutor/conversation` and `POST /api/v1/ai-tutor/conversation/messages` are byte-identical to their Phase 1-4 shape; the streamed `text/plain` chunked response body is unchanged. The one genuinely new thing is a new possible error outcome: `AI_TUTOR_UNAVAILABLE` (HTTP 503), returned when the provider fails before any content streams — this required **zero** KMP/Android/Web change because it falls through to each client's existing `Unknown`-error fallback case (the same forward-compatible pattern already used for e.g. `FORBIDDEN_CSRF`), confirmed by reading the shared error-mapping code and re-confirmed live during T7. Recorded as an as-built addition in `execution/INTEGRATION_CONTRACT.md` § 4.

### 4. Database changes

**None.** No new collection, field, index, or seed-data change. `aiConversations`/`aiMessages` (already implemented in Phases 1-4) are unchanged.

### 5. Tests/verification performed

**Automated:** full backend suite **127/127 green** (fresh run at this T9 audit; up from 79 before Phase 6 began), zero failures, zero errors, spanning every backend module — not just `aitutor`. New coverage: 22 `MockEngine`-based unit tests for `AnthropicAiProvider`'s request shape, SSE parsing, and full error-mapping table (exercising 401/404/429/529, malformed streams, clean EOF, CRLF framing, cancellation semantics — no real network call anywhere in the default suite); 18 tests for `AiPromptBuilder`'s prompt construction, history normalization, and prompt-injection tag-neutralization; 4 tests proving enrolled-course-context resolution is scoped to the calling principal's own enrollments; 4 new `AiTutorIntegrationTest` cases (pre-stream failure, mid-stream failure, enrolled-course context, an F1-regression case for retry-after-failure turn merging).

**Manual/live (T7, no API key involved):** Website — self-performed via Chrome browser automation against the locally-running stub-mode backend: navigation, first-open welcome + quick actions, loading state, all 5 quick actions, a real killed-backend outage producing the clean generic error state with the user's message preserved, a successful retry once the backend was restarted, Arabic/RTL, Light/Dark — all PASS. Android — delegated to a background agent driving a real debug APK on an emulator via adb: the identical checklist, all PASS, plus per-turn retry-isolation confirmed with two independent triggered errors. **Zero `web/` or `mobile/` file was changed by this verification** — it was a pure verify-only pass, confirming the clients Phases 2/4 already built work correctly against the now-real backend code path (still running in stub mode, since no key exists in this environment).

**Independent review (T5, the mandatory checkpoint):** a full-diff Opus review found and fixed 7 real defects across severities HIGH-to-LOW (most critically F1: a retry after any transient provider failure could permanently corrupt a conversation by sending two adjacent same-role turns to Anthropic — a defect none of T1-T4's own test suites caught in isolation, and one the design's own § 22 had wrongly claimed was already handled), 2 more deliberately deferred as low-severity/non-blocking. A follow-up independent Codex second opinion, scoped specifically to the provider/key-handling boundary, then found 2 further genuine defects the Opus round missed (a second cancellation-swallowing path one suspension point later than F4's fix; an empty text delta that could satisfy a "first token received" check). All fixed; full accounts in `DECISIONS_LOG.md` D141/D142.

### 6. Known limitations

None of these are new defects — each is either a deliberate, disclosed, low-severity deferral, or a genuine external blocker, not an undetected problem:

1. **T8 (live runtime verification against the real Anthropic API) is hard-BLOCKED** — `AI_PROVIDER_API_KEY` is unset in this environment, and per explicit standing instruction this was never worked around, simulated, or assumed. The following remain genuinely, only, runtime-unverified: a real Anthropic call succeeding end-to-end (A1/A3/B1), real recommendation quality for "What should I learn next?" against real enrollments (C3), real bilingual response behavior (D3), and Android/Web against the real provider specifically (F4/G4). Everything else about this feature is implementation-complete and test-verified.
2. **F6 (D141):** no explicit "data, not instructions" preamble on the `<enrolled_courses>` prompt block, and case-sensitive tag matching in the prompt-injection mitigation — both low-severity, deliberately deferred, not blocking.
3. **F8 (D141):** mid-stream failures are logged under the same `outcome=provider_unavailable` bucket as pre-stream failures, rather than a distinct `stream_failed` outcome with a `partialChars` count as § 21 of the design anticipated — low-severity, deliberately deferred.
4. **Pre-existing, disclosed, carried-forward gaps, not this phase's to fix:** no docked/contextual AI Tutor panel inside Course Player on Web or Android (full-screen/full-tab chat only, accepted since Phase 2/4); Website's AI Tutor is global-mode-only (never sends `courseId`/`lessonContextId`).
5. **One incidental, unrelated finding from the T7 Android agent, explicitly out of scope:** on Course Details, the "Login to Enroll" CTA doesn't immediately refresh to "Enroll" right after logging in via the auth-gate flow (a stale auth-state read on that screen) — not an AI Tutor defect, not investigated further, worth a look in a future pass.

### 7. Decisions made

Six sequential decisions recorded in `execution/DECISIONS_LOG.md` (D139-D144): D139 (kickoff recovery findings + the Codex/Sonnet delegation-policy reconciliation for this boundary specifically); D140 (System Design + Implementation Plan authored); D141 (T1-T4 implementation + the T5 Opus review round: 7 findings, F1/F2 the most significant, all fixed or explicitly deferred); D142 (the T5 Codex second-opinion round: 2 more findings, both fixed — T5 formally closes here); D143 (T6 documentation pass); D144 (T7 cross-platform verification, both platforms PASS). None reopens or contradicts a locked architecture/product/UX decision — the provider abstraction and Anthropic choice were already locked (ADR-009) before this phase began.

### 8. What Phase 7 (and later phases) depend on

- The `AiProvider` interface's suspend+callback shape (`complete(request, onStream): AiUsage`) is now the permanent provider-boundary contract — any future provider swap or addition implements this same interface; do not revert to the old `Flow`-returning shape.
- `AiPromptBuilder` is now the single owner of prompt construction, enrolled-course-context injection, and history normalization — any future change to tutoring behavior (new quick actions, richer context, a different persona) belongs there, not scattered into `AiTutorService` or a client.
- The `AI_TUTOR_UNAVAILABLE` / `Unknown`-fallback precedent (§ 3 above) is the reusable pattern for any future backend error code that doesn't need a dedicated client-visible case — extend the fallback, don't reflexively add a new sealed-class case to the shared KMP taxonomy.
- T8's checklist (A1/A3/B1/C3/D3/F4/G4) is exactly what must be run, in full, the moment a real `AI_PROVIDER_API_KEY` and a confirmed Anthropic model id become available — before Phase 6 can be marked truly, fully complete (not just "T9-audited with a known gate").

### 9. What later phases must NOT redo

- Do not re-implement or re-review `AnthropicAiProvider`/`AnthropicSse`/`AnthropicWire` — they are complete, independently reviewed twice (Opus + Codex), and every found defect is fixed. A future provider addition (not a fix to this one) is new work, not a redo of this.
- Do not re-litigate the docked-AI-Tutor-panel gap or Website's global-mode-only behavior as new discoveries — both are already disclosed, cross-phase-consistent, and out of scope unless the user explicitly asks to change them.
- Do not treat F6/F8 as unknown or undiscovered — they are known, deliberately deferred, and documented; picking them up later is optional follow-up work, not a missed defect.
- Do not assume T7's manual verification was exhaustive across every locale/theme/screen-state combination — it was a representative, real-device/real-browser pass covering the checklist the plan specified (B2-B6, F2, F3, E3, plus outage/retry), not a full combinatorial sweep.

### 10. Git commit/state reference

`main` branch, working tree clean, all commits pushed to `origin/main`. Phase 6 spans commits `a607da6`..`f08cfee` (see § 2) plus this T9 documentation/handoff commit on top. The `architecture/`, `product/`, `ux/`, `design-system/`, `design-review-locked/`, `web/`, and `mobile/` directories were never modified by any Phase 6 commit (verified via `git diff --stat` at every task gate and again at this audit) — every locked doc and every Phase 1-5 client is exactly as it was at Phase 6's start. `backend/.env` remains gitignored, untracked, and its `AI_PROVIDER_API_KEY` value remains unset in this environment; no secret has ever been committed (verified via full-history scan at this audit). **T8 remains BLOCKED and Phase 7 has explicitly NOT been started — both require action outside this session's authority: a real Anthropic API key for T8, and the user's own separate, explicit approval for Phase 7.**
