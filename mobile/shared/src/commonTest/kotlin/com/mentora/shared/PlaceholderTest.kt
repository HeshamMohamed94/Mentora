package com.mentora.shared

import kotlin.test.Test
import kotlin.test.assertTrue

/**
 * Task 1 scaffold placeholder — proves `commonTest` actually compiles and executes via
 * `:shared:testDebugUnitTest`. Replaced by real coverage starting Task 2.
 */
class PlaceholderTest {

    @Test
    fun `shared module test infrastructure is wired up`() {
        assertTrue(true)
    }
}
