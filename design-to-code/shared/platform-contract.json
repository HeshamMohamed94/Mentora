{
  "$description": "The platform-neutral contract every generator (Web now, Android/iOS later) must satisfy. Restates design-system/platform-mapping.md's conventions in a form the design-to-code pipeline itself can be validated against. Android and iOS are NOT implemented this phase — this file only makes the shared JSON structurally suitable for them later.",
  "sourceReferences": ["design-system/platform-mapping.md"],

  "visualParityRule": {
    "$title": "MENTORA VISUAL PARITY RULE (D53)",
    "rule": "Web, Android, and iOS use the same semantic visual system. Platform-native UI implementation is allowed (each platform renders with its own toolkit — Tailwind/CSS on Web, Jetpack Compose on Android, SwiftUI on iOS), but visual semantics must remain equivalent across all three. No platform may independently choose a different visual identity.",
    "mustBeTheSameAcrossPlatforms": [
      "colors — every color a screen shows must resolve from shared/tokens.json#/color/semantic (light and dark), never a platform-local palette",
      "background hierarchy — the same background.primary/background.secondary/surface.default/surface.elevated/surface.variant layering, in the same relative order of lightness",
      "typography hierarchy — the same shared/typography.json scale and weight relationships (display/heading/body/label/caption), even though the concrete unit differs (rem vs sp vs Dynamic-Type pt)",
      "spacing scale — the same shared/spacing.json 4px-based step values, converted to the platform's fixed layout unit (px/dp/pt), never a platform-invented scale",
      "radius family — the same shared/shape.json radius steps mapped to the same component types (e.g. radius.large for all cards on every platform)",
      "surface hierarchy — the same card/panel/sheet elevation relationships from shared/elevation.json",
      "component intent — the same shared/components.json recipes (e.g. PrimaryButton, CourseCard, Checkout) resolve to the same token references regardless of platform; only the rendering technology changes",
      "artwork system — the same shared/artwork.json 5-motif course-artwork composition (gradient + icon + category chip), never a platform-specific illustration style"
    ],
    "platformNativeDifferencesAllowed": [
      "native navigation mechanics — e.g. Android back-stack/gesture nav, iOS swipe-back, Web browser history — the navigation MODEL is native, the navigation SURFACE'S color/typography/spacing is not",
      "platform-safe-area behavior — status bar / notch / home-indicator insets are platform-native concerns with no Web equivalent",
      "native interaction affordances — e.g. Android ripple, iOS haptic feedback / press-scale, Web :hover — the interaction MECHANISM is native, the resulting state color (hover/pressed containers from shared/tokens.json) is not",
      "native control behavior where required by platform convention — e.g. a native iOS date/time picker sheet, an Android native Snackbar host — as long as the control's own visible colors/typography still resolve from the shared semantic tokens, not a platform default theme"
    ],
    "explicitlyNotAllowed": [
      "an independent Android color palette (e.g. Material You dynamic color) replacing shared/tokens.json's semantic colors",
      "an independent iOS color palette (e.g. raw UIColor system colors) replacing shared/tokens.json's semantic colors",
      "separate auth (Login/Register) styling that diverges from the same background/surface/card treatment used elsewhere on that platform",
      "a separate CourseCard identity per platform — artwork motifs, category chip treatment, and card surface must be recognizably the same component everywhere",
      "unrelated dark/light systems — dark mode on every platform must be the same semantic structure re-resolved (shared/tokens.json#/color/semantic/dark), never a platform-specific dark palette invented independently"
    ],
    "enforcementToday": "No Android/iOS code exists yet (still NOT IMPLEMENTED, see android/ios sections below) — this rule governs how that future code must be generated from shared/*.json, and is the criterion a future PR review must check against. Today it also re-states why Web's own Login/Register (D51) and Purchase Success/Instructor Dashboard/etc. (D52) corrections were required to consume the SAME shared background/surface tokens as every other Web screen rather than any screen-invented alternative."
  },

  "pipeline": {
    "diagram": "Shared JSON (design-to-code/shared/*.json, design-to-code/screens/*.json, design-to-code/patterns/*.json) -> Web generator (tools/design-to-code/generate.js) -> [Android generator later] -> [iOS generator later]",
    "rule": "A generator READS shared JSON and WRITES platform-specific generated artifacts. A generator never writes back into shared/screens/patterns JSON, and a generated artifact is never hand-edited — regenerate from source instead.",
    "existingWebTokenPipeline": {
      "path": "tools/token-pipeline/generate.js",
      "role": "Pre-existing, NOT replaced. Reads design-system/design-tokens.json + themes/*.json directly and writes web/styles/tokens.css, web/styles/tailwind-theme.css, web/src/lib/design-tokens.generated.ts. This design-to-code pipeline is a SEPARATE, ADDITIONAL generator for the layers token-pipeline does not cover (component recipes, navigation/shell rules, artwork rules, screen-to-route mapping) — it does not duplicate or conflict with token-pipeline's output.",
      "decisionReference": "execution/DECISIONS_LOG.md D35 (token-pipeline's own plain-Node, no-external-dependency precedent, followed by tools/design-to-code/ for the same reason)."
    }
  },

  "web": {
    "consumes": ["design-to-code/shared/*.json", "design-to-code/screens/*.json (for route/shell mapping only, not for visual re-generation of full pages)"],
    "generatesInto": "web/src/lib/design-to-code.generated.ts (typed constants: component recipe names, navigation shell rules, artwork motif mapping, responsive breakpoint re-export for JS-side consumers that can't read a CSS var)",
    "colorConvention": "CSS custom property, kebab-case, theme-scoped via [data-theme] — already generated by tools/token-pipeline/generate.js; this pipeline's TS constants REFERENCE the same var names, never redefine them.",
    "doesNotGenerate": "Raw CSS custom properties for tokens — that remains token-pipeline's job. This pipeline generates the layer ABOVE raw tokens: named recipes and structural constants."
  },

  "android": {
    "status": "NOT IMPLEMENTED — mapping only, for future readiness.",
    "wouldConsume": "design-to-code/shared/*.json",
    "wouldGenerateInto": "android/app/src/main/java/.../design/MentoraTokens.kt (or equivalent) — a MentoraTheme Compose theme object exposing colors/typography/spacing/shape/elevation via CompositionLocal, plus component-recipe data classes mirroring shared/components.json's structure 1:1.",
    "namingConvention": "camelCase, e.g. color.brand.primary -> MentoraTheme.colors.brandPrimary (per design-system/platform-mapping.md § 1).",
    "readinessNotes": [
      "shared/tokens.json's dot-path names already match platform-mapping.md's documented Android naming convention 1:1 — a generator only needs a dot-path -> camelCase transform, already implemented once in tools/token-pipeline/generate.js's kebabPath()/camelToKebabSegment() helpers (inverse direction needed for Android/iOS, not yet written).",
      "shared/components.json's token references (e.g. \"color.brand.primary\") resolve identically regardless of target platform — no Android-specific re-authoring of component recipes is anticipated, only a new renderer for the same recipe data.",
      "shared/navigation.json's shell definitions were originally Web-shaped only (Sidebar/Navbar) since Instructor/Admin are Web-only by product design (product/USER_ROLES.md). As of Phase 4 Task 3 (design-to-code/screens/mobile-*.json extraction pass), the mobile bottom-tab shell IS now specified: shared/navigation.json#/shells/mobileStudentShell — the 5-item MobileBottomNavigation (Home/Explore/My Learning/AI Tutor/Profile), with real showcase-extracted visual values, LTR+RTL (real Arabic labels), tap-active-tab-pops-to-root behavior, and a disclosed platform difference for the focused-learning-shell case (mobile fully HIDES the nav on Course Player/Quiz, vs. Web's 72px icon-only collapse). An Android app (Student-only surfaces) would consume shared/navigation.json#/shells/mobileStudentShell for its 5 tab roots, and reuse shared/navigation.json#/shells/coursePlayerShell only for its underlying focused-learning-shell CONCEPT (not its Web-specific collapse mechanism) on Course Player/Quiz — never Sidebar/Navbar, which remain Web-only."
    ],
    "colorSchemeMapping": {
      "$description": "Every dot-path in shared/tokens.json#/color/semantic mapped to its Jetpack Compose Material 3 ColorScheme slot, for BOTH light and dark — the generator's job is filling these slots from the SAME resolved values already in tokens.json, never choosing new ones (e.g. never Material You dynamic color).",
      "light": {
        "background.primary": "ColorScheme.background", "surface.default": "ColorScheme.surface", "surface.elevated": "ColorScheme.surfaceContainerHigh", "surface.variant": "ColorScheme.surfaceVariant", "surface.inverse": "ColorScheme.inverseSurface",
        "text.primary": "ColorScheme.onBackground / ColorScheme.onSurface", "text.secondary": "ColorScheme.onSurfaceVariant", "text.disabled": "ColorScheme.onSurface @ disabledContentOpacity", "text.inverse": "ColorScheme.inverseOnSurface", "text.link": "ColorScheme.primary (rendered as a clickable AnnotatedString span, not a separate slot)",
        "border.default": "ColorScheme.outlineVariant", "border.strong": "ColorScheme.outline", "border.focus": "ColorScheme.primary", "border.error": "ColorScheme.error",
        "brand.primary": "ColorScheme.primary", "brand.primaryContainer": "ColorScheme.primaryContainer", "brand.onPrimary": "ColorScheme.onPrimary", "brand.onPrimaryContainer": "ColorScheme.onPrimaryContainer", "brand.onSurfaceInverse": "ColorScheme.inversePrimary",
        "secondary.default": "ColorScheme.secondary", "secondary.container": "ColorScheme.secondaryContainer", "secondary.onSecondary": "ColorScheme.onSecondary", "secondary.onSecondaryContainer": "ColorScheme.onSecondaryContainer",
        "success.default/container/onSuccess/onSuccessContainer": "custom ColorScheme extension (M3 has no built-in success role) — extend MaterialTheme via a CompositionLocal, same pattern web uses for --color-success-*",
        "warning.*": "custom ColorScheme extension, same reasoning as success",
        "error.default": "ColorScheme.error", "error.container": "ColorScheme.errorContainer", "error.onError": "ColorScheme.onError", "error.onErrorContainer": "ColorScheme.onErrorContainer",
        "info.*": "custom ColorScheme extension, same reasoning as success",
        "overlay.scrim": "ColorScheme.scrim"
      },
      "dark": "Same slot mapping as light — Compose's darkColorScheme(...) is filled from shared/tokens.json#/color/semantic/dark's values for the identical slot names above. No slot is ever re-mapped between light/dark; only the resolved value differs, per the Layer-1/Layer-2/Layer-3 rule shared/tokens.json itself documents."
    },
    "typographyMapping": { "$description": "shared/typography.json scale -> Compose Typography slots.", "display.large": "Typography.displayLarge", "display.medium": "Typography.displayMedium", "heading.h1": "Typography.headlineLarge", "heading.h2": "Typography.headlineMedium", "heading.h3": "Typography.headlineSmall", "heading.h4": "Typography.titleLarge", "body.large": "Typography.bodyLarge", "body.medium": "Typography.bodyMedium", "body.small": "Typography.bodySmall", "label.large": "Typography.labelLarge", "label.medium": "Typography.labelMedium", "caption": "Typography.labelSmall" },
    "shapeMapping": { "$description": "shared/shape.json radius steps -> Compose Shapes slots.", "radius.small": "Shapes.extraSmall", "radius.medium": "Shapes.small", "radius.large": "Shapes.medium (cards, dashboard cards, all cards per shared/shape.json)", "radius.xlarge": "Shapes.large (dialogs, bottom sheets — top corners only, via a custom RoundedCornerShape not Shapes.extraLarge's all-corner default)", "radius.full": "CircleShape / RoundedCornerShape(percent = 50) (chips, badges, avatar, progress bar, switch)" },
    "elevationMapping": "shared/elevation.json's e1-e4 shadow values map to Compose `tonalElevation`/`shadowElevation` Dp values on Surface/Card composables — per the design system's own 'borders over shadow' principle (see visualParityRule above), Compose cards should prefer a `border` stroke (BorderStroke(1.dp, ColorScheme.outlineVariant)) with LOW tonalElevation, not a heavy shadowElevation, to visually match Web's subtle e1/e2 shadows rather than Material's default elevated-card shadow weight."
  },

  "ios": {
    "status": "Token layer IMPLEMENTED (Phase 5 Task T2) — mobile/iosApp/iosApp/Theme/{MentoraTokens.swift, Color+Mentora.swift, MentoraColors.xcassets/**}, generated by tools/token-pipeline/generate.js's iOS output target from design-system/design-tokens.json + design-system/themes/theme-{light,dark}.json (the same authoritative inputs Web/Android already read — NOT a re-derivation from this file's own mapping tables below, which remain descriptive documentation of the generator's behavior). Component-recipe/navigation/artwork generation (the design-to-code layer above raw tokens) remains NOT IMPLEMENTED for iOS.",
    "wouldConsume": "design-system/design-tokens.json + design-system/themes/theme-{light,dark}.json (token layer, implemented); design-to-code/shared/*.json (component-recipe/navigation/artwork layer, not yet implemented for iOS).",
    "wouldGenerateInto": "mobile/iosApp/iosApp/Theme/MentoraTokens.swift (spacing, radius incl. xlarge=24, elevation radius/y/opacity per step, icon sizes, typography METRICS ONLY, per-theme state opacities, touchTarget.ios_pt) + mobile/iosApp/iosApp/Theme/Color+Mentora.swift (a Color extension) + mobile/iosApp/iosApp/Theme/MentoraColors.xcassets/ (46 semantic-color colorsets + one mentoraShadowElevation<N> colorset per elevation step, each with Any Appearance + Dark appearance) — mirrors design-system/platform-mapping.md § 9's suggested repository structure exactly.",
    "namingConvention": "Color.mentora<PascalCase>, e.g. color.brand.primary -> Color.mentoraBrandPrimary.",
    "readinessNotes": [
      "Typography is emitted as PLAIN NUMBERS (MentoraTypographyMetrics: fontSize/lineHeight/fontWeight/tracking), never a Font value — see typographyMapping below for why the previously-assumed `Font.mentora<Name>` static-value shape is unimplementable in SwiftUI, and PHASE_5_IOS_SYSTEM_DESIGN.md § 15.1 for the runtime ViewModifier + @ScaledMetric approach a later task (T6) builds on top of these numbers.",
      "shared/components.json's `select` recipe explicitly maps to a Menu/Picker-style SwiftUI control (design-system/COMPONENTS.md § Select, Native mapping) — already documented, no additional shared-JSON change needed to support it later.",
      "shared/artwork.json's motif system is platform-agnostic (gradient + icon + chip composition) and requires no iOS-specific restructuring — only a new SwiftUI view implementing the same composition recipe."
    ],
    "colorMapping": {
      "$description": "Every dot-path in design-tokens.json#/color/semantic (46 leaves per theme; the tree's other 5 leaves are state opacities, which are numbers, not colors, and are emitted into MentoraTokens.swift instead — see stateOpacityMapping) mapped to a Color Set asset name (Any Appearance = light theme value, Dark Appearance = dark theme value, one asset per dot-path, so SwiftUI's automatic dark-mode resolution does the theme switch, mirroring Web's [data-theme] and Android's ColorScheme — never a manually-branched `if colorScheme == .dark` in view code). IMPLEMENTED as described — see MentoraColors.xcassets/**/Contents.json.",
      "assetNamingConvention": "mentora<PascalCase dot-path>, e.g. background.primary -> mentoraBackgroundPrimary, brand.primaryContainer -> mentoraBrandPrimaryContainer",
      "swiftAccessorConvention": "Color.mentora<PascalCase>, e.g. Color.mentoraBrandPrimary, generated as a Color extension reading the asset catalog entry above — the SAME name for every dot-path in the semantic table (background.*, surface.*, text.*, border.*, brand.*, secondary.*, accent.*, success.*, warning.*, error.*, info.*, overlay.*).",
      "systemColorRule": "Never substitute a raw UIKit/SwiftUI system color (Color.gray, Color(.systemBackground), etc.) for a semantic token slot — every one of the dot-paths above must resolve through its mentora-prefixed asset, per explicitlyNotAllowed above."
    },
    "stateOpacityMapping": "design-tokens.json's 5 state-opacity leaves (hover/pressed/focus/disabledContent/disabledContainer) differ per theme (e.g. pressed/focus/disabledContainer are 0.12 light vs 0.16 dark) so they are NOT one flat constant set — generated as MentoraStateOpacityLight / MentoraStateOpacityDark enums in MentoraTokens.swift, each with one static let per opacity, mirroring how the same generator already resolves Android's per-theme MentoraStateOpacityLight/Dark objects.",
    "shadowColorMapping": "elevation.<N>.ios.opacity (design-tokens.json) drives a generated mentoraShadowElevation<N> colorset per elevation step: Any Appearance = theme-light.json#/elevationShadowBase at that step's opacity; Dark Appearance = theme-dark.json#/elevationShadowBase at that step's opacity x 0.7 — the ~30% dark-mode shadow-opacity reduction elevation.darkModeNote documents, applied once in the generator so no SwiftUI view ever branches on colorScheme for shadow strength.",
    "typographyMapping": {
      "$description": "PREVIOUSLY assumed a `Font.mentora<Name>` static-Font-constant shape (relativeTo: an anchor). That shape is UNIMPLEMENTABLE in SwiftUI: Dynamic Type's `relativeTo:` parameter only exists on `Font.custom(_:size:relativeTo:)`, which requires a bundled font — unacceptable since H8 requires no bundled font (Apple's automatic SF Arabic substitution for Arabic runs depends on using the system font, not a custom one). A static `Font.system(size:weight:)` constant, the only alternative, does not scale with Dynamic Type at all. See PHASE_5_IOS_SYSTEM_DESIGN.md § 15.1 for the full rejection analysis.",
      "actualShape": "Phase 5 T2 (this generator) emits typography as PLAIN NUMBERS ONLY — MentoraTypography.<name>: MentoraTypographyMetrics { fontSize, lineHeight, fontWeight, tracking } — one entry per design-tokens.json#/typography/scale step, in mobile/iosApp/iosApp/Theme/MentoraTokens.swift. A later task (T6, not yet implemented) composes the actual SwiftUI Font at runtime via a `.mentoraFont(_:)` ViewModifier using `@ScaledMetric(relativeTo: <anchor>)` over these numbers, so Dynamic Type scaling is genuinely honored and no font is bundled.",
      "relativeToAnchors": { "$description": "The system Dynamic-Type text-style anchor T6's ViewModifier passes to @ScaledMetric(relativeTo:) for each scale step — kept from the original mapping since the anchors themselves were never the problem, only the Font-constant API shape was.", "display.large": ".largeTitle", "display.medium": ".largeTitle", "heading.h1": ".title", "heading.h2": ".title2", "heading.h3": ".title3", "heading.h4": ".headline", "body.large": ".body", "body.medium": ".body", "body.small": ".subheadline", "label.large": ".subheadline", "label.medium": ".footnote", "caption": ".caption" }
    },
    "shapeMapping": { "$description": "shared/shape.json radius steps -> SwiftUI shape values. Radius NUMBERS are implemented (MentoraRadius in MentoraTokens.swift); the SwiftUI Shape views below remain a later task's (T4b+) job.", "radius.small": "RoundedRectangle(cornerRadius: 8)", "radius.medium": "RoundedRectangle(cornerRadius: 12) — buttons, inputs, select, snackbar, menus", "radius.large": "RoundedRectangle(cornerRadius: 16) — course card, dashboard card, all cards", "radius.xlarge": "UnevenRoundedRectangle (top corners only, 24pt) — dialog / bottom sheet", "radius.full": "Capsule() — chip, badge, avatar (Circle() when 1:1), progress bar" },
    "elevationMapping": "elevation.<N> radius/y/opacity NUMBERS are implemented (MentoraElevation in MentoraTokens.swift) and the step's shadow COLOR is implemented (mentoraShadowElevation<N> colorset, see shadowColorMapping above). Composing those into a `.mentoraElevation(_:)` ViewModifier applying `.shadow(color:radius:y:)` remains a later task's (T4b+) job — per the 'borders over shadow' principle, pair every elevated Card view with a `.overlay(RoundedRectangle(...).stroke(Color.mentoraBorderDefault, lineWidth: 1))` rather than relying on shadow weight alone, matching shared/components.json's card recipe (border + subtle shadow, not shadow-only)."
  },

  "generatorFailureContract": {
    "rule": "Generation MUST fail loudly (non-zero exit, printed reason) rather than emit partial/guessed output when: a screen references a component name absent from shared/components.json; a component/screen references a token dot-path absent from shared/tokens.json (or shared/typography|spacing|shape|elevation.json); two screens declare the same screenId; a pattern is referenced by a screen but does not exist under design-to-code/patterns/.",
    "implementedBy": "tools/design-to-code/validate.js — see design-to-code/validation/EXTRACTION_REPORT.md and MAPPING_REPORT.md for what it actually checks and found."
  }
}
