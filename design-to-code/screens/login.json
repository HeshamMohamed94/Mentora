{
  "screenId": "login",
  "screenNumber": 6,
  "role": ["Guest"],
  "platform": "Both",
  "shell": "minimal — logo/wordmark only, no Navbar link row",
  "routeIntent": "/{locale}/login",
  "referenceType": "approved-pattern",
  "sourceReferences": ["ux/SCREEN_UX_SPECS.md § 6", "design-system/COMPONENTS.md § Inputs, § Buttons", "product/SCREEN_INVENTORY.md § 6"],
  "d51Note": "D51 (2026-09-07): reclassified from 'ux-only' to 'approved-pattern' — no screen-specific showcase mockup exists (the archived '06-login-design-component-spec.png' is a generic, multi-screen-reused Form Controls sheet, not a Login-specific frame), but the screen is now deliberately and strictly derived from the locked design-system Form Controls pattern (COMPONENTS.md § Inputs/§ Buttons) rather than approximated ad hoc. See execution/DECISIONS_LOG.md D51.",

  "layout": { "structure": "centered form card at form-width cap, every breakpoint" },
  "sections": [
    { "order": 1, "id": "title", "content": ["\"Log in to Mentora\""] },
    { "order": 2, "id": "form", "content": ["Email TextField", "Password PasswordField", "\"Login\" PrimaryButton"] },
    { "order": 3, "id": "secondary", "content": ["\"New to Mentora? Register\" TextButton"] }
  ],
  "componentSequence": ["textField", "textField (passwordField variant)", "button.primary", "button.text"],
  "navigation": { "exitDestinations": ["role-appropriate landing", "Register"] },
  "states": { "loading": "Login button's own loading state", "error": "inline, non-field-specific ('Incorrect email or password') for security" },
  "ltrBehavior": "default", "rtlBehavior": "form fields/labels follow logical start alignment; email field content stays LTR-typed even inside an RTL form",
  "themeBehavior": "standard semantic token resolution",
  "responsiveRules": "form-width cap at every breakpoint; full-width fields on Mobile",
  "knownGaps": ["No showcase mockup of any kind exists for this screen — judged solely against design-system/COMPONENTS.md's TextField/PasswordField/Button specs.", "D51 fix: field-level client errors previously rendered the field's own label text (e.g. literally \"Email\") instead of a real message — now uses dedicated auth.emailInvalid/auth.passwordRequired copy. D51 fix: .mtx-auth-page did not vertically center the card on tall viewports — now centers via the same calc(100dvh - space.16) convention used by the Course Player shell."]
}
