{
  "screenId": "dashboard",
  "screenNumber": 8,
  "role": ["Student"],
  "platform": "Both (Web: \"Dashboard\"; Mobile \"Home\" not built)",
  "shell": "shared/navigation.json#/shells/authenticatedStudent",
  "routeIntent": "/{locale}/app",
  "referenceType": "exact-showcase",
  "sourceReferences": [
    "design-review-locked/Mentora Showcase.dc.html § 22 Product Preview — dashboard-dark-web variant",
    "ux/SCREEN_UX_SPECS.md § 8",
    "ux/WEB_UX.md § 5 (\"Avoiding Dashboard Over-Complexity\" — exactly 3 modules, no more than one StatCard row)",
    "product/SCREEN_INVENTORY.md § 8"
  ],
  "conflicts": [
    {
      "description": "The locked ux/WEB_UX.md § 5 caps the Dashboard at exactly 3 modules and 'no more than one StatCard row.' The current implementation (carried forward from an earlier D48 pass and extended in D49) renders a 4-stat-card row + an AI Tutor nudge card + an Up Next card in a 2-column right rail — a deliberate, disclosed deviation from the letter of the 3-module cap, extending an existing pre-D49 divergence rather than introducing a new one.",
      "resolution": "This screen spec records the CURRENT implementation's structure as-built (rank 4 evidence) while flagging the rank-1 conflict explicitly — chasing either direction (reverting to strict 3-module, or re-ratifying the richer layout) is a product/UX decision, not something this design-to-code pass resolves unilaterally.",
      "recordedIn": "design-to-code/validation/EXTRACTION_REPORT.md"
    }
  ],

  "layout": {
    "grid": "desktop: grid-cols-[minmax(0,2fr)_minmax(0,1fr)] — left column (Continue Learning + Recommended), right column (AI Tutor nudge + Up Next, stacked)",
    "stackBelowDesktop": "single column, right-rail content moves below left-column content"
  },

  "sections": [
    { "order": 1, "id": "greeting", "content": ["\"Welcome back, {name}\" — real heading (h2-equivalent)"] },
    { "order": 2, "id": "statRow", "content": ["4 statCards: courses in progress, avg. progress, courses completed, certificates earned"] },
    { "order": 3, "id": "continueLearning", "content": ["1 courseProgressCard (most recent in-progress course) — replaced by a single 'Start your first course' prompt if none in progress (module-level empty state, not a separate Empty screen)"] },
    { "order": 4, "id": "recommended", "content": ["3-4 courseCard row, from followed Learning Paths' next course or catalog highlights"] },
    { "order": 5, "id": "aiTutorNudge", "content": ["static brand-tinted card linking to /app/ai-tutor — no fabricated content"] },
    { "order": 6, "id": "upNext", "content": ["real-data-driven module (D49): up to 2 entries derived from CourseResponse/ProgressResponse — next lesson after primary Continue item's currentLessonId, plus secondary Continue item's own resume point. Hidden entirely if zero entries (no fabricated placeholder rows)."] }
  ],

  "componentSequence": ["card.statCard", "card.courseProgressCard", "card.courseCard", "card.learningPathCard", "progressBar"],

  "typographyHierarchy": { "greeting": "typography.heading.h2 (or h1-equivalent per implementation)", "moduleHeadings": "typography.heading.h2 (real headings, not styled divs — screen-reader jumpability)", "statValue": "typography.heading.h2", "statLabel": "typography.body.small" },

  "artworkRules": "shared/artwork.json — Continue Learning and Recommended course thumbnails.",
  "navigation": { "exitDestinations": ["Course Player (Continue)", "Explore", "My Learning", "AI Tutor"] },

  "states": { "loading": "skeletons per module", "empty": "module 1 degrades to a single prompt; module 'Up Next' hides entirely if no real data exists — never fabricated" },

  "ltrBehavior": "default", "rtlBehavior": "greeting and module titles start-aligned; Continue CTA position follows logical end",
  "themeBehavior": "standard semantic token resolution, verified Light/Dark/EN/AR live (D49 browser verification)",
  "responsiveRules": "modules stack vertically at every breakpoint; module 2's course row uses courseGrid column count",
  "knownGaps": ["4-stat-card + AI-nudge + Up-Next layout exceeds ux/WEB_UX.md § 5's literal 3-module/1-StatCard-row cap — disclosed conflict above, not silently resolved."]
}
