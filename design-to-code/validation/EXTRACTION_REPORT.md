# Design-to-Code — Extraction Report

What was extracted directly from the locked Mentora Showcase, what was sourced from the Design System, what was inferred (and why), every conflict discovered between sources, and unresolved ambiguities. See `design-to-code/SOURCE_MANIFEST.json` for the precedence rule every conflict below was resolved against.

---

## 1. Values extracted directly from the Showcase

Programmatic extraction (not screenshot-eyeballing) from `design-review-locked/Mentora Showcase.dc.html`'s raw HTML/CSS:

- **`grid-template-columns` declarations** — a full family of `repeat(auto-fit/auto-fill, minmax(Npx,1fr))` patterns (96px, 120-360px in ~20px/40px steps) plus fixed multi-column layouts (`104px repeat(6,1fr)`, `2.2fr 1.4fr 1.2fr 1fr 0.5fr` DataTable-shaped ratios, `260px minmax(0,1fr) minmax(0,1fr)`, `repeat(3,1fr)`). These corroborate the design system's grid/courseGrid token structure (fluid card grids at multiple minimum widths) rather than contradicting it.
- **`max-width` declarations** — `1280px`, `1000px`, `900px`, `360px`, `340px`, `320px`, `280px`. The `1280px` value directly corroborates `design-tokens.json → grid.preferredMarketingWidth` ("1200-1280"), confirming Landing/marketing pages' content cap is a real, showcase-verified number, not an invented one.
- **`aspect-ratio:16/9`** — exactly one declaration, confirming the 16:9 convention design-tokens.json's `component.videoPlayer.aspectRatio` and `COMPONENTS.md`'s CourseCard thumbnail spec both already state. Used as corroboration in `shared/artwork.json#/aspectRatio`.
- **Course-artwork governance quote** (§ 12): *"these bases and motifs are an artwork system, not application UI colours — they must never enter the semantic colour tokens"* and *"artwork scales from an 88px list thumbnail to a full-width hero without re-composition"* — quoted verbatim in `shared/artwork.json#/governanceNote` and `#/aspectRatio`.

## 2. Values sourced from the Design System (rank 2, no showcase extraction needed)

The overwhelming majority of `design-to-code/shared/*.json` — every color, typography scale step, spacing step, radius, elevation level, motion duration/easing, icon size, breakpoint, and component-state table — is copied directly from `design-system/design-tokens.json` + `themes/theme-{light,dark}.json` + `COMPONENTS.md`, per `SOURCE_MANIFEST.json` rank 2. These are locked, hand-authored, and were never in question; extraction from the showcase was unnecessary and would have been strictly worse evidence than the token file itself.

## 3. Values marked "inferred"

- `shared/typography.json#/showcaseCorroboration` — per-type-scale-step showcase pixel values were **not** individually cross-referenced, because the showcase's CSS uses component-scoped class names, not `typography.scale.*` names, so a byte-exact per-step mapping would require guessing which showcase rule corresponds to which named step. Marked `inferred`; the design-tokens.json values are used as-is (rank 2 authority) and were visually confirmed at the correct rendered size during the D49 browser pass.
- `screens/landing.json#/layout/heroCollage` — the showcase's hero collage uses a large+medium+wide mixed-size composition; the current implementation's 3-equal-column grid is an approximation, explicitly marked `inferred proportions` rather than claimed exact.
- Originally, `shared/artwork.json`'s 5 motif entries were marked `inferred` (gradient stops not given byte-exact by the showcase, which only names the "dark purple/indigo base" rule, not literal hex values per motif). **This was upgraded during the pipeline build**: reading `web/src/components/ui/course-thumbnail.tsx` (rank 4, implementation evidence) surfaced the actual, already-correct gradient CSS in use — copied verbatim into `shared/artwork.json` and re-labeled `source: web/src/components/ui/course-thumbnail.tsx (implementation evidence)`. This is evidence, not new design authority — the values were already correct per the showcase's *rule* (dark purple/indigo base), just not independently re-derivable from the showcase's own bytes with pixel precision.

## 4. Conflicts discovered (recorded, never silently resolved)

1. **Course Player shell.** The showcase's own caption over-states its Course Player exception as *"the one documented exception to authenticated sidebar navigation,"* readable as "no sidebar at all." The locked `ux/SCREEN_UX_SPECS.md § 10` says "Sidebar collapsed by default" — a collapse, not a removal. **Resolved in favor of rank 1 (UX)**: `shared/navigation.json#/shells/coursePlayerShell` encodes the collapse-only rule as locked, with the conflict explicitly recorded there and in `screens/course-player.json#/conflicts`.
2. **Instructor Dashboard stat cards/columns.** The showcase's reference mockup shows 4 stat cards (incl. "Avg. completion rate") and Publish-toggle/Students/Updated-date DataTable columns. The locked `ux/INSTRUCTOR_ADMIN_UX.md § 1` specifies exactly 3 stat cards and a different column set. **Resolved in favor of rank 1**: `screens/instructor-dashboard.json#/conflicts` and the current implementation both hold the locked 3-card/column set — pre-existing, re-confirmed (not newly discovered), see `execution/DECISIONS_LOG.md` D48 §7/D49.
3. **Course Editor Media tab / persistent Curriculum rail.** The showcase's mockup shows a 3-tab (Details/Curriculum/Media) layout with a persistent right-hand panel. The locked `product/SCREEN_INVENTORY.md §C` specifies exactly two separate tab-screens, no Media tab, no split panel. **Resolved in favor of rank 1**: `screens/course-editor-overview.json#/conflicts` and `course-editor-curriculum.json#/conflicts` record this as the largest single driver of those two screens' fidelity ceiling — a product/UX decision to re-ratify or not, out of scope for this pipeline to resolve unilaterally.
4. **Dashboard module count.** The locked `ux/WEB_UX.md § 5` caps the Dashboard at exactly 3 modules and "no more than one StatCard row." The current implementation (pre-existing from D48, extended in D49's Up Next module) ships 4 stat cards + an AI Tutor nudge + an Up Next card. **Recorded as an open, disclosed deviation** in `screens/dashboard.json#/conflicts` — not resolved either direction by this pipeline, since reverting or re-ratifying is a product decision, not a data-modeling one.
5. **Explore's reference ambiguity.** The showcase's own captured reference frame for Explore is itself a pair of stacked MOBILE mockups (a Dashboard mobile screen + an Explore mobile screen), not an assembled desktop composition — an artifact of how the showcase's own Section 22 capture was organized, not a deliberate mobile-only intent for this screen. Recorded in `screens/explore.json#/conflicts` as a source-quality caveat, not a product/UX conflict.

## 5. Unresolved ambiguities

- The exact per-motif gradient-stop-count rationale (why each of the 5 motifs uses a different repeating-pattern function) is not documented anywhere in the locked sources — it reads as a deliberate "each subject gets a visually distinct texture" design choice, but no source states this explicitly. Left as an observation in `shared/artwork.json`, not asserted as a rule.
- Whether the Dashboard's 4-module/4-stat-card layout (conflict #4 above) should be formally re-ratified as an intentional deviation from `ux/WEB_UX.md § 5`, or reverted to the locked 3-module cap, is unresolved — flagged for a product/UX decision, not decided here.
- Whether Admin's screens (25-29) should eventually get `design-to-code/screens/` specs is unresolved by design — deliberately deferred past this phase to avoid any appearance of starting Task 12 (see `COVERAGE_REPORT.md`).

## 6. 2026-09-13 pass — Mobile visual references extracted (Phase 4 Android Task 3; screen count 29 → 37)

Phase 2's design-to-code pass (§§ 1-5 above) never captured the locked showcase's genuine mobile/Android device mockups — every one of the 29 pre-existing `screens/*.json` files was Web-composed. This pass reads those real mobile mockups directly (never a screenshot impression) and adds 8 new `design-to-code/screens/mobile-*.json` files for the mobile screens the showcase actually shows, plus a new `mobileStudentShell` entry in `shared/navigation.json` and an updated `android` readiness note in `shared/platform-contract.json`. See `execution/PHASE_4_ANDROID_PLAN.md` § 4 for the task brief this pass satisfies.

**What was extracted, and from where (all line numbers re-verified by direct read of `design-review-locked/Mentora Showcase.dc.html`, not taken on faith from the plan's approximate citations):**

- **§ 17 Navigation** (lines 1416-1534) — the `MobileBottomNavigation` component itself, LTR (lines 1504-1512) and RTL with real Arabic labels (lines 1513-1521): 64px height + top border, 24px icons, `typography.caption`-matching labels (byte-identical to `shared/typography.json#/caption`: 12/16/400/0.4), active `color.brand.primary` / inactive `color.text.secondary`, no badges. This is the source for `shared/navigation.json#/shells/mobileStudentShell`'s `visualValues`.
- **§ 19 English and Arabic** (lines 1647-1812) — the `PLAYER · EN` (lines 1707-1724) / `PLAYER · AR` (lines 1725-1744) frames: chrome mirrors via logical properties (icon row order unchanged in the DOM, `margin-inline-start:auto` flips visually), the scrubber is wrapped in its own explicit `dir="ltr"` container regardless of the outer frame's direction — the authoritative confirmation of `mobile-course-player.json`'s one RTL exception.
- **§ 20 Responsive** (lines 1814-1921) — the "MOBILE · 360" Explore composition (lines 1830-1868) and the locked guidance quote at line 1918 ("the sidebar becomes a bottom nav with five destinations, the filter row becomes horizontally scrollable chips, and the card action goes full-width for thumb reach"), quoted into `mobile-explore.json`.
- **§ 22 Product Preview, "MOBILE PREVIEW"** (lines 2032-2409, 340×788 frames) — Home (2036-2069), Explore (2071-2115), Course Details (2117-2156), My Learning (2158-2197), AI Tutor (2199-2232), Course Player with its Bottom Sheet curriculum (2234-2278), Demo Checkout (2280-2308), Purchase Success (2310-2326), Home · Arabic RTL (2328-2372), and Home · Dark Mode (2374-2409). Each frame's exact structure/content was read and cited per-section inside its corresponding `mobile-*.json` file, not paraphrased.

**The resolved nav-label conflict ("Learning" vs "My Learning"):** confirmed by direct read of every occurrence, not assumed from the plan's description. `§ 17`'s own dedicated `MobileBottomNavigation` component block (both LTR and RTL rows) already correctly says **"My Learning"** / `مساحة التعلّم` — it does not disagree with rank-1 sources. The actual disagreement is English-microcopy-only, confined to `§§ 20/22`'s five separate assembled illustration frames (line 1864, and lines 2063/2109/2191/2403), every one of which labels bottom-nav item 3 the single word **"Learning"** — including, ironically, inside the My Learning screen's own preview frame (page heading "My Learning" at line 2162, its own active nav label reads "Learning" at line 2191). The Arabic label is unaffected throughout, including inside `§ 22`'s own Home · Arabic RTL frame (line 2366: `مساحة التعلّم`, matching `§ 17` exactly) — so this is not a translation disagreement, only an English shorthand used in the smaller illustration chrome. **Resolved to "My Learning"** per `SOURCE_MANIFEST.json`'s rank-1-over-rank-3 precedence (`ux/MOBILE_UX.md § 1`, `product/INFORMATION_ARCHITECTURE.md § 3`, `ux/NAVIGATION_SPEC.md § 3` all lock "My Learning"), which also happens to agree with `§ 17`'s own correct component spec. Recorded in full in `shared/navigation.json#/shells/mobileStudentShell/labelConflict` and cross-referenced from `mobile-home.json`, `mobile-explore.json`, and `mobile-my-learning.json`'s own `conflicts[]` arrays.

**Other conflicts discovered and disclosed this pass (never silently resolved):**

1. **Course Details' bottom-nav visibility.** `ux/MOBILE_UX.md § 1` locks Course Details as a nav-visible screen ("still 'browsing,' not 'doing'"), but the showcase's own Course Details mobile frame (lines 2117-2156) shows no nav row at all — its 788px height is fully consumed by header + content + a sticky price/CTA footer. Resolved in favor of rank-1: the nav stays present, read alongside the sticky CTA bar; the showcase's omission is treated as a space constraint of that one frame, not a hide instruction (Course Details is never listed anywhere beside Course Player/Quiz's explicit hide rule).
2. **Learning Paths' segment/tab control has no direct visual evidence.** `ux/MOBILE_UX.md § 3` and `product/INFORMATION_ARCHITECTURE.md § 3` both lock a segment/tab into Learning Paths from within Explore; neither showcase Explore frame shows one — `§ 22`'s Explore frame instead shows a single inline `LearningPathCard` promo. Genuinely ambiguous evidence, disclosed rather than guessed in `mobile-explore.json#/conflicts[1]`; rank-1 governs the required behavior regardless.
3. **Purchase Success's secondary-action label.** `product/SCREEN_INVENTORY.md § 19` locks "Back to My Learning" (matching Web exactly); the showcase's mobile frame instead labels this button "View Demo Summary" (line 2322), which could imply reviewing the order rather than navigating to My Learning. Resolved in favor of rank-1 in `mobile-purchase-success.json#/conflicts[0]` — the showcase's alternate wording is not adopted and not asserted to be intentional, just disclosed.
4. **AI Tutor's mobile frame is the contextual variant, not the tab-root variant**, and shows no bottom-nav row at all — whether the true tab-root AI Tutor screen shows the nav (as rank-1's general persistence rule would imply) is inferred, not shown, and disclosed as such in `mobile-ai-tutor.json#/conflicts[0]`.
5. **Course Player's own Bottom Sheet frame is captured fully expanded**, not at the "partial, several lessons visible" default height `ux/MOBILE_UX.md § 7` describes — disclosed in `mobile-course-player.json#/knownGaps` as inferred-from-text rather than pixel-verified.

**Deliberately NOT authored this pass — the 10 remaining in-scope mobile screens with no showcase mockup:** Login, Register, Learning Paths (Explore segment), Learning Path Details, Quiz, Quiz Results, Certificates List, Certificate Detail, Profile, Settings. None of these appear anywhere in the showcase's mobile sections (§§ 17, 19, 20, 22 read in full, plus a scan of the rest of the file for any other mobile-labelled frame) — a genuine, confirmed absence, not an oversight. These remain `ux-only` honest gaps for a later Phase 4 task to build directly from `ux/SCREEN_UX_SPECS.md`/`MOBILE_UX.md`, per the task brief; no placeholder files were created for them here.

**Shell/platform-contract changes:** `shared/navigation.json#/shells/mobileStudentShell` is new (this pass added it — it did not exist before), carrying the 5-item order/labels, LTR+RTL values, tap-active-pops-to-root behavior, the nav-hidden-on-Course-Player/Quiz rule, and an explicit disclosed platform difference from Web's `coursePlayerShell` (mobile fully HIDES the nav there; Web only COLLAPSES its Sidebar to 72px icon-only — never the same mechanism, sanctioned as platform-appropriate per `design-system/DESIGN_RULES.md` rule 10). `shared/platform-contract.json#/android/readinessNotes`'s last bullet, which previously called the mobile shell "not-yet-specified," was updated to point at `mobileStudentShell` — no other part of that file was touched (the overall Android code-generator `status` line remains `NOT IMPLEMENTED`, unaffected by this screen/shell-spec-only pass).

**Verification performed:** `node tools/design-to-code/validate.js` — 37 screens (29 → 37), 6 patterns, 11 shared files, 0 errors. `node tools/design-to-code/generate.js` — succeeds with no errors; `web/src/lib/design-to-code.generated.ts` and `design-to-code/generated/web/resolved-screens.json` gained exactly 8 new additive entries (the generator walks the whole `screens/` directory, not a fixed known-ID list); a byte-diff against the pre-change output confirms every one of the pre-existing 29 screens' generated entries is untouched — purely additive, no corruption of existing Web-relevant output. `design-to-code/generated/web/component-index.json` is byte-identical (no new component recipe names were introduced). `web`'s own `npm run typecheck` and `npm run lint` both stayed clean (0 errors; lint's one warning is pre-existing and unrelated to this pass, in `course-thumbnail.tsx`, not touched here).
